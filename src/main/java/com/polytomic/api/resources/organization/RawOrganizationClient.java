/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.polytomic.api.resources.organization;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.polytomic.api.core.ClientOptions;
import com.polytomic.api.core.IdempotentRequestOptions;
import com.polytomic.api.core.MediaTypes;
import com.polytomic.api.core.ObjectMappers;
import com.polytomic.api.core.PolytomicApiException;
import com.polytomic.api.core.PolytomicException;
import com.polytomic.api.core.PolytomicHttpResponse;
import com.polytomic.api.core.RequestOptions;
import com.polytomic.api.errors.ConflictError;
import com.polytomic.api.errors.ForbiddenError;
import com.polytomic.api.errors.InternalServerError;
import com.polytomic.api.errors.NotFoundError;
import com.polytomic.api.errors.UnauthorizedError;
import com.polytomic.api.errors.UnprocessableEntityError;
import com.polytomic.api.resources.organization.requests.CreateOrganizationRequestSchema;
import com.polytomic.api.resources.organization.requests.UpdateOrganizationRequestSchema;
import com.polytomic.api.types.ApiError;
import com.polytomic.api.types.OrganizationEnvelope;
import com.polytomic.api.types.OrganizationsEnvelope;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawOrganizationClient {
    protected final ClientOptions clientOptions;

    public RawOrganizationClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns the organization the caller is authenticated against.
     * <p>This endpoint is the safest way to discover the effective organization for a
     * user-scoped or organization-scoped credential. It does not let callers inspect
     * arbitrary organizations; it only returns the organization implied by the
     * credential that authenticated the request.</p>
     * <p>If you need to enumerate or look up organizations across a partner account, use
     * <a href="../../api-reference/organization/list"><code>GET /api/organizations</code></a> or
     * <a href="../../api-reference/organization/get"><code>GET /api/organizations/{id}</code></a> instead.</p>
     */
    public PolytomicHttpResponse<OrganizationEnvelope> getCurrent() {
        return getCurrent(null);
    }

    /**
     * Returns the organization the caller is authenticated against.
     * <p>This endpoint is the safest way to discover the effective organization for a
     * user-scoped or organization-scoped credential. It does not let callers inspect
     * arbitrary organizations; it only returns the organization implied by the
     * credential that authenticated the request.</p>
     * <p>If you need to enumerate or look up organizations across a partner account, use
     * <a href="../../api-reference/organization/list"><code>GET /api/organizations</code></a> or
     * <a href="../../api-reference/organization/get"><code>GET /api/organizations/{id}</code></a> instead.</p>
     */
    public PolytomicHttpResponse<OrganizationEnvelope> getCurrent(RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/organization");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, OrganizationEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Lists every organization accessible to the calling partner, with the partner's owner organization first.
     * <p>In <code>2025-09-18</code>, this endpoint is partner-scoped rather than a general
     * &quot;current caller visibility&quot; listing. The partner owner organization is returned
     * first, followed by child organizations.</p>
     * <p>This ordering matters for partner workflows such as shared connections, where
     * the parent connection must live in the partner owner organization.</p>
     * <p>If you need only the organization implied by the current credential, use
     * <a href="../../api-reference/organization/get-current"><code>GET /api/organization</code></a> instead.</p>
     */
    public PolytomicHttpResponse<OrganizationsEnvelope> list() {
        return list(null);
    }

    /**
     * Lists every organization accessible to the calling partner, with the partner's owner organization first.
     * <p>In <code>2025-09-18</code>, this endpoint is partner-scoped rather than a general
     * &quot;current caller visibility&quot; listing. The partner owner organization is returned
     * first, followed by child organizations.</p>
     * <p>This ordering matters for partner workflows such as shared connections, where
     * the parent connection must live in the partner owner organization.</p>
     * <p>If you need only the organization implied by the current credential, use
     * <a href="../../api-reference/organization/get-current"><code>GET /api/organization</code></a> instead.</p>
     */
    public PolytomicHttpResponse<OrganizationsEnvelope> list(RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/organizations");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, OrganizationsEnvelope.class), response);
            }
            try {
                if (response.code() == 500) {
                    throw new InternalServerError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Creates a new organization under the calling partner account, optionally configuring SSO or OIDC at creation time.
     * <blockquote>
     * <p>🚧 Requires partner key</p>
     * <p>This endpoint is only accessible using <a href="../../guides/obtaining-api-keys#partner-keys">partner keys</a>.</p>
     * </blockquote>
     * <p>SSO and OIDC settings supplied at creation time can be updated later via
     * <code>PUT /api/organizations/{id}</code>.</p>
     */
    public PolytomicHttpResponse<OrganizationEnvelope> create(CreateOrganizationRequestSchema request) {
        return create(request, null);
    }

    /**
     * Creates a new organization under the calling partner account, optionally configuring SSO or OIDC at creation time.
     * <blockquote>
     * <p>🚧 Requires partner key</p>
     * <p>This endpoint is only accessible using <a href="../../guides/obtaining-api-keys#partner-keys">partner keys</a>.</p>
     * </blockquote>
     * <p>SSO and OIDC settings supplied at creation time can be updated later via
     * <code>PUT /api/organizations/{id}</code>.</p>
     */
    public PolytomicHttpResponse<OrganizationEnvelope> create(
            CreateOrganizationRequestSchema request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/organizations");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, OrganizationEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns a single organization by ID.
     * <blockquote>
     * <p>📘 Credential scope varies by endpoint and API version</p>
     * <p>Organization endpoints do not all share the same credential requirements.
     * Check each endpoint's description for the caller scope that applies in that
     * API version.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<OrganizationEnvelope> get(String id) {
        return get(id, null);
    }

    /**
     * Returns a single organization by ID.
     * <blockquote>
     * <p>📘 Credential scope varies by endpoint and API version</p>
     * <p>Organization endpoints do not all share the same credential requirements.
     * Check each endpoint's description for the caller scope that applies in that
     * API version.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<OrganizationEnvelope> get(String id, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/organizations")
                .addPathSegment(id);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, OrganizationEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Updates an organization's name and SSO or OIDC configuration.
     * <blockquote>
     * <p>🚧 Requires partner key</p>
     * <p>This endpoint is only accessible using <a href="../../../guides/obtaining-api-keys#partner-keys">partner keys</a>.</p>
     * </blockquote>
     * <blockquote>
     * <p>📘 SSO and OIDC configuration is replaced in full on each update. Include all
     * desired settings in the request body, not just the fields you want to change.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<OrganizationEnvelope> update(String id, UpdateOrganizationRequestSchema request) {
        return update(id, request, null);
    }

    /**
     * Updates an organization's name and SSO or OIDC configuration.
     * <blockquote>
     * <p>🚧 Requires partner key</p>
     * <p>This endpoint is only accessible using <a href="../../../guides/obtaining-api-keys#partner-keys">partner keys</a>.</p>
     * </blockquote>
     * <blockquote>
     * <p>📘 SSO and OIDC configuration is replaced in full on each update. Include all
     * desired settings in the request body, not just the fields you want to change.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<OrganizationEnvelope> update(
            String id, UpdateOrganizationRequestSchema request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/organizations")
                .addPathSegment(id);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, OrganizationEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Deletes an organization.
     * <p>Partner callers cannot delete their own owner organization.</p>
     */
    public PolytomicHttpResponse<Void> remove(String id) {
        return remove(id, null);
    }

    /**
     * Deletes an organization.
     * <p>Partner callers cannot delete their own owner organization.</p>
     */
    public PolytomicHttpResponse<Void> remove(String id, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/organizations")
                .addPathSegment(id);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }
}
