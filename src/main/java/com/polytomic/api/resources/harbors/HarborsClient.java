/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.polytomic.api.resources.harbors;

import com.polytomic.api.core.ClientOptions;
import com.polytomic.api.core.IdempotentRequestOptions;
import com.polytomic.api.core.RequestOptions;
import com.polytomic.api.resources.harbors.requests.CreateHarborContextDraftRequest;
import com.polytomic.api.resources.harbors.requests.CreateHarborContextRequest;
import com.polytomic.api.resources.harbors.requests.CreateHarborRequest;
import com.polytomic.api.resources.harbors.requests.HarborsCloseSessionRequest;
import com.polytomic.api.resources.harbors.requests.HarborsDeleteContextDraftRequest;
import com.polytomic.api.resources.harbors.requests.HarborsDeleteContextRequest;
import com.polytomic.api.resources.harbors.requests.HarborsGetAuthorizedSchemaRequest;
import com.polytomic.api.resources.harbors.requests.HarborsGetContextDraftRequest;
import com.polytomic.api.resources.harbors.requests.HarborsGetContextRequest;
import com.polytomic.api.resources.harbors.requests.HarborsGetContextVersionRequest;
import com.polytomic.api.resources.harbors.requests.HarborsListAuthorizedConnectionsRequest;
import com.polytomic.api.resources.harbors.requests.HarborsListAuthorizedSchemasRequest;
import com.polytomic.api.resources.harbors.requests.HarborsListContextDraftsRequest;
import com.polytomic.api.resources.harbors.requests.HarborsListContextVersionsRequest;
import com.polytomic.api.resources.harbors.requests.HarborsListContextsRequest;
import com.polytomic.api.resources.harbors.requests.HarborsListKeysRequest;
import com.polytomic.api.resources.harbors.requests.HarborsListRequest;
import com.polytomic.api.resources.harbors.requests.HarborsListUsersRequest;
import com.polytomic.api.resources.harbors.requests.HarborsPromoteContextDraftRequest;
import com.polytomic.api.resources.harbors.requests.InviteHarborUserRequest;
import com.polytomic.api.resources.harbors.requests.RegisterHarborSessionRequest;
import com.polytomic.api.resources.harbors.requests.ResolveHarborSourceMappingsRequest;
import com.polytomic.api.resources.harbors.requests.SaveHarborContextDraftRequest;
import com.polytomic.api.resources.harbors.requests.SaveHarborContextRequest;
import com.polytomic.api.resources.harbors.requests.UpdateHarborRequest;
import com.polytomic.api.types.CloseHarborSessionEnvelope;
import com.polytomic.api.types.CreateHarborEnvelope;
import com.polytomic.api.types.DeletedHarborContextDraftEnvelope;
import com.polytomic.api.types.DeletedHarborContextEnvelope;
import com.polytomic.api.types.DeletedHarborEnvelope;
import com.polytomic.api.types.HarborConnectionListEnvelope;
import com.polytomic.api.types.HarborConnectionSchemaEnvelope;
import com.polytomic.api.types.HarborContextDraftEnvelope;
import com.polytomic.api.types.HarborContextDraftListEnvelope;
import com.polytomic.api.types.HarborContextEnvelope;
import com.polytomic.api.types.HarborContextListEnvelope;
import com.polytomic.api.types.HarborContextVersionEnvelope;
import com.polytomic.api.types.HarborContextVersionListEnvelope;
import com.polytomic.api.types.HarborEnvelope;
import com.polytomic.api.types.HarborKeyEnvelope;
import com.polytomic.api.types.HarborKeyListEnvelope;
import com.polytomic.api.types.HarborListEnvelope;
import com.polytomic.api.types.HarborSchemaListEnvelope;
import com.polytomic.api.types.HarborStatusEnvelope;
import com.polytomic.api.types.HarborUserEnvelope;
import com.polytomic.api.types.HarborUserListEnvelope;
import com.polytomic.api.types.RegisterHarborSessionEnvelope;
import com.polytomic.api.types.ResolveHarborSourceMappingsEnvelope;
import com.polytomic.api.types.RevokedHarborKeyEnvelope;
import com.polytomic.api.types.UnassignedHarborUserEnvelope;

public class HarborsClient {
    protected final ClientOptions clientOptions;

    private final RawHarborsClient rawClient;

    public HarborsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawHarborsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawHarborsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Lists profile-authorized connections and capabilities for the current Harbor credential.
     */
    public HarborConnectionListEnvelope listAuthorizedConnections() {
        return this.rawClient.listAuthorizedConnections().body();
    }

    /**
     * Lists profile-authorized connections and capabilities for the current Harbor credential.
     */
    public HarborConnectionListEnvelope listAuthorizedConnections(RequestOptions requestOptions) {
        return this.rawClient.listAuthorizedConnections(requestOptions).body();
    }

    /**
     * Lists profile-authorized connections and capabilities for the current Harbor credential.
     */
    public HarborConnectionListEnvelope listAuthorizedConnections(HarborsListAuthorizedConnectionsRequest request) {
        return this.rawClient.listAuthorizedConnections(request).body();
    }

    /**
     * Lists profile-authorized connections and capabilities for the current Harbor credential.
     */
    public HarborConnectionListEnvelope listAuthorizedConnections(
            HarborsListAuthorizedConnectionsRequest request, RequestOptions requestOptions) {
        return this.rawClient.listAuthorizedConnections(request, requestOptions).body();
    }

    /**
     * Lists one bounded page of schema resources authorized by the current Harbor profile.
     */
    public HarborSchemaListEnvelope listAuthorizedSchemas(String connectionId) {
        return this.rawClient.listAuthorizedSchemas(connectionId).body();
    }

    /**
     * Lists one bounded page of schema resources authorized by the current Harbor profile.
     */
    public HarborSchemaListEnvelope listAuthorizedSchemas(String connectionId, RequestOptions requestOptions) {
        return this.rawClient
                .listAuthorizedSchemas(connectionId, requestOptions)
                .body();
    }

    /**
     * Lists one bounded page of schema resources authorized by the current Harbor profile.
     */
    public HarborSchemaListEnvelope listAuthorizedSchemas(
            String connectionId, HarborsListAuthorizedSchemasRequest request) {
        return this.rawClient.listAuthorizedSchemas(connectionId, request).body();
    }

    /**
     * Lists one bounded page of schema resources authorized by the current Harbor profile.
     */
    public HarborSchemaListEnvelope listAuthorizedSchemas(
            String connectionId, HarborsListAuthorizedSchemasRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .listAuthorizedSchemas(connectionId, request, requestOptions)
                .body();
    }

    /**
     * Returns one schema resource authorized by the current Harbor profile.
     */
    public HarborConnectionSchemaEnvelope getAuthorizedSchema(String connectionId, String schemaId) {
        return this.rawClient.getAuthorizedSchema(connectionId, schemaId).body();
    }

    /**
     * Returns one schema resource authorized by the current Harbor profile.
     */
    public HarborConnectionSchemaEnvelope getAuthorizedSchema(
            String connectionId, String schemaId, RequestOptions requestOptions) {
        return this.rawClient
                .getAuthorizedSchema(connectionId, schemaId, requestOptions)
                .body();
    }

    /**
     * Returns one schema resource authorized by the current Harbor profile.
     */
    public HarborConnectionSchemaEnvelope getAuthorizedSchema(
            String connectionId, String schemaId, HarborsGetAuthorizedSchemaRequest request) {
        return this.rawClient
                .getAuthorizedSchema(connectionId, schemaId, request)
                .body();
    }

    /**
     * Returns one schema resource authorized by the current Harbor profile.
     */
    public HarborConnectionSchemaEnvelope getAuthorizedSchema(
            String connectionId,
            String schemaId,
            HarborsGetAuthorizedSchemaRequest request,
            RequestOptions requestOptions) {
        return this.rawClient
                .getAuthorizedSchema(connectionId, schemaId, request, requestOptions)
                .body();
    }

    /**
     * Registers a service-attested MCP transport session for a scoped Harbor credential.
     */
    public RegisterHarborSessionEnvelope registerSession() {
        return this.rawClient.registerSession().body();
    }

    /**
     * Registers a service-attested MCP transport session for a scoped Harbor credential.
     */
    public RegisterHarborSessionEnvelope registerSession(IdempotentRequestOptions requestOptions) {
        return this.rawClient.registerSession(requestOptions).body();
    }

    /**
     * Registers a service-attested MCP transport session for a scoped Harbor credential.
     */
    public RegisterHarborSessionEnvelope registerSession(RegisterHarborSessionRequest request) {
        return this.rawClient.registerSession(request).body();
    }

    /**
     * Registers a service-attested MCP transport session for a scoped Harbor credential.
     */
    public RegisterHarborSessionEnvelope registerSession(
            RegisterHarborSessionRequest request, IdempotentRequestOptions requestOptions) {
        return this.rawClient.registerSession(request, requestOptions).body();
    }

    /**
     * Closes a service-attested Harbor MCP transport session.
     */
    public CloseHarborSessionEnvelope closeSession(String sessionId) {
        return this.rawClient.closeSession(sessionId).body();
    }

    /**
     * Closes a service-attested Harbor MCP transport session.
     */
    public CloseHarborSessionEnvelope closeSession(String sessionId, IdempotentRequestOptions requestOptions) {
        return this.rawClient.closeSession(sessionId, requestOptions).body();
    }

    /**
     * Closes a service-attested Harbor MCP transport session.
     */
    public CloseHarborSessionEnvelope closeSession(String sessionId, HarborsCloseSessionRequest request) {
        return this.rawClient.closeSession(sessionId, request).body();
    }

    /**
     * Closes a service-attested Harbor MCP transport session.
     */
    public CloseHarborSessionEnvelope closeSession(
            String sessionId, HarborsCloseSessionRequest request, IdempotentRequestOptions requestOptions) {
        return this.rawClient.closeSession(sessionId, request, requestOptions).body();
    }

    /**
     * Lists Harbors in the caller's current organization.
     * <p>Returns Harbors in creation order. Use <code>pagination.next_page_token</code> to continue when more results are available.</p>
     */
    public HarborListEnvelope list() {
        return this.rawClient.list().body();
    }

    /**
     * Lists Harbors in the caller's current organization.
     * <p>Returns Harbors in creation order. Use <code>pagination.next_page_token</code> to continue when more results are available.</p>
     */
    public HarborListEnvelope list(RequestOptions requestOptions) {
        return this.rawClient.list(requestOptions).body();
    }

    /**
     * Lists Harbors in the caller's current organization.
     * <p>Returns Harbors in creation order. Use <code>pagination.next_page_token</code> to continue when more results are available.</p>
     */
    public HarborListEnvelope list(HarborsListRequest request) {
        return this.rawClient.list(request).body();
    }

    /**
     * Lists Harbors in the caller's current organization.
     * <p>Returns Harbors in creation order. Use <code>pagination.next_page_token</code> to continue when more results are available.</p>
     */
    public HarborListEnvelope list(HarborsListRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(request, requestOptions).body();
    }

    /**
     * Creates a managed or customer-managed Harbor in the caller's current organization.
     * <p><code>generate_api_key</code> defaults to <code>true</code>. Polytomic returns a new plaintext credential only in this response. Set it to <code>false</code> to create the Harbor without a credential.</p>
     * <p>For <code>customer_managed</code>, <code>backing_connection_id</code> must identify a queryable Connection that your credential can access.</p>
     */
    public CreateHarborEnvelope create(CreateHarborRequest request) {
        return this.rawClient.create(request).body();
    }

    /**
     * Creates a managed or customer-managed Harbor in the caller's current organization.
     * <p><code>generate_api_key</code> defaults to <code>true</code>. Polytomic returns a new plaintext credential only in this response. Set it to <code>false</code> to create the Harbor without a credential.</p>
     * <p>For <code>customer_managed</code>, <code>backing_connection_id</code> must identify a queryable Connection that your credential can access.</p>
     */
    public CreateHarborEnvelope create(CreateHarborRequest request, IdempotentRequestOptions requestOptions) {
        return this.rawClient.create(request, requestOptions).body();
    }

    /**
     * Returns one Harbor by its first-class Harbor ID.
     * <p>The response exposes the backing Connection ID but not the internal profile used to authorize Harbor credentials.</p>
     */
    public HarborEnvelope get(String harborId) {
        return this.rawClient.get(harborId).body();
    }

    /**
     * Returns one Harbor by its first-class Harbor ID.
     * <p>The response exposes the backing Connection ID but not the internal profile used to authorize Harbor credentials.</p>
     */
    public HarborEnvelope get(String harborId, RequestOptions requestOptions) {
        return this.rawClient.get(harborId, requestOptions).body();
    }

    /**
     * Updates a Harbor's name and description.
     * <p>This operation does not change <code>backing_mode</code> or <code>backing_connection_id</code>.</p>
     */
    public HarborEnvelope update(String harborId, UpdateHarborRequest request) {
        return this.rawClient.update(harborId, request).body();
    }

    /**
     * Updates a Harbor's name and description.
     * <p>This operation does not change <code>backing_mode</code> or <code>backing_connection_id</code>.</p>
     */
    public HarborEnvelope update(
            String harborId, UpdateHarborRequest request, IdempotentRequestOptions requestOptions) {
        return this.rawClient.update(harborId, request, requestOptions).body();
    }

    /**
     * Deletes a Harbor and revokes its credentials.
     * <blockquote>
     * <p>🚧 Harbor deletion</p>
     * <p>Deleting a Harbor revokes its credentials, context documents, and user assignments. A customer-managed backing Connection remains available. Polytomic deletes a managed backing Connection only when no other resource uses it.</p>
     * </blockquote>
     */
    public DeletedHarborEnvelope delete(String harborId) {
        return this.rawClient.delete(harborId).body();
    }

    /**
     * Deletes a Harbor and revokes its credentials.
     * <blockquote>
     * <p>🚧 Harbor deletion</p>
     * <p>Deleting a Harbor revokes its credentials, context documents, and user assignments. A customer-managed backing Connection remains available. Polytomic deletes a managed backing Connection only when no other resource uses it.</p>
     * </blockquote>
     */
    public DeletedHarborEnvelope delete(String harborId, IdempotentRequestOptions requestOptions) {
        return this.rawClient.delete(harborId, requestOptions).body();
    }

    /**
     * Lists context document metadata for a Harbor without returning document content.
     * <p>Collection items include the current published <code>version</code> number and omit
     * <code>content</code>. Use the context item endpoint to retrieve a complete document.</p>
     * <p>A Harbor profile credential can read context only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public HarborContextListEnvelope listContexts(String harborId) {
        return this.rawClient.listContexts(harborId).body();
    }

    /**
     * Lists context document metadata for a Harbor without returning document content.
     * <p>Collection items include the current published <code>version</code> number and omit
     * <code>content</code>. Use the context item endpoint to retrieve a complete document.</p>
     * <p>A Harbor profile credential can read context only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public HarborContextListEnvelope listContexts(String harborId, RequestOptions requestOptions) {
        return this.rawClient.listContexts(harborId, requestOptions).body();
    }

    /**
     * Lists context document metadata for a Harbor without returning document content.
     * <p>Collection items include the current published <code>version</code> number and omit
     * <code>content</code>. Use the context item endpoint to retrieve a complete document.</p>
     * <p>A Harbor profile credential can read context only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public HarborContextListEnvelope listContexts(String harborId, HarborsListContextsRequest request) {
        return this.rawClient.listContexts(harborId, request).body();
    }

    /**
     * Lists context document metadata for a Harbor without returning document content.
     * <p>Collection items include the current published <code>version</code> number and omit
     * <code>content</code>. Use the context item endpoint to retrieve a complete document.</p>
     * <p>A Harbor profile credential can read context only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public HarborContextListEnvelope listContexts(
            String harborId, HarborsListContextsRequest request, RequestOptions requestOptions) {
        return this.rawClient.listContexts(harborId, request, requestOptions).body();
    }

    /**
     * Creates and attaches a context document to a Harbor.
     * <p>The new document belongs only to this Harbor. Context documents cannot be attached to multiple Harbors.</p>
     */
    public HarborContextEnvelope createContext(String harborId, CreateHarborContextRequest request) {
        return this.rawClient.createContext(harborId, request).body();
    }

    /**
     * Creates and attaches a context document to a Harbor.
     * <p>The new document belongs only to this Harbor. Context documents cannot be attached to multiple Harbors.</p>
     */
    public HarborContextEnvelope createContext(
            String harborId, CreateHarborContextRequest request, IdempotentRequestOptions requestOptions) {
        return this.rawClient.createContext(harborId, request, requestOptions).body();
    }

    /**
     * Lists mutable Harbor context drafts without returning document content.
     * <p>The collection includes drafts for published context documents and initial drafts
     * that have not yet been published. Use <code>context_id</code> with the draft detail,
     * replacement, promotion, and discard endpoints.</p>
     * <p>Draft metadata does not include <code>content</code>. Fetch a selected draft through its
     * detail endpoint to read the complete candidate. Normal context list and detail
     * operations continue to return published content only.</p>
     */
    public HarborContextDraftListEnvelope listContextDrafts(String harborId) {
        return this.rawClient.listContextDrafts(harborId).body();
    }

    /**
     * Lists mutable Harbor context drafts without returning document content.
     * <p>The collection includes drafts for published context documents and initial drafts
     * that have not yet been published. Use <code>context_id</code> with the draft detail,
     * replacement, promotion, and discard endpoints.</p>
     * <p>Draft metadata does not include <code>content</code>. Fetch a selected draft through its
     * detail endpoint to read the complete candidate. Normal context list and detail
     * operations continue to return published content only.</p>
     */
    public HarborContextDraftListEnvelope listContextDrafts(String harborId, RequestOptions requestOptions) {
        return this.rawClient.listContextDrafts(harborId, requestOptions).body();
    }

    /**
     * Lists mutable Harbor context drafts without returning document content.
     * <p>The collection includes drafts for published context documents and initial drafts
     * that have not yet been published. Use <code>context_id</code> with the draft detail,
     * replacement, promotion, and discard endpoints.</p>
     * <p>Draft metadata does not include <code>content</code>. Fetch a selected draft through its
     * detail endpoint to read the complete candidate. Normal context list and detail
     * operations continue to return published content only.</p>
     */
    public HarborContextDraftListEnvelope listContextDrafts(String harborId, HarborsListContextDraftsRequest request) {
        return this.rawClient.listContextDrafts(harborId, request).body();
    }

    /**
     * Lists mutable Harbor context drafts without returning document content.
     * <p>The collection includes drafts for published context documents and initial drafts
     * that have not yet been published. Use <code>context_id</code> with the draft detail,
     * replacement, promotion, and discard endpoints.</p>
     * <p>Draft metadata does not include <code>content</code>. Fetch a selected draft through its
     * detail endpoint to read the complete candidate. Normal context list and detail
     * operations continue to return published content only.</p>
     */
    public HarborContextDraftListEnvelope listContextDrafts(
            String harborId, HarborsListContextDraftsRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .listContextDrafts(harborId, request, requestOptions)
                .body();
    }

    /**
     * Creates an unpublished context document with its initial mutable draft.
     * <p>Creates a stable context identity and its initial mutable draft without publishing
     * content to the Harbor. The response includes <code>context_id</code>, which identifies the
     * draft replacement, promotion, and discard routes.</p>
     * <p>The initial draft has a null <code>base_revision_id</code>. It remains absent from normal
     * context list/detail, GraphQL, entity lookup, and Harbor MCP reads until promoted.
     * Use the regular context creation endpoint instead when the initial payload should
     * be published immediately.</p>
     */
    public HarborContextDraftEnvelope createContextDraft(String harborId, CreateHarborContextDraftRequest request) {
        return this.rawClient.createContextDraft(harborId, request).body();
    }

    /**
     * Creates an unpublished context document with its initial mutable draft.
     * <p>Creates a stable context identity and its initial mutable draft without publishing
     * content to the Harbor. The response includes <code>context_id</code>, which identifies the
     * draft replacement, promotion, and discard routes.</p>
     * <p>The initial draft has a null <code>base_revision_id</code>. It remains absent from normal
     * context list/detail, GraphQL, entity lookup, and Harbor MCP reads until promoted.
     * Use the regular context creation endpoint instead when the initial payload should
     * be published immediately.</p>
     */
    public HarborContextDraftEnvelope createContextDraft(
            String harborId, CreateHarborContextDraftRequest request, IdempotentRequestOptions requestOptions) {
        return this.rawClient
                .createContextDraft(harborId, request, requestOptions)
                .body();
    }

    /**
     * Returns one complete Harbor context document.
     * <p>The response includes the current published <code>version</code> number and complete
     * plain-text <code>content</code>.</p>
     * <p>A Harbor profile credential can read context only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public HarborContextEnvelope getContext(String harborId, String contextId) {
        return this.rawClient.getContext(harborId, contextId).body();
    }

    /**
     * Returns one complete Harbor context document.
     * <p>The response includes the current published <code>version</code> number and complete
     * plain-text <code>content</code>.</p>
     * <p>A Harbor profile credential can read context only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public HarborContextEnvelope getContext(String harborId, String contextId, RequestOptions requestOptions) {
        return this.rawClient.getContext(harborId, contextId, requestOptions).body();
    }

    /**
     * Returns one complete Harbor context document.
     * <p>The response includes the current published <code>version</code> number and complete
     * plain-text <code>content</code>.</p>
     * <p>A Harbor profile credential can read context only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public HarborContextEnvelope getContext(String harborId, String contextId, HarborsGetContextRequest request) {
        return this.rawClient.getContext(harborId, contextId, request).body();
    }

    /**
     * Returns one complete Harbor context document.
     * <p>The response includes the current published <code>version</code> number and complete
     * plain-text <code>content</code>.</p>
     * <p>A Harbor profile credential can read context only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public HarborContextEnvelope getContext(
            String harborId, String contextId, HarborsGetContextRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .getContext(harborId, contextId, request, requestOptions)
                .body();
    }

    /**
     * Replaces one Harbor context document.
     * <p>Each successful request publishes the next immutable version. You can omit
     * <code>change_note</code>.</p>
     * <p>A direct publication leaves an existing draft unchanged.</p>
     */
    public HarborContextEnvelope updateContext(String harborId, String contextId, SaveHarborContextRequest request) {
        return this.rawClient.updateContext(harborId, contextId, request).body();
    }

    /**
     * Replaces one Harbor context document.
     * <p>Each successful request publishes the next immutable version. You can omit
     * <code>change_note</code>.</p>
     * <p>A direct publication leaves an existing draft unchanged.</p>
     */
    public HarborContextEnvelope updateContext(
            String harborId,
            String contextId,
            SaveHarborContextRequest request,
            IdempotentRequestOptions requestOptions) {
        return this.rawClient
                .updateContext(harborId, contextId, request, requestOptions)
                .body();
    }

    /**
     * Deletes one context document from a Harbor.
     * <p>Deleting a context document does not affect the Harbor or its other context documents.</p>
     */
    public DeletedHarborContextEnvelope deleteContext(String harborId, String contextId) {
        return this.rawClient.deleteContext(harborId, contextId).body();
    }

    /**
     * Deletes one context document from a Harbor.
     * <p>Deleting a context document does not affect the Harbor or its other context documents.</p>
     */
    public DeletedHarborContextEnvelope deleteContext(
            String harborId, String contextId, IdempotentRequestOptions requestOptions) {
        return this.rawClient.deleteContext(harborId, contextId, requestOptions).body();
    }

    /**
     * Deletes one context document from a Harbor.
     * <p>Deleting a context document does not affect the Harbor or its other context documents.</p>
     */
    public DeletedHarborContextEnvelope deleteContext(
            String harborId, String contextId, HarborsDeleteContextRequest request) {
        return this.rawClient.deleteContext(harborId, contextId, request).body();
    }

    /**
     * Deletes one context document from a Harbor.
     * <p>Deleting a context document does not affect the Harbor or its other context documents.</p>
     */
    public DeletedHarborContextEnvelope deleteContext(
            String harborId,
            String contextId,
            HarborsDeleteContextRequest request,
            IdempotentRequestOptions requestOptions) {
        return this.rawClient
                .deleteContext(harborId, contextId, request, requestOptions)
                .body();
    }

    /**
     * Returns the mutable draft for a Harbor context document.
     * <p>Drafts are available only through the draft endpoints. <code>base_revision_id</code>
     * identifies the published revision from which the candidate was created. Normal
     * context reads and Harbor MCP tools continue to return the current published
     * version. Creator and updater IDs are null when their actor type is <code>system</code>.</p>
     */
    public HarborContextDraftEnvelope getContextDraft(String harborId, String contextId) {
        return this.rawClient.getContextDraft(harborId, contextId).body();
    }

    /**
     * Returns the mutable draft for a Harbor context document.
     * <p>Drafts are available only through the draft endpoints. <code>base_revision_id</code>
     * identifies the published revision from which the candidate was created. Normal
     * context reads and Harbor MCP tools continue to return the current published
     * version. Creator and updater IDs are null when their actor type is <code>system</code>.</p>
     */
    public HarborContextDraftEnvelope getContextDraft(
            String harborId, String contextId, RequestOptions requestOptions) {
        return this.rawClient
                .getContextDraft(harborId, contextId, requestOptions)
                .body();
    }

    /**
     * Returns the mutable draft for a Harbor context document.
     * <p>Drafts are available only through the draft endpoints. <code>base_revision_id</code>
     * identifies the published revision from which the candidate was created. Normal
     * context reads and Harbor MCP tools continue to return the current published
     * version. Creator and updater IDs are null when their actor type is <code>system</code>.</p>
     */
    public HarborContextDraftEnvelope getContextDraft(
            String harborId, String contextId, HarborsGetContextDraftRequest request) {
        return this.rawClient.getContextDraft(harborId, contextId, request).body();
    }

    /**
     * Returns the mutable draft for a Harbor context document.
     * <p>Drafts are available only through the draft endpoints. <code>base_revision_id</code>
     * identifies the published revision from which the candidate was created. Normal
     * context reads and Harbor MCP tools continue to return the current published
     * version. Creator and updater IDs are null when their actor type is <code>system</code>.</p>
     */
    public HarborContextDraftEnvelope getContextDraft(
            String harborId, String contextId, HarborsGetContextDraftRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .getContextDraft(harborId, contextId, request, requestOptions)
                .body();
    }

    /**
     * Creates or completely replaces the mutable draft for a Harbor context document.
     * <p>The request supplies the complete draft payload. If a draft already exists,
     * this request replaces it while preserving the draft ID, creation metadata, and
     * <code>base_revision_id</code>.</p>
     * <p>A new draft for a published document records the current revision as its base.
     * An unpublished document's initial draft keeps a null base when replaced. Saving
     * a draft does not change published content or its <code>updated_at</code>. Creator and
     * updater provenance comes from the request actor; system actors have a null actor
     * ID.</p>
     */
    public HarborContextDraftEnvelope saveContextDraft(
            String harborId, String contextId, SaveHarborContextDraftRequest request) {
        return this.rawClient.saveContextDraft(harborId, contextId, request).body();
    }

    /**
     * Creates or completely replaces the mutable draft for a Harbor context document.
     * <p>The request supplies the complete draft payload. If a draft already exists,
     * this request replaces it while preserving the draft ID, creation metadata, and
     * <code>base_revision_id</code>.</p>
     * <p>A new draft for a published document records the current revision as its base.
     * An unpublished document's initial draft keeps a null base when replaced. Saving
     * a draft does not change published content or its <code>updated_at</code>. Creator and
     * updater provenance comes from the request actor; system actors have a null actor
     * ID.</p>
     */
    public HarborContextDraftEnvelope saveContextDraft(
            String harborId,
            String contextId,
            SaveHarborContextDraftRequest request,
            IdempotentRequestOptions requestOptions) {
        return this.rawClient
                .saveContextDraft(harborId, contextId, request, requestOptions)
                .body();
    }

    /**
     * Discards the mutable draft for a Harbor context document.
     * <p>Discarding a draft does not change the current published version or its
     * history. If the draft belongs to a context that has never been published,
     * discarding it also removes the otherwise empty context identity.</p>
     */
    public DeletedHarborContextDraftEnvelope deleteContextDraft(String harborId, String contextId) {
        return this.rawClient.deleteContextDraft(harborId, contextId).body();
    }

    /**
     * Discards the mutable draft for a Harbor context document.
     * <p>Discarding a draft does not change the current published version or its
     * history. If the draft belongs to a context that has never been published,
     * discarding it also removes the otherwise empty context identity.</p>
     */
    public DeletedHarborContextDraftEnvelope deleteContextDraft(
            String harborId, String contextId, IdempotentRequestOptions requestOptions) {
        return this.rawClient
                .deleteContextDraft(harborId, contextId, requestOptions)
                .body();
    }

    /**
     * Discards the mutable draft for a Harbor context document.
     * <p>Discarding a draft does not change the current published version or its
     * history. If the draft belongs to a context that has never been published,
     * discarding it also removes the otherwise empty context identity.</p>
     */
    public DeletedHarborContextDraftEnvelope deleteContextDraft(
            String harborId, String contextId, HarborsDeleteContextDraftRequest request) {
        return this.rawClient.deleteContextDraft(harborId, contextId, request).body();
    }

    /**
     * Discards the mutable draft for a Harbor context document.
     * <p>Discarding a draft does not change the current published version or its
     * history. If the draft belongs to a context that has never been published,
     * discarding it also removes the otherwise empty context identity.</p>
     */
    public DeletedHarborContextDraftEnvelope deleteContextDraft(
            String harborId,
            String contextId,
            HarborsDeleteContextDraftRequest request,
            IdempotentRequestOptions requestOptions) {
        return this.rawClient
                .deleteContextDraft(harborId, contextId, request, requestOptions)
                .body();
    }

    /**
     * Promotes the draft to the next immutable published context version.
     * <p>Promotion publishes the draft's exact title, description, content, and optional
     * change note as the next version. An unpublished context's initial draft becomes
     * version 1. The draft is removed after publication.</p>
     * <p>Promotion returns a conflict when the current published revision differs from
     * the draft's <code>base_revision_id</code>. The stale draft remains available so an author
     * can compare it with the current version before discarding and recreating it.</p>
     */
    public HarborContextVersionEnvelope promoteContextDraft(String harborId, String contextId) {
        return this.rawClient.promoteContextDraft(harborId, contextId).body();
    }

    /**
     * Promotes the draft to the next immutable published context version.
     * <p>Promotion publishes the draft's exact title, description, content, and optional
     * change note as the next version. An unpublished context's initial draft becomes
     * version 1. The draft is removed after publication.</p>
     * <p>Promotion returns a conflict when the current published revision differs from
     * the draft's <code>base_revision_id</code>. The stale draft remains available so an author
     * can compare it with the current version before discarding and recreating it.</p>
     */
    public HarborContextVersionEnvelope promoteContextDraft(
            String harborId, String contextId, IdempotentRequestOptions requestOptions) {
        return this.rawClient
                .promoteContextDraft(harborId, contextId, requestOptions)
                .body();
    }

    /**
     * Promotes the draft to the next immutable published context version.
     * <p>Promotion publishes the draft's exact title, description, content, and optional
     * change note as the next version. An unpublished context's initial draft becomes
     * version 1. The draft is removed after publication.</p>
     * <p>Promotion returns a conflict when the current published revision differs from
     * the draft's <code>base_revision_id</code>. The stale draft remains available so an author
     * can compare it with the current version before discarding and recreating it.</p>
     */
    public HarborContextVersionEnvelope promoteContextDraft(
            String harborId, String contextId, HarborsPromoteContextDraftRequest request) {
        return this.rawClient.promoteContextDraft(harborId, contextId, request).body();
    }

    /**
     * Promotes the draft to the next immutable published context version.
     * <p>Promotion publishes the draft's exact title, description, content, and optional
     * change note as the next version. An unpublished context's initial draft becomes
     * version 1. The draft is removed after publication.</p>
     * <p>Promotion returns a conflict when the current published revision differs from
     * the draft's <code>base_revision_id</code>. The stale draft remains available so an author
     * can compare it with the current version before discarding and recreating it.</p>
     */
    public HarborContextVersionEnvelope promoteContextDraft(
            String harborId,
            String contextId,
            HarborsPromoteContextDraftRequest request,
            IdempotentRequestOptions requestOptions) {
        return this.rawClient
                .promoteContextDraft(harborId, contextId, request, requestOptions)
                .body();
    }

    /**
     * Lists immutable published versions of a Harbor context document without returning content.
     * <p>Versions are ordered from newest to oldest. Collection items omit <code>content</code>,
     * and the draft is never included. Pagination tokens continue from the last
     * returned version, so publishing a newer version between requests does not shift
     * or duplicate older results.</p>
     * <p>Published revision IDs are durable artifact identities intended for future
     * Harbor activity and audit records. Publisher IDs are paired with actor types.
     * System publications have <code>published_by_type: &quot;system&quot;</code> and a null
     * <code>published_by</code> because the system actor has no UUID.</p>
     * <p>A Harbor profile credential can read versions only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public HarborContextVersionListEnvelope listContextVersions(String harborId, String contextId) {
        return this.rawClient.listContextVersions(harborId, contextId).body();
    }

    /**
     * Lists immutable published versions of a Harbor context document without returning content.
     * <p>Versions are ordered from newest to oldest. Collection items omit <code>content</code>,
     * and the draft is never included. Pagination tokens continue from the last
     * returned version, so publishing a newer version between requests does not shift
     * or duplicate older results.</p>
     * <p>Published revision IDs are durable artifact identities intended for future
     * Harbor activity and audit records. Publisher IDs are paired with actor types.
     * System publications have <code>published_by_type: &quot;system&quot;</code> and a null
     * <code>published_by</code> because the system actor has no UUID.</p>
     * <p>A Harbor profile credential can read versions only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public HarborContextVersionListEnvelope listContextVersions(
            String harborId, String contextId, RequestOptions requestOptions) {
        return this.rawClient
                .listContextVersions(harborId, contextId, requestOptions)
                .body();
    }

    /**
     * Lists immutable published versions of a Harbor context document without returning content.
     * <p>Versions are ordered from newest to oldest. Collection items omit <code>content</code>,
     * and the draft is never included. Pagination tokens continue from the last
     * returned version, so publishing a newer version between requests does not shift
     * or duplicate older results.</p>
     * <p>Published revision IDs are durable artifact identities intended for future
     * Harbor activity and audit records. Publisher IDs are paired with actor types.
     * System publications have <code>published_by_type: &quot;system&quot;</code> and a null
     * <code>published_by</code> because the system actor has no UUID.</p>
     * <p>A Harbor profile credential can read versions only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public HarborContextVersionListEnvelope listContextVersions(
            String harborId, String contextId, HarborsListContextVersionsRequest request) {
        return this.rawClient.listContextVersions(harborId, contextId, request).body();
    }

    /**
     * Lists immutable published versions of a Harbor context document without returning content.
     * <p>Versions are ordered from newest to oldest. Collection items omit <code>content</code>,
     * and the draft is never included. Pagination tokens continue from the last
     * returned version, so publishing a newer version between requests does not shift
     * or duplicate older results.</p>
     * <p>Published revision IDs are durable artifact identities intended for future
     * Harbor activity and audit records. Publisher IDs are paired with actor types.
     * System publications have <code>published_by_type: &quot;system&quot;</code> and a null
     * <code>published_by</code> because the system actor has no UUID.</p>
     * <p>A Harbor profile credential can read versions only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public HarborContextVersionListEnvelope listContextVersions(
            String harborId,
            String contextId,
            HarborsListContextVersionsRequest request,
            RequestOptions requestOptions) {
        return this.rawClient
                .listContextVersions(harborId, contextId, request, requestOptions)
                .body();
    }

    /**
     * Returns one immutable published version of a Harbor context document.
     * <p>The response includes the complete title, description, and plain-text
     * <code>content</code> captured when the version was published.</p>
     * <p>Published versions cannot be changed or deleted. System publications have
     * <code>published_by_type: &quot;system&quot;</code> and a null <code>published_by</code>.</p>
     */
    public HarborContextVersionEnvelope getContextVersion(String harborId, String contextId, String versionId) {
        return this.rawClient.getContextVersion(harborId, contextId, versionId).body();
    }

    /**
     * Returns one immutable published version of a Harbor context document.
     * <p>The response includes the complete title, description, and plain-text
     * <code>content</code> captured when the version was published.</p>
     * <p>Published versions cannot be changed or deleted. System publications have
     * <code>published_by_type: &quot;system&quot;</code> and a null <code>published_by</code>.</p>
     */
    public HarborContextVersionEnvelope getContextVersion(
            String harborId, String contextId, String versionId, RequestOptions requestOptions) {
        return this.rawClient
                .getContextVersion(harborId, contextId, versionId, requestOptions)
                .body();
    }

    /**
     * Returns one immutable published version of a Harbor context document.
     * <p>The response includes the complete title, description, and plain-text
     * <code>content</code> captured when the version was published.</p>
     * <p>Published versions cannot be changed or deleted. System publications have
     * <code>published_by_type: &quot;system&quot;</code> and a null <code>published_by</code>.</p>
     */
    public HarborContextVersionEnvelope getContextVersion(
            String harborId, String contextId, String versionId, HarborsGetContextVersionRequest request) {
        return this.rawClient
                .getContextVersion(harborId, contextId, versionId, request)
                .body();
    }

    /**
     * Returns one immutable published version of a Harbor context document.
     * <p>The response includes the complete title, description, and plain-text
     * <code>content</code> captured when the version was published.</p>
     * <p>Published versions cannot be changed or deleted. System publications have
     * <code>published_by_type: &quot;system&quot;</code> and a null <code>published_by</code>.</p>
     */
    public HarborContextVersionEnvelope getContextVersion(
            String harborId,
            String contextId,
            String versionId,
            HarborsGetContextVersionRequest request,
            RequestOptions requestOptions) {
        return this.rawClient
                .getContextVersion(harborId, contextId, versionId, request, requestOptions)
                .body();
    }

    /**
     * Lists active masked credentials for a Harbor.
     * <p>Each item contains a masked <code>key_hint</code>. Polytomic never returns a credential plaintext after creation.</p>
     */
    public HarborKeyListEnvelope listKeys(String harborId) {
        return this.rawClient.listKeys(harborId).body();
    }

    /**
     * Lists active masked credentials for a Harbor.
     * <p>Each item contains a masked <code>key_hint</code>. Polytomic never returns a credential plaintext after creation.</p>
     */
    public HarborKeyListEnvelope listKeys(String harborId, RequestOptions requestOptions) {
        return this.rawClient.listKeys(harborId, requestOptions).body();
    }

    /**
     * Lists active masked credentials for a Harbor.
     * <p>Each item contains a masked <code>key_hint</code>. Polytomic never returns a credential plaintext after creation.</p>
     */
    public HarborKeyListEnvelope listKeys(String harborId, HarborsListKeysRequest request) {
        return this.rawClient.listKeys(harborId, request).body();
    }

    /**
     * Lists active masked credentials for a Harbor.
     * <p>Each item contains a masked <code>key_hint</code>. Polytomic never returns a credential plaintext after creation.</p>
     */
    public HarborKeyListEnvelope listKeys(
            String harborId, HarborsListKeysRequest request, RequestOptions requestOptions) {
        return this.rawClient.listKeys(harborId, request, requestOptions).body();
    }

    /**
     * Generates a new Harbor credential and returns its plaintext value once.
     * <p>Store the returned <code>value</code> securely. Polytomic returns it only in this response.</p>
     */
    public HarborKeyEnvelope createKey(String harborId) {
        return this.rawClient.createKey(harborId).body();
    }

    /**
     * Generates a new Harbor credential and returns its plaintext value once.
     * <p>Store the returned <code>value</code> securely. Polytomic returns it only in this response.</p>
     */
    public HarborKeyEnvelope createKey(String harborId, IdempotentRequestOptions requestOptions) {
        return this.rawClient.createKey(harborId, requestOptions).body();
    }

    /**
     * Revokes one Harbor credential by its credential ID.
     * <p>Revocation affects only the selected credential. Other active Harbor credentials remain valid.</p>
     */
    public RevokedHarborKeyEnvelope deleteKey(String harborId, String keyId) {
        return this.rawClient.deleteKey(harborId, keyId).body();
    }

    /**
     * Revokes one Harbor credential by its credential ID.
     * <p>Revocation affects only the selected credential. Other active Harbor credentials remain valid.</p>
     */
    public RevokedHarborKeyEnvelope deleteKey(String harborId, String keyId, IdempotentRequestOptions requestOptions) {
        return this.rawClient.deleteKey(harborId, keyId, requestOptions).body();
    }

    /**
     * Resolves documented source table and field identities to the names a Harbor's backing Connection accepts.
     */
    public ResolveHarborSourceMappingsEnvelope resolveSourceMappings(String harborId) {
        return this.rawClient.resolveSourceMappings(harborId).body();
    }

    /**
     * Resolves documented source table and field identities to the names a Harbor's backing Connection accepts.
     */
    public ResolveHarborSourceMappingsEnvelope resolveSourceMappings(
            String harborId, IdempotentRequestOptions requestOptions) {
        return this.rawClient.resolveSourceMappings(harborId, requestOptions).body();
    }

    /**
     * Resolves documented source table and field identities to the names a Harbor's backing Connection accepts.
     */
    public ResolveHarborSourceMappingsEnvelope resolveSourceMappings(
            String harborId, ResolveHarborSourceMappingsRequest request) {
        return this.rawClient.resolveSourceMappings(harborId, request).body();
    }

    /**
     * Resolves documented source table and field identities to the names a Harbor's backing Connection accepts.
     */
    public ResolveHarborSourceMappingsEnvelope resolveSourceMappings(
            String harborId, ResolveHarborSourceMappingsRequest request, IdempotentRequestOptions requestOptions) {
        return this.rawClient
                .resolveSourceMappings(harborId, request, requestOptions)
                .body();
    }

    /**
     * Returns raw Polytomic refresh evidence for datasets written to a Harbor.
     * <p>Each pipeline corresponds to a bulk sync or model sync that writes at least one
     * dataset to the Harbor's backing Connection. Tables populated outside Polytomic
     * are not included, even when they are queryable through a customer-managed
     * backing Connection.</p>
     * <p>The response groups shared pipeline evidence so schedules and configuration are
     * not repeated for every dataset:</p>
     * <ul>
     * <li>Each entry in <code>pipelines</code> identifies the producer through <code>type</code> and <code>id</code>.
     * Separate pipelines targeting the same physical dataset remain separate
     * entries.</li>
     * <li><code>datasets</code> is keyed by the effective destination dataset name.
     * <code>last_success_at</code> is the start time of the most recent execution in which that
     * dataset completed successfully, including a successful dataset within a bulk
     * execution that completes with errors. The start time is a conservative upper
     * bound because source reads and destination writes happen afterward.</li>
     * <li><code>latest_status</code> preserves the latest Polytomic execution status. Never-run
     * datasets omit this field.</li>
     * <li>Pipeline-level <code>schedules</code> preserves schedule parameters and selectors. A
     * schedule can be manual, event-driven, advanced, selective, or limited to
     * named source schemas, so callers should not reduce the list to one inferred
     * cadence.</li>
     * <li>Continuous schedules use the scheduler's persisted next firing. If scheduler
     * state is unavailable, <code>next_run_at</code> is omitted rather than recalculated with
     * new jitter.</li>
     * <li>Paused pipelines remain present with <code>refresh_enabled</code> set to <code>false</code> and no
     * <code>next_run_at</code>.</li>
     * </ul>
     * <p>Use absolute timestamps and the raw statuses to apply the maximum acceptable
     * staleness for your task. The endpoint does not classify datasets or the Harbor
     * as healthy, stale, or unhealthy.</p>
     */
    public HarborStatusEnvelope getStatus(String harborId) {
        return this.rawClient.getStatus(harborId).body();
    }

    /**
     * Returns raw Polytomic refresh evidence for datasets written to a Harbor.
     * <p>Each pipeline corresponds to a bulk sync or model sync that writes at least one
     * dataset to the Harbor's backing Connection. Tables populated outside Polytomic
     * are not included, even when they are queryable through a customer-managed
     * backing Connection.</p>
     * <p>The response groups shared pipeline evidence so schedules and configuration are
     * not repeated for every dataset:</p>
     * <ul>
     * <li>Each entry in <code>pipelines</code> identifies the producer through <code>type</code> and <code>id</code>.
     * Separate pipelines targeting the same physical dataset remain separate
     * entries.</li>
     * <li><code>datasets</code> is keyed by the effective destination dataset name.
     * <code>last_success_at</code> is the start time of the most recent execution in which that
     * dataset completed successfully, including a successful dataset within a bulk
     * execution that completes with errors. The start time is a conservative upper
     * bound because source reads and destination writes happen afterward.</li>
     * <li><code>latest_status</code> preserves the latest Polytomic execution status. Never-run
     * datasets omit this field.</li>
     * <li>Pipeline-level <code>schedules</code> preserves schedule parameters and selectors. A
     * schedule can be manual, event-driven, advanced, selective, or limited to
     * named source schemas, so callers should not reduce the list to one inferred
     * cadence.</li>
     * <li>Continuous schedules use the scheduler's persisted next firing. If scheduler
     * state is unavailable, <code>next_run_at</code> is omitted rather than recalculated with
     * new jitter.</li>
     * <li>Paused pipelines remain present with <code>refresh_enabled</code> set to <code>false</code> and no
     * <code>next_run_at</code>.</li>
     * </ul>
     * <p>Use absolute timestamps and the raw statuses to apply the maximum acceptable
     * staleness for your task. The endpoint does not classify datasets or the Harbor
     * as healthy, stale, or unhealthy.</p>
     */
    public HarborStatusEnvelope getStatus(String harborId, RequestOptions requestOptions) {
        return this.rawClient.getStatus(harborId, requestOptions).body();
    }

    /**
     * Lists Harbor-only users assigned to a Harbor.
     * <p>The response contains only Harbor-only users currently assigned to this Harbor.</p>
     */
    public HarborUserListEnvelope listUsers(String harborId) {
        return this.rawClient.listUsers(harborId).body();
    }

    /**
     * Lists Harbor-only users assigned to a Harbor.
     * <p>The response contains only Harbor-only users currently assigned to this Harbor.</p>
     */
    public HarborUserListEnvelope listUsers(String harborId, RequestOptions requestOptions) {
        return this.rawClient.listUsers(harborId, requestOptions).body();
    }

    /**
     * Lists Harbor-only users assigned to a Harbor.
     * <p>The response contains only Harbor-only users currently assigned to this Harbor.</p>
     */
    public HarborUserListEnvelope listUsers(String harborId, HarborsListUsersRequest request) {
        return this.rawClient.listUsers(harborId, request).body();
    }

    /**
     * Lists Harbor-only users assigned to a Harbor.
     * <p>The response contains only Harbor-only users currently assigned to this Harbor.</p>
     */
    public HarborUserListEnvelope listUsers(
            String harborId, HarborsListUsersRequest request, RequestOptions requestOptions) {
        return this.rawClient.listUsers(harborId, request, requestOptions).body();
    }

    /**
     * Invites and assigns a new Harbor-only user.
     * <p>The invited account is restricted to assigned Harbors and does not receive regular Polytomic application access.</p>
     */
    public HarborUserEnvelope inviteUser(String harborId, InviteHarborUserRequest request) {
        return this.rawClient.inviteUser(harborId, request).body();
    }

    /**
     * Invites and assigns a new Harbor-only user.
     * <p>The invited account is restricted to assigned Harbors and does not receive regular Polytomic application access.</p>
     */
    public HarborUserEnvelope inviteUser(
            String harborId, InviteHarborUserRequest request, IdempotentRequestOptions requestOptions) {
        return this.rawClient.inviteUser(harborId, request, requestOptions).body();
    }

    /**
     * Assigns an existing Harbor-only user to a Harbor.
     * <p>The assignment is idempotent. Regular Polytomic users cannot be assigned because they already have application access to Harbors.</p>
     */
    public HarborUserEnvelope assignUser(String harborId, String userId) {
        return this.rawClient.assignUser(harborId, userId).body();
    }

    /**
     * Assigns an existing Harbor-only user to a Harbor.
     * <p>The assignment is idempotent. Regular Polytomic users cannot be assigned because they already have application access to Harbors.</p>
     */
    public HarborUserEnvelope assignUser(String harborId, String userId, IdempotentRequestOptions requestOptions) {
        return this.rawClient.assignUser(harborId, userId, requestOptions).body();
    }

    /**
     * Removes a Harbor assignment without deleting the user.
     * <p>This removes only the Harbor assignment. The organization user remains available and may retain assignments to other Harbors.</p>
     */
    public UnassignedHarborUserEnvelope unassignUser(String harborId, String userId) {
        return this.rawClient.unassignUser(harborId, userId).body();
    }

    /**
     * Removes a Harbor assignment without deleting the user.
     * <p>This removes only the Harbor assignment. The organization user remains available and may retain assignments to other Harbors.</p>
     */
    public UnassignedHarborUserEnvelope unassignUser(
            String harborId, String userId, IdempotentRequestOptions requestOptions) {
        return this.rawClient.unassignUser(harborId, userId, requestOptions).body();
    }
}
