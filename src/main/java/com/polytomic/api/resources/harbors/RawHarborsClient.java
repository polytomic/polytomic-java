/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.polytomic.api.resources.harbors;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.polytomic.api.core.ClientOptions;
import com.polytomic.api.core.IdempotentRequestOptions;
import com.polytomic.api.core.MediaTypes;
import com.polytomic.api.core.ObjectMappers;
import com.polytomic.api.core.PolytomicApiException;
import com.polytomic.api.core.PolytomicException;
import com.polytomic.api.core.PolytomicHttpResponse;
import com.polytomic.api.core.QueryStringMapper;
import com.polytomic.api.core.RequestOptions;
import com.polytomic.api.errors.ConflictError;
import com.polytomic.api.errors.ForbiddenError;
import com.polytomic.api.errors.InternalServerError;
import com.polytomic.api.errors.NotFoundError;
import com.polytomic.api.errors.ServiceUnavailableError;
import com.polytomic.api.errors.UnprocessableEntityError;
import com.polytomic.api.resources.harbors.requests.CreateHarborContextDraftRequest;
import com.polytomic.api.resources.harbors.requests.CreateHarborContextRequest;
import com.polytomic.api.resources.harbors.requests.CreateHarborRequest;
import com.polytomic.api.resources.harbors.requests.HarborsCloseSessionRequest;
import com.polytomic.api.resources.harbors.requests.HarborsDeleteContextDraftRequest;
import com.polytomic.api.resources.harbors.requests.HarborsDeleteContextRequest;
import com.polytomic.api.resources.harbors.requests.HarborsGetAuthorizedSchemaRequest;
import com.polytomic.api.resources.harbors.requests.HarborsGetContextDraftRequest;
import com.polytomic.api.resources.harbors.requests.HarborsGetContextRequest;
import com.polytomic.api.resources.harbors.requests.HarborsGetContextVersionRequest;
import com.polytomic.api.resources.harbors.requests.HarborsListAuthorizedConnectionsRequest;
import com.polytomic.api.resources.harbors.requests.HarborsListAuthorizedSchemasRequest;
import com.polytomic.api.resources.harbors.requests.HarborsListContextDraftsRequest;
import com.polytomic.api.resources.harbors.requests.HarborsListContextVersionsRequest;
import com.polytomic.api.resources.harbors.requests.HarborsListContextsRequest;
import com.polytomic.api.resources.harbors.requests.HarborsListKeysRequest;
import com.polytomic.api.resources.harbors.requests.HarborsListRequest;
import com.polytomic.api.resources.harbors.requests.HarborsListUsersRequest;
import com.polytomic.api.resources.harbors.requests.HarborsPromoteContextDraftRequest;
import com.polytomic.api.resources.harbors.requests.InviteHarborUserRequest;
import com.polytomic.api.resources.harbors.requests.RegisterHarborSessionRequest;
import com.polytomic.api.resources.harbors.requests.ResolveHarborSourceMappingsRequest;
import com.polytomic.api.resources.harbors.requests.SaveHarborContextDraftRequest;
import com.polytomic.api.resources.harbors.requests.SaveHarborContextRequest;
import com.polytomic.api.resources.harbors.requests.UpdateHarborRequest;
import com.polytomic.api.types.ApiError;
import com.polytomic.api.types.CloseHarborSessionEnvelope;
import com.polytomic.api.types.CreateHarborEnvelope;
import com.polytomic.api.types.DeletedHarborContextDraftEnvelope;
import com.polytomic.api.types.DeletedHarborContextEnvelope;
import com.polytomic.api.types.DeletedHarborEnvelope;
import com.polytomic.api.types.HarborConnectionListEnvelope;
import com.polytomic.api.types.HarborConnectionSchemaEnvelope;
import com.polytomic.api.types.HarborContextDraftEnvelope;
import com.polytomic.api.types.HarborContextDraftListEnvelope;
import com.polytomic.api.types.HarborContextEnvelope;
import com.polytomic.api.types.HarborContextListEnvelope;
import com.polytomic.api.types.HarborContextVersionEnvelope;
import com.polytomic.api.types.HarborContextVersionListEnvelope;
import com.polytomic.api.types.HarborEnvelope;
import com.polytomic.api.types.HarborKeyEnvelope;
import com.polytomic.api.types.HarborKeyListEnvelope;
import com.polytomic.api.types.HarborListEnvelope;
import com.polytomic.api.types.HarborSchemaListEnvelope;
import com.polytomic.api.types.HarborStatusEnvelope;
import com.polytomic.api.types.HarborUserEnvelope;
import com.polytomic.api.types.HarborUserListEnvelope;
import com.polytomic.api.types.RegisterHarborSessionEnvelope;
import com.polytomic.api.types.ResolveHarborSourceMappingsEnvelope;
import com.polytomic.api.types.RevokedHarborKeyEnvelope;
import com.polytomic.api.types.UnassignedHarborUserEnvelope;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawHarborsClient {
    protected final ClientOptions clientOptions;

    public RawHarborsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Lists profile-authorized connections and capabilities for the current Harbor credential.
     */
    public PolytomicHttpResponse<HarborConnectionListEnvelope> listAuthorizedConnections() {
        return listAuthorizedConnections(
                HarborsListAuthorizedConnectionsRequest.builder().build());
    }

    /**
     * Lists profile-authorized connections and capabilities for the current Harbor credential.
     */
    public PolytomicHttpResponse<HarborConnectionListEnvelope> listAuthorizedConnections(
            RequestOptions requestOptions) {
        return listAuthorizedConnections(
                HarborsListAuthorizedConnectionsRequest.builder().build(), requestOptions);
    }

    /**
     * Lists profile-authorized connections and capabilities for the current Harbor credential.
     */
    public PolytomicHttpResponse<HarborConnectionListEnvelope> listAuthorizedConnections(
            HarborsListAuthorizedConnectionsRequest request) {
        return listAuthorizedConnections(request, null);
    }

    /**
     * Lists profile-authorized connections and capabilities for the current Harbor credential.
     */
    public PolytomicHttpResponse<HarborConnectionListEnvelope> listAuthorizedConnections(
            HarborsListAuthorizedConnectionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbor/connections");
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getPageToken().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_token", request.getPageToken().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        if (request.getPolytomicHarborSession().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Harbor-Session",
                    request.getPolytomicHarborSession().get());
        }
        if (request.getPolytomicActivityRequestId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Activity-Request-ID",
                    request.getPolytomicActivityRequestId().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborConnectionListEnvelope.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Lists one bounded page of schema resources authorized by the current Harbor profile.
     */
    public PolytomicHttpResponse<HarborSchemaListEnvelope> listAuthorizedSchemas(String connectionId) {
        return listAuthorizedSchemas(
                connectionId, HarborsListAuthorizedSchemasRequest.builder().build());
    }

    /**
     * Lists one bounded page of schema resources authorized by the current Harbor profile.
     */
    public PolytomicHttpResponse<HarborSchemaListEnvelope> listAuthorizedSchemas(
            String connectionId, RequestOptions requestOptions) {
        return listAuthorizedSchemas(
                connectionId, HarborsListAuthorizedSchemasRequest.builder().build(), requestOptions);
    }

    /**
     * Lists one bounded page of schema resources authorized by the current Harbor profile.
     */
    public PolytomicHttpResponse<HarborSchemaListEnvelope> listAuthorizedSchemas(
            String connectionId, HarborsListAuthorizedSchemasRequest request) {
        return listAuthorizedSchemas(connectionId, request, null);
    }

    /**
     * Lists one bounded page of schema resources authorized by the current Harbor profile.
     */
    public PolytomicHttpResponse<HarborSchemaListEnvelope> listAuthorizedSchemas(
            String connectionId, HarborsListAuthorizedSchemasRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbor/connections")
                .addPathSegment(connectionId)
                .addPathSegments("schemas");
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getPageToken().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_token", request.getPageToken().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        if (request.getPolytomicHarborSession().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Harbor-Session",
                    request.getPolytomicHarborSession().get());
        }
        if (request.getPolytomicActivityRequestId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Activity-Request-ID",
                    request.getPolytomicActivityRequestId().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborSchemaListEnvelope.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns one schema resource authorized by the current Harbor profile.
     */
    public PolytomicHttpResponse<HarborConnectionSchemaEnvelope> getAuthorizedSchema(
            String connectionId, String schemaId) {
        return getAuthorizedSchema(
                connectionId,
                schemaId,
                HarborsGetAuthorizedSchemaRequest.builder().build());
    }

    /**
     * Returns one schema resource authorized by the current Harbor profile.
     */
    public PolytomicHttpResponse<HarborConnectionSchemaEnvelope> getAuthorizedSchema(
            String connectionId, String schemaId, RequestOptions requestOptions) {
        return getAuthorizedSchema(
                connectionId,
                schemaId,
                HarborsGetAuthorizedSchemaRequest.builder().build(),
                requestOptions);
    }

    /**
     * Returns one schema resource authorized by the current Harbor profile.
     */
    public PolytomicHttpResponse<HarborConnectionSchemaEnvelope> getAuthorizedSchema(
            String connectionId, String schemaId, HarborsGetAuthorizedSchemaRequest request) {
        return getAuthorizedSchema(connectionId, schemaId, request, null);
    }

    /**
     * Returns one schema resource authorized by the current Harbor profile.
     */
    public PolytomicHttpResponse<HarborConnectionSchemaEnvelope> getAuthorizedSchema(
            String connectionId,
            String schemaId,
            HarborsGetAuthorizedSchemaRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbor/connections")
                .addPathSegment(connectionId)
                .addPathSegments("schemas")
                .addPathSegment(schemaId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        if (request.getPolytomicHarborSession().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Harbor-Session",
                    request.getPolytomicHarborSession().get());
        }
        if (request.getPolytomicActivityRequestId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Activity-Request-ID",
                    request.getPolytomicActivityRequestId().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborConnectionSchemaEnvelope.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Registers a service-attested MCP transport session for a scoped Harbor credential.
     */
    public PolytomicHttpResponse<RegisterHarborSessionEnvelope> registerSession() {
        return registerSession(RegisterHarborSessionRequest.builder().build());
    }

    /**
     * Registers a service-attested MCP transport session for a scoped Harbor credential.
     */
    public PolytomicHttpResponse<RegisterHarborSessionEnvelope> registerSession(
            IdempotentRequestOptions requestOptions) {
        return registerSession(RegisterHarborSessionRequest.builder().build(), requestOptions);
    }

    /**
     * Registers a service-attested MCP transport session for a scoped Harbor credential.
     */
    public PolytomicHttpResponse<RegisterHarborSessionEnvelope> registerSession(RegisterHarborSessionRequest request) {
        return registerSession(request, null);
    }

    /**
     * Registers a service-attested MCP transport session for a scoped Harbor credential.
     */
    public PolytomicHttpResponse<RegisterHarborSessionEnvelope> registerSession(
            RegisterHarborSessionRequest request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbor/sessions");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        if (request.getPolytomicMcpKeyId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-MCP-Key-ID", request.getPolytomicMcpKeyId().get());
        }
        if (request.getPolytomicMcpTimestamp().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-MCP-Timestamp",
                    request.getPolytomicMcpTimestamp().get().toString());
        }
        if (request.getPolytomicMcpNonce().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-MCP-Nonce", request.getPolytomicMcpNonce().get());
        }
        if (request.getPolytomicMcpSignature().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-MCP-Signature",
                    request.getPolytomicMcpSignature().get());
        }
        if (request.getPolytomicActivityRequestId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Activity-Request-ID",
                    request.getPolytomicActivityRequestId().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RegisterHarborSessionEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Closes a service-attested Harbor MCP transport session.
     */
    public PolytomicHttpResponse<CloseHarborSessionEnvelope> closeSession(String sessionId) {
        return closeSession(sessionId, HarborsCloseSessionRequest.builder().build());
    }

    /**
     * Closes a service-attested Harbor MCP transport session.
     */
    public PolytomicHttpResponse<CloseHarborSessionEnvelope> closeSession(
            String sessionId, IdempotentRequestOptions requestOptions) {
        return closeSession(sessionId, HarborsCloseSessionRequest.builder().build(), requestOptions);
    }

    /**
     * Closes a service-attested Harbor MCP transport session.
     */
    public PolytomicHttpResponse<CloseHarborSessionEnvelope> closeSession(
            String sessionId, HarborsCloseSessionRequest request) {
        return closeSession(sessionId, request, null);
    }

    /**
     * Closes a service-attested Harbor MCP transport session.
     */
    public PolytomicHttpResponse<CloseHarborSessionEnvelope> closeSession(
            String sessionId, HarborsCloseSessionRequest request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbor/sessions")
                .addPathSegment(sessionId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        if (request.getPolytomicMcpKeyId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-MCP-Key-ID", request.getPolytomicMcpKeyId().get());
        }
        if (request.getPolytomicMcpTimestamp().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-MCP-Timestamp",
                    request.getPolytomicMcpTimestamp().get().toString());
        }
        if (request.getPolytomicMcpNonce().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-MCP-Nonce", request.getPolytomicMcpNonce().get());
        }
        if (request.getPolytomicMcpSignature().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-MCP-Signature",
                    request.getPolytomicMcpSignature().get());
        }
        if (request.getPolytomicActivityRequestId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Activity-Request-ID",
                    request.getPolytomicActivityRequestId().get());
        }
        if (request.getPolytomicHarborSession().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Harbor-Session",
                    request.getPolytomicHarborSession().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CloseHarborSessionEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Lists Harbors in the caller's current organization.
     * <p>Returns Harbors in creation order. Use <code>pagination.next_page_token</code> to continue when more results are available.</p>
     */
    public PolytomicHttpResponse<HarborListEnvelope> list() {
        return list(HarborsListRequest.builder().build());
    }

    /**
     * Lists Harbors in the caller's current organization.
     * <p>Returns Harbors in creation order. Use <code>pagination.next_page_token</code> to continue when more results are available.</p>
     */
    public PolytomicHttpResponse<HarborListEnvelope> list(RequestOptions requestOptions) {
        return list(HarborsListRequest.builder().build(), requestOptions);
    }

    /**
     * Lists Harbors in the caller's current organization.
     * <p>Returns Harbors in creation order. Use <code>pagination.next_page_token</code> to continue when more results are available.</p>
     */
    public PolytomicHttpResponse<HarborListEnvelope> list(HarborsListRequest request) {
        return list(request, null);
    }

    /**
     * Lists Harbors in the caller's current organization.
     * <p>Returns Harbors in creation order. Use <code>pagination.next_page_token</code> to continue when more results are available.</p>
     */
    public PolytomicHttpResponse<HarborListEnvelope> list(HarborsListRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors");
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getPageToken().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_token", request.getPageToken().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborListEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Creates a managed or customer-managed Harbor in the caller's current organization.
     * <p><code>generate_api_key</code> defaults to <code>true</code>. Polytomic returns a new plaintext credential only in this response. Set it to <code>false</code> to create the Harbor without a credential.</p>
     * <p>For <code>customer_managed</code>, <code>backing_connection_id</code> must identify a queryable Connection that your credential can access.</p>
     */
    public PolytomicHttpResponse<CreateHarborEnvelope> create(CreateHarborRequest request) {
        return create(request, null);
    }

    /**
     * Creates a managed or customer-managed Harbor in the caller's current organization.
     * <p><code>generate_api_key</code> defaults to <code>true</code>. Polytomic returns a new plaintext credential only in this response. Set it to <code>false</code> to create the Harbor without a credential.</p>
     * <p>For <code>customer_managed</code>, <code>backing_connection_id</code> must identify a queryable Connection that your credential can access.</p>
     */
    public PolytomicHttpResponse<CreateHarborEnvelope> create(
            CreateHarborRequest request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateHarborEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns one Harbor by its first-class Harbor ID.
     * <p>The response exposes the backing Connection ID but not the internal profile used to authorize Harbor credentials.</p>
     */
    public PolytomicHttpResponse<HarborEnvelope> get(String harborId) {
        return get(harborId, null);
    }

    /**
     * Returns one Harbor by its first-class Harbor ID.
     * <p>The response exposes the backing Connection ID but not the internal profile used to authorize Harbor credentials.</p>
     */
    public PolytomicHttpResponse<HarborEnvelope> get(String harborId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Updates a Harbor's name and description.
     * <p>This operation does not change <code>backing_mode</code> or <code>backing_connection_id</code>.</p>
     */
    public PolytomicHttpResponse<HarborEnvelope> update(String harborId, UpdateHarborRequest request) {
        return update(harborId, request, null);
    }

    /**
     * Updates a Harbor's name and description.
     * <p>This operation does not change <code>backing_mode</code> or <code>backing_connection_id</code>.</p>
     */
    public PolytomicHttpResponse<HarborEnvelope> update(
            String harborId, UpdateHarborRequest request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Deletes a Harbor and revokes its credentials.
     * <blockquote>
     * <p>🚧 Harbor deletion</p>
     * <p>Deleting a Harbor revokes its credentials, context documents, and user assignments. A customer-managed backing Connection remains available. Polytomic deletes a managed backing Connection only when no other resource uses it.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<DeletedHarborEnvelope> delete(String harborId) {
        return delete(harborId, null);
    }

    /**
     * Deletes a Harbor and revokes its credentials.
     * <blockquote>
     * <p>🚧 Harbor deletion</p>
     * <p>Deleting a Harbor revokes its credentials, context documents, and user assignments. A customer-managed backing Connection remains available. Polytomic deletes a managed backing Connection only when no other resource uses it.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<DeletedHarborEnvelope> delete(
            String harborId, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DeletedHarborEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Lists context document metadata for a Harbor without returning document content.
     * <p>Collection items include the current published <code>version</code> number and omit
     * <code>content</code>. Use the context item endpoint to retrieve a complete document.</p>
     * <p>A Harbor profile credential can read context only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public PolytomicHttpResponse<HarborContextListEnvelope> listContexts(String harborId) {
        return listContexts(harborId, HarborsListContextsRequest.builder().build());
    }

    /**
     * Lists context document metadata for a Harbor without returning document content.
     * <p>Collection items include the current published <code>version</code> number and omit
     * <code>content</code>. Use the context item endpoint to retrieve a complete document.</p>
     * <p>A Harbor profile credential can read context only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public PolytomicHttpResponse<HarborContextListEnvelope> listContexts(
            String harborId, RequestOptions requestOptions) {
        return listContexts(harborId, HarborsListContextsRequest.builder().build(), requestOptions);
    }

    /**
     * Lists context document metadata for a Harbor without returning document content.
     * <p>Collection items include the current published <code>version</code> number and omit
     * <code>content</code>. Use the context item endpoint to retrieve a complete document.</p>
     * <p>A Harbor profile credential can read context only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public PolytomicHttpResponse<HarborContextListEnvelope> listContexts(
            String harborId, HarborsListContextsRequest request) {
        return listContexts(harborId, request, null);
    }

    /**
     * Lists context document metadata for a Harbor without returning document content.
     * <p>Collection items include the current published <code>version</code> number and omit
     * <code>content</code>. Use the context item endpoint to retrieve a complete document.</p>
     * <p>A Harbor profile credential can read context only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public PolytomicHttpResponse<HarborContextListEnvelope> listContexts(
            String harborId, HarborsListContextsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("contexts");
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getPageToken().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_token", request.getPageToken().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        if (request.getPolytomicHarborSession().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Harbor-Session",
                    request.getPolytomicHarborSession().get());
        }
        if (request.getPolytomicActivityRequestId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Activity-Request-ID",
                    request.getPolytomicActivityRequestId().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborContextListEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Creates and attaches a context document to a Harbor.
     * <p>The new document belongs only to this Harbor. Context documents cannot be attached to multiple Harbors.</p>
     */
    public PolytomicHttpResponse<HarborContextEnvelope> createContext(
            String harborId, CreateHarborContextRequest request) {
        return createContext(harborId, request, null);
    }

    /**
     * Creates and attaches a context document to a Harbor.
     * <p>The new document belongs only to this Harbor. Context documents cannot be attached to multiple Harbors.</p>
     */
    public PolytomicHttpResponse<HarborContextEnvelope> createContext(
            String harborId, CreateHarborContextRequest request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("contexts");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborContextEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Lists mutable Harbor context drafts without returning document content.
     * <p>The collection includes drafts for published context documents and initial drafts
     * that have not yet been published. Use <code>context_id</code> with the draft detail,
     * replacement, promotion, and discard endpoints.</p>
     * <p>Draft metadata does not include <code>content</code>. Fetch a selected draft through its
     * detail endpoint to read the complete candidate. Normal context list and detail
     * operations continue to return published content only.</p>
     */
    public PolytomicHttpResponse<HarborContextDraftListEnvelope> listContextDrafts(String harborId) {
        return listContextDrafts(
                harborId, HarborsListContextDraftsRequest.builder().build());
    }

    /**
     * Lists mutable Harbor context drafts without returning document content.
     * <p>The collection includes drafts for published context documents and initial drafts
     * that have not yet been published. Use <code>context_id</code> with the draft detail,
     * replacement, promotion, and discard endpoints.</p>
     * <p>Draft metadata does not include <code>content</code>. Fetch a selected draft through its
     * detail endpoint to read the complete candidate. Normal context list and detail
     * operations continue to return published content only.</p>
     */
    public PolytomicHttpResponse<HarborContextDraftListEnvelope> listContextDrafts(
            String harborId, RequestOptions requestOptions) {
        return listContextDrafts(
                harborId, HarborsListContextDraftsRequest.builder().build(), requestOptions);
    }

    /**
     * Lists mutable Harbor context drafts without returning document content.
     * <p>The collection includes drafts for published context documents and initial drafts
     * that have not yet been published. Use <code>context_id</code> with the draft detail,
     * replacement, promotion, and discard endpoints.</p>
     * <p>Draft metadata does not include <code>content</code>. Fetch a selected draft through its
     * detail endpoint to read the complete candidate. Normal context list and detail
     * operations continue to return published content only.</p>
     */
    public PolytomicHttpResponse<HarborContextDraftListEnvelope> listContextDrafts(
            String harborId, HarborsListContextDraftsRequest request) {
        return listContextDrafts(harborId, request, null);
    }

    /**
     * Lists mutable Harbor context drafts without returning document content.
     * <p>The collection includes drafts for published context documents and initial drafts
     * that have not yet been published. Use <code>context_id</code> with the draft detail,
     * replacement, promotion, and discard endpoints.</p>
     * <p>Draft metadata does not include <code>content</code>. Fetch a selected draft through its
     * detail endpoint to read the complete candidate. Normal context list and detail
     * operations continue to return published content only.</p>
     */
    public PolytomicHttpResponse<HarborContextDraftListEnvelope> listContextDrafts(
            String harborId, HarborsListContextDraftsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("contexts")
                .addPathSegments("drafts");
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getPageToken().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_token", request.getPageToken().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborContextDraftListEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Creates an unpublished context document with its initial mutable draft.
     * <p>Creates a stable context identity and its initial mutable draft without publishing
     * content to the Harbor. The response includes <code>context_id</code>, which identifies the
     * draft replacement, promotion, and discard routes.</p>
     * <p>The initial draft has a null <code>base_revision_id</code>. It remains absent from normal
     * context list/detail, GraphQL, entity lookup, and Harbor MCP reads until promoted.
     * Use the regular context creation endpoint instead when the initial payload should
     * be published immediately.</p>
     */
    public PolytomicHttpResponse<HarborContextDraftEnvelope> createContextDraft(
            String harborId, CreateHarborContextDraftRequest request) {
        return createContextDraft(harborId, request, null);
    }

    /**
     * Creates an unpublished context document with its initial mutable draft.
     * <p>Creates a stable context identity and its initial mutable draft without publishing
     * content to the Harbor. The response includes <code>context_id</code>, which identifies the
     * draft replacement, promotion, and discard routes.</p>
     * <p>The initial draft has a null <code>base_revision_id</code>. It remains absent from normal
     * context list/detail, GraphQL, entity lookup, and Harbor MCP reads until promoted.
     * Use the regular context creation endpoint instead when the initial payload should
     * be published immediately.</p>
     */
    public PolytomicHttpResponse<HarborContextDraftEnvelope> createContextDraft(
            String harborId, CreateHarborContextDraftRequest request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("contexts")
                .addPathSegments("drafts");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborContextDraftEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns one complete Harbor context document.
     * <p>The response includes the current published <code>version</code> number and complete
     * plain-text <code>content</code>.</p>
     * <p>A Harbor profile credential can read context only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public PolytomicHttpResponse<HarborContextEnvelope> getContext(String harborId, String contextId) {
        return getContext(
                harborId, contextId, HarborsGetContextRequest.builder().build());
    }

    /**
     * Returns one complete Harbor context document.
     * <p>The response includes the current published <code>version</code> number and complete
     * plain-text <code>content</code>.</p>
     * <p>A Harbor profile credential can read context only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public PolytomicHttpResponse<HarborContextEnvelope> getContext(
            String harborId, String contextId, RequestOptions requestOptions) {
        return getContext(
                harborId, contextId, HarborsGetContextRequest.builder().build(), requestOptions);
    }

    /**
     * Returns one complete Harbor context document.
     * <p>The response includes the current published <code>version</code> number and complete
     * plain-text <code>content</code>.</p>
     * <p>A Harbor profile credential can read context only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public PolytomicHttpResponse<HarborContextEnvelope> getContext(
            String harborId, String contextId, HarborsGetContextRequest request) {
        return getContext(harborId, contextId, request, null);
    }

    /**
     * Returns one complete Harbor context document.
     * <p>The response includes the current published <code>version</code> number and complete
     * plain-text <code>content</code>.</p>
     * <p>A Harbor profile credential can read context only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public PolytomicHttpResponse<HarborContextEnvelope> getContext(
            String harborId, String contextId, HarborsGetContextRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("contexts")
                .addPathSegment(contextId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        if (request.getPolytomicHarborSession().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Harbor-Session",
                    request.getPolytomicHarborSession().get());
        }
        if (request.getPolytomicActivityRequestId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Activity-Request-ID",
                    request.getPolytomicActivityRequestId().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborContextEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Replaces one Harbor context document.
     * <p>Each successful request publishes the next immutable version. You can omit
     * <code>change_note</code>.</p>
     * <p>A direct publication leaves an existing draft unchanged.</p>
     */
    public PolytomicHttpResponse<HarborContextEnvelope> updateContext(
            String harborId, String contextId, SaveHarborContextRequest request) {
        return updateContext(harborId, contextId, request, null);
    }

    /**
     * Replaces one Harbor context document.
     * <p>Each successful request publishes the next immutable version. You can omit
     * <code>change_note</code>.</p>
     * <p>A direct publication leaves an existing draft unchanged.</p>
     */
    public PolytomicHttpResponse<HarborContextEnvelope> updateContext(
            String harborId,
            String contextId,
            SaveHarborContextRequest request,
            IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("contexts")
                .addPathSegment(contextId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        if (request.getPolytomicHarborSession().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Harbor-Session",
                    request.getPolytomicHarborSession().get());
        }
        if (request.getPolytomicActivityRequestId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Activity-Request-ID",
                    request.getPolytomicActivityRequestId().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborContextEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Deletes one context document from a Harbor.
     * <p>Deleting a context document does not affect the Harbor or its other context documents.</p>
     */
    public PolytomicHttpResponse<DeletedHarborContextEnvelope> deleteContext(String harborId, String contextId) {
        return deleteContext(
                harborId, contextId, HarborsDeleteContextRequest.builder().build());
    }

    /**
     * Deletes one context document from a Harbor.
     * <p>Deleting a context document does not affect the Harbor or its other context documents.</p>
     */
    public PolytomicHttpResponse<DeletedHarborContextEnvelope> deleteContext(
            String harborId, String contextId, IdempotentRequestOptions requestOptions) {
        return deleteContext(
                harborId, contextId, HarborsDeleteContextRequest.builder().build(), requestOptions);
    }

    /**
     * Deletes one context document from a Harbor.
     * <p>Deleting a context document does not affect the Harbor or its other context documents.</p>
     */
    public PolytomicHttpResponse<DeletedHarborContextEnvelope> deleteContext(
            String harborId, String contextId, HarborsDeleteContextRequest request) {
        return deleteContext(harborId, contextId, request, null);
    }

    /**
     * Deletes one context document from a Harbor.
     * <p>Deleting a context document does not affect the Harbor or its other context documents.</p>
     */
    public PolytomicHttpResponse<DeletedHarborContextEnvelope> deleteContext(
            String harborId,
            String contextId,
            HarborsDeleteContextRequest request,
            IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("contexts")
                .addPathSegment(contextId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        if (request.getPolytomicHarborSession().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Harbor-Session",
                    request.getPolytomicHarborSession().get());
        }
        if (request.getPolytomicActivityRequestId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Activity-Request-ID",
                    request.getPolytomicActivityRequestId().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DeletedHarborContextEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns the mutable draft for a Harbor context document.
     * <p>Drafts are available only through the draft endpoints. <code>base_revision_id</code>
     * identifies the published revision from which the candidate was created. Normal
     * context reads and Harbor MCP tools continue to return the current published
     * version. Creator and updater IDs are null when their actor type is <code>system</code>.</p>
     */
    public PolytomicHttpResponse<HarborContextDraftEnvelope> getContextDraft(String harborId, String contextId) {
        return getContextDraft(
                harborId, contextId, HarborsGetContextDraftRequest.builder().build());
    }

    /**
     * Returns the mutable draft for a Harbor context document.
     * <p>Drafts are available only through the draft endpoints. <code>base_revision_id</code>
     * identifies the published revision from which the candidate was created. Normal
     * context reads and Harbor MCP tools continue to return the current published
     * version. Creator and updater IDs are null when their actor type is <code>system</code>.</p>
     */
    public PolytomicHttpResponse<HarborContextDraftEnvelope> getContextDraft(
            String harborId, String contextId, RequestOptions requestOptions) {
        return getContextDraft(
                harborId, contextId, HarborsGetContextDraftRequest.builder().build(), requestOptions);
    }

    /**
     * Returns the mutable draft for a Harbor context document.
     * <p>Drafts are available only through the draft endpoints. <code>base_revision_id</code>
     * identifies the published revision from which the candidate was created. Normal
     * context reads and Harbor MCP tools continue to return the current published
     * version. Creator and updater IDs are null when their actor type is <code>system</code>.</p>
     */
    public PolytomicHttpResponse<HarborContextDraftEnvelope> getContextDraft(
            String harborId, String contextId, HarborsGetContextDraftRequest request) {
        return getContextDraft(harborId, contextId, request, null);
    }

    /**
     * Returns the mutable draft for a Harbor context document.
     * <p>Drafts are available only through the draft endpoints. <code>base_revision_id</code>
     * identifies the published revision from which the candidate was created. Normal
     * context reads and Harbor MCP tools continue to return the current published
     * version. Creator and updater IDs are null when their actor type is <code>system</code>.</p>
     */
    public PolytomicHttpResponse<HarborContextDraftEnvelope> getContextDraft(
            String harborId, String contextId, HarborsGetContextDraftRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("contexts")
                .addPathSegment(contextId)
                .addPathSegments("draft");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        if (request.getPolytomicHarborSession().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Harbor-Session",
                    request.getPolytomicHarborSession().get());
        }
        if (request.getPolytomicActivityRequestId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Activity-Request-ID",
                    request.getPolytomicActivityRequestId().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborContextDraftEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Creates or completely replaces the mutable draft for a Harbor context document.
     * <p>The request supplies the complete draft payload. If a draft already exists,
     * this request replaces it while preserving the draft ID, creation metadata, and
     * <code>base_revision_id</code>.</p>
     * <p>A new draft for a published document records the current revision as its base.
     * An unpublished document's initial draft keeps a null base when replaced. Saving
     * a draft does not change published content or its <code>updated_at</code>. Creator and
     * updater provenance comes from the request actor; system actors have a null actor
     * ID.</p>
     */
    public PolytomicHttpResponse<HarborContextDraftEnvelope> saveContextDraft(
            String harborId, String contextId, SaveHarborContextDraftRequest request) {
        return saveContextDraft(harborId, contextId, request, null);
    }

    /**
     * Creates or completely replaces the mutable draft for a Harbor context document.
     * <p>The request supplies the complete draft payload. If a draft already exists,
     * this request replaces it while preserving the draft ID, creation metadata, and
     * <code>base_revision_id</code>.</p>
     * <p>A new draft for a published document records the current revision as its base.
     * An unpublished document's initial draft keeps a null base when replaced. Saving
     * a draft does not change published content or its <code>updated_at</code>. Creator and
     * updater provenance comes from the request actor; system actors have a null actor
     * ID.</p>
     */
    public PolytomicHttpResponse<HarborContextDraftEnvelope> saveContextDraft(
            String harborId,
            String contextId,
            SaveHarborContextDraftRequest request,
            IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("contexts")
                .addPathSegment(contextId)
                .addPathSegments("draft");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        if (request.getPolytomicHarborSession().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Harbor-Session",
                    request.getPolytomicHarborSession().get());
        }
        if (request.getPolytomicActivityRequestId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Activity-Request-ID",
                    request.getPolytomicActivityRequestId().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborContextDraftEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Discards the mutable draft for a Harbor context document.
     * <p>Discarding a draft does not change the current published version or its
     * history. If the draft belongs to a context that has never been published,
     * discarding it also removes the otherwise empty context identity.</p>
     */
    public PolytomicHttpResponse<DeletedHarborContextDraftEnvelope> deleteContextDraft(
            String harborId, String contextId) {
        return deleteContextDraft(
                harborId, contextId, HarborsDeleteContextDraftRequest.builder().build());
    }

    /**
     * Discards the mutable draft for a Harbor context document.
     * <p>Discarding a draft does not change the current published version or its
     * history. If the draft belongs to a context that has never been published,
     * discarding it also removes the otherwise empty context identity.</p>
     */
    public PolytomicHttpResponse<DeletedHarborContextDraftEnvelope> deleteContextDraft(
            String harborId, String contextId, IdempotentRequestOptions requestOptions) {
        return deleteContextDraft(
                harborId, contextId, HarborsDeleteContextDraftRequest.builder().build(), requestOptions);
    }

    /**
     * Discards the mutable draft for a Harbor context document.
     * <p>Discarding a draft does not change the current published version or its
     * history. If the draft belongs to a context that has never been published,
     * discarding it also removes the otherwise empty context identity.</p>
     */
    public PolytomicHttpResponse<DeletedHarborContextDraftEnvelope> deleteContextDraft(
            String harborId, String contextId, HarborsDeleteContextDraftRequest request) {
        return deleteContextDraft(harborId, contextId, request, null);
    }

    /**
     * Discards the mutable draft for a Harbor context document.
     * <p>Discarding a draft does not change the current published version or its
     * history. If the draft belongs to a context that has never been published,
     * discarding it also removes the otherwise empty context identity.</p>
     */
    public PolytomicHttpResponse<DeletedHarborContextDraftEnvelope> deleteContextDraft(
            String harborId,
            String contextId,
            HarborsDeleteContextDraftRequest request,
            IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("contexts")
                .addPathSegment(contextId)
                .addPathSegments("draft");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        if (request.getPolytomicHarborSession().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Harbor-Session",
                    request.getPolytomicHarborSession().get());
        }
        if (request.getPolytomicActivityRequestId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Activity-Request-ID",
                    request.getPolytomicActivityRequestId().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, DeletedHarborContextDraftEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Promotes the draft to the next immutable published context version.
     * <p>Promotion publishes the draft's exact title, description, content, and optional
     * change note as the next version. An unpublished context's initial draft becomes
     * version 1. The draft is removed after publication.</p>
     * <p>Promotion returns a conflict when the current published revision differs from
     * the draft's <code>base_revision_id</code>. The stale draft remains available so an author
     * can compare it with the current version before discarding and recreating it.</p>
     */
    public PolytomicHttpResponse<HarborContextVersionEnvelope> promoteContextDraft(String harborId, String contextId) {
        return promoteContextDraft(
                harborId, contextId, HarborsPromoteContextDraftRequest.builder().build());
    }

    /**
     * Promotes the draft to the next immutable published context version.
     * <p>Promotion publishes the draft's exact title, description, content, and optional
     * change note as the next version. An unpublished context's initial draft becomes
     * version 1. The draft is removed after publication.</p>
     * <p>Promotion returns a conflict when the current published revision differs from
     * the draft's <code>base_revision_id</code>. The stale draft remains available so an author
     * can compare it with the current version before discarding and recreating it.</p>
     */
    public PolytomicHttpResponse<HarborContextVersionEnvelope> promoteContextDraft(
            String harborId, String contextId, IdempotentRequestOptions requestOptions) {
        return promoteContextDraft(
                harborId, contextId, HarborsPromoteContextDraftRequest.builder().build(), requestOptions);
    }

    /**
     * Promotes the draft to the next immutable published context version.
     * <p>Promotion publishes the draft's exact title, description, content, and optional
     * change note as the next version. An unpublished context's initial draft becomes
     * version 1. The draft is removed after publication.</p>
     * <p>Promotion returns a conflict when the current published revision differs from
     * the draft's <code>base_revision_id</code>. The stale draft remains available so an author
     * can compare it with the current version before discarding and recreating it.</p>
     */
    public PolytomicHttpResponse<HarborContextVersionEnvelope> promoteContextDraft(
            String harborId, String contextId, HarborsPromoteContextDraftRequest request) {
        return promoteContextDraft(harborId, contextId, request, null);
    }

    /**
     * Promotes the draft to the next immutable published context version.
     * <p>Promotion publishes the draft's exact title, description, content, and optional
     * change note as the next version. An unpublished context's initial draft becomes
     * version 1. The draft is removed after publication.</p>
     * <p>Promotion returns a conflict when the current published revision differs from
     * the draft's <code>base_revision_id</code>. The stale draft remains available so an author
     * can compare it with the current version before discarding and recreating it.</p>
     */
    public PolytomicHttpResponse<HarborContextVersionEnvelope> promoteContextDraft(
            String harborId,
            String contextId,
            HarborsPromoteContextDraftRequest request,
            IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("contexts")
                .addPathSegment(contextId)
                .addPathSegments("draft")
                .addPathSegments("promote");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        if (request.getPolytomicHarborSession().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Harbor-Session",
                    request.getPolytomicHarborSession().get());
        }
        if (request.getPolytomicActivityRequestId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Activity-Request-ID",
                    request.getPolytomicActivityRequestId().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborContextVersionEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Lists immutable published versions of a Harbor context document without returning content.
     * <p>Versions are ordered from newest to oldest. Collection items omit <code>content</code>,
     * and the draft is never included. Pagination tokens continue from the last
     * returned version, so publishing a newer version between requests does not shift
     * or duplicate older results.</p>
     * <p>Published revision IDs are durable artifact identities intended for future
     * Harbor activity and audit records. Publisher IDs are paired with actor types.
     * System publications have <code>published_by_type: &quot;system&quot;</code> and a null
     * <code>published_by</code> because the system actor has no UUID.</p>
     * <p>A Harbor profile credential can read versions only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public PolytomicHttpResponse<HarborContextVersionListEnvelope> listContextVersions(
            String harborId, String contextId) {
        return listContextVersions(
                harborId, contextId, HarborsListContextVersionsRequest.builder().build());
    }

    /**
     * Lists immutable published versions of a Harbor context document without returning content.
     * <p>Versions are ordered from newest to oldest. Collection items omit <code>content</code>,
     * and the draft is never included. Pagination tokens continue from the last
     * returned version, so publishing a newer version between requests does not shift
     * or duplicate older results.</p>
     * <p>Published revision IDs are durable artifact identities intended for future
     * Harbor activity and audit records. Publisher IDs are paired with actor types.
     * System publications have <code>published_by_type: &quot;system&quot;</code> and a null
     * <code>published_by</code> because the system actor has no UUID.</p>
     * <p>A Harbor profile credential can read versions only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public PolytomicHttpResponse<HarborContextVersionListEnvelope> listContextVersions(
            String harborId, String contextId, RequestOptions requestOptions) {
        return listContextVersions(
                harborId, contextId, HarborsListContextVersionsRequest.builder().build(), requestOptions);
    }

    /**
     * Lists immutable published versions of a Harbor context document without returning content.
     * <p>Versions are ordered from newest to oldest. Collection items omit <code>content</code>,
     * and the draft is never included. Pagination tokens continue from the last
     * returned version, so publishing a newer version between requests does not shift
     * or duplicate older results.</p>
     * <p>Published revision IDs are durable artifact identities intended for future
     * Harbor activity and audit records. Publisher IDs are paired with actor types.
     * System publications have <code>published_by_type: &quot;system&quot;</code> and a null
     * <code>published_by</code> because the system actor has no UUID.</p>
     * <p>A Harbor profile credential can read versions only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public PolytomicHttpResponse<HarborContextVersionListEnvelope> listContextVersions(
            String harborId, String contextId, HarborsListContextVersionsRequest request) {
        return listContextVersions(harborId, contextId, request, null);
    }

    /**
     * Lists immutable published versions of a Harbor context document without returning content.
     * <p>Versions are ordered from newest to oldest. Collection items omit <code>content</code>,
     * and the draft is never included. Pagination tokens continue from the last
     * returned version, so publishing a newer version between requests does not shift
     * or duplicate older results.</p>
     * <p>Published revision IDs are durable artifact identities intended for future
     * Harbor activity and audit records. Publisher IDs are paired with actor types.
     * System publications have <code>published_by_type: &quot;system&quot;</code> and a null
     * <code>published_by</code> because the system actor has no UUID.</p>
     * <p>A Harbor profile credential can read versions only when <code>harbor_id</code> identifies
     * its own Harbor.</p>
     */
    public PolytomicHttpResponse<HarborContextVersionListEnvelope> listContextVersions(
            String harborId,
            String contextId,
            HarborsListContextVersionsRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("contexts")
                .addPathSegment(contextId)
                .addPathSegments("versions");
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getPageToken().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_token", request.getPageToken().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        if (request.getPolytomicHarborSession().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Harbor-Session",
                    request.getPolytomicHarborSession().get());
        }
        if (request.getPolytomicActivityRequestId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Activity-Request-ID",
                    request.getPolytomicActivityRequestId().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborContextVersionListEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns one immutable published version of a Harbor context document.
     * <p>The response includes the complete title, description, and plain-text
     * <code>content</code> captured when the version was published.</p>
     * <p>Published versions cannot be changed or deleted. System publications have
     * <code>published_by_type: &quot;system&quot;</code> and a null <code>published_by</code>.</p>
     */
    public PolytomicHttpResponse<HarborContextVersionEnvelope> getContextVersion(
            String harborId, String contextId, String versionId) {
        return getContextVersion(
                harborId,
                contextId,
                versionId,
                HarborsGetContextVersionRequest.builder().build());
    }

    /**
     * Returns one immutable published version of a Harbor context document.
     * <p>The response includes the complete title, description, and plain-text
     * <code>content</code> captured when the version was published.</p>
     * <p>Published versions cannot be changed or deleted. System publications have
     * <code>published_by_type: &quot;system&quot;</code> and a null <code>published_by</code>.</p>
     */
    public PolytomicHttpResponse<HarborContextVersionEnvelope> getContextVersion(
            String harborId, String contextId, String versionId, RequestOptions requestOptions) {
        return getContextVersion(
                harborId,
                contextId,
                versionId,
                HarborsGetContextVersionRequest.builder().build(),
                requestOptions);
    }

    /**
     * Returns one immutable published version of a Harbor context document.
     * <p>The response includes the complete title, description, and plain-text
     * <code>content</code> captured when the version was published.</p>
     * <p>Published versions cannot be changed or deleted. System publications have
     * <code>published_by_type: &quot;system&quot;</code> and a null <code>published_by</code>.</p>
     */
    public PolytomicHttpResponse<HarborContextVersionEnvelope> getContextVersion(
            String harborId, String contextId, String versionId, HarborsGetContextVersionRequest request) {
        return getContextVersion(harborId, contextId, versionId, request, null);
    }

    /**
     * Returns one immutable published version of a Harbor context document.
     * <p>The response includes the complete title, description, and plain-text
     * <code>content</code> captured when the version was published.</p>
     * <p>Published versions cannot be changed or deleted. System publications have
     * <code>published_by_type: &quot;system&quot;</code> and a null <code>published_by</code>.</p>
     */
    public PolytomicHttpResponse<HarborContextVersionEnvelope> getContextVersion(
            String harborId,
            String contextId,
            String versionId,
            HarborsGetContextVersionRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("contexts")
                .addPathSegment(contextId)
                .addPathSegments("versions")
                .addPathSegment(versionId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        if (request.getPolytomicHarborSession().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Harbor-Session",
                    request.getPolytomicHarborSession().get());
        }
        if (request.getPolytomicActivityRequestId().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Polytomic-Activity-Request-ID",
                    request.getPolytomicActivityRequestId().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborContextVersionEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Lists active masked credentials for a Harbor.
     * <p>Each item contains a masked <code>key_hint</code>. Polytomic never returns a credential plaintext after creation.</p>
     */
    public PolytomicHttpResponse<HarborKeyListEnvelope> listKeys(String harborId) {
        return listKeys(harborId, HarborsListKeysRequest.builder().build());
    }

    /**
     * Lists active masked credentials for a Harbor.
     * <p>Each item contains a masked <code>key_hint</code>. Polytomic never returns a credential plaintext after creation.</p>
     */
    public PolytomicHttpResponse<HarborKeyListEnvelope> listKeys(String harborId, RequestOptions requestOptions) {
        return listKeys(harborId, HarborsListKeysRequest.builder().build(), requestOptions);
    }

    /**
     * Lists active masked credentials for a Harbor.
     * <p>Each item contains a masked <code>key_hint</code>. Polytomic never returns a credential plaintext after creation.</p>
     */
    public PolytomicHttpResponse<HarborKeyListEnvelope> listKeys(String harborId, HarborsListKeysRequest request) {
        return listKeys(harborId, request, null);
    }

    /**
     * Lists active masked credentials for a Harbor.
     * <p>Each item contains a masked <code>key_hint</code>. Polytomic never returns a credential plaintext after creation.</p>
     */
    public PolytomicHttpResponse<HarborKeyListEnvelope> listKeys(
            String harborId, HarborsListKeysRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("keys");
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getPageToken().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_token", request.getPageToken().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborKeyListEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Generates a new Harbor credential and returns its plaintext value once.
     * <p>Store the returned <code>value</code> securely. Polytomic returns it only in this response.</p>
     */
    public PolytomicHttpResponse<HarborKeyEnvelope> createKey(String harborId) {
        return createKey(harborId, null);
    }

    /**
     * Generates a new Harbor credential and returns its plaintext value once.
     * <p>Store the returned <code>value</code> securely. Polytomic returns it only in this response.</p>
     */
    public PolytomicHttpResponse<HarborKeyEnvelope> createKey(
            String harborId, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("keys");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborKeyEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Revokes one Harbor credential by its credential ID.
     * <p>Revocation affects only the selected credential. Other active Harbor credentials remain valid.</p>
     */
    public PolytomicHttpResponse<RevokedHarborKeyEnvelope> deleteKey(String harborId, String keyId) {
        return deleteKey(harborId, keyId, null);
    }

    /**
     * Revokes one Harbor credential by its credential ID.
     * <p>Revocation affects only the selected credential. Other active Harbor credentials remain valid.</p>
     */
    public PolytomicHttpResponse<RevokedHarborKeyEnvelope> deleteKey(
            String harborId, String keyId, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("keys")
                .addPathSegment(keyId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RevokedHarborKeyEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Resolves documented source table and field identities to the names a Harbor's backing Connection accepts.
     */
    public PolytomicHttpResponse<ResolveHarborSourceMappingsEnvelope> resolveSourceMappings(String harborId) {
        return resolveSourceMappings(
                harborId, ResolveHarborSourceMappingsRequest.builder().build());
    }

    /**
     * Resolves documented source table and field identities to the names a Harbor's backing Connection accepts.
     */
    public PolytomicHttpResponse<ResolveHarborSourceMappingsEnvelope> resolveSourceMappings(
            String harborId, IdempotentRequestOptions requestOptions) {
        return resolveSourceMappings(
                harborId, ResolveHarborSourceMappingsRequest.builder().build(), requestOptions);
    }

    /**
     * Resolves documented source table and field identities to the names a Harbor's backing Connection accepts.
     */
    public PolytomicHttpResponse<ResolveHarborSourceMappingsEnvelope> resolveSourceMappings(
            String harborId, ResolveHarborSourceMappingsRequest request) {
        return resolveSourceMappings(harborId, request, null);
    }

    /**
     * Resolves documented source table and field identities to the names a Harbor's backing Connection accepts.
     */
    public PolytomicHttpResponse<ResolveHarborSourceMappingsEnvelope> resolveSourceMappings(
            String harborId, ResolveHarborSourceMappingsRequest request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("source-mappings")
                .addPathSegments("resolve");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ResolveHarborSourceMappingsEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns raw Polytomic refresh evidence for datasets written to a Harbor.
     * <p>Each pipeline corresponds to a bulk sync or model sync that writes at least one
     * dataset to the Harbor's backing Connection. Tables populated outside Polytomic
     * are not included, even when they are queryable through a customer-managed
     * backing Connection.</p>
     * <p>The response groups shared pipeline evidence so schedules and configuration are
     * not repeated for every dataset:</p>
     * <ul>
     * <li>Each entry in <code>pipelines</code> identifies the producer through <code>type</code> and <code>id</code>.
     * Separate pipelines targeting the same physical dataset remain separate
     * entries.</li>
     * <li><code>datasets</code> is keyed by the effective destination dataset name.
     * <code>last_success_at</code> is the start time of the most recent execution in which that
     * dataset completed successfully, including a successful dataset within a bulk
     * execution that completes with errors. The start time is a conservative upper
     * bound because source reads and destination writes happen afterward.</li>
     * <li><code>latest_status</code> preserves the latest Polytomic execution status. Never-run
     * datasets omit this field.</li>
     * <li>Pipeline-level <code>schedules</code> preserves schedule parameters and selectors. A
     * schedule can be manual, event-driven, advanced, selective, or limited to
     * named source schemas, so callers should not reduce the list to one inferred
     * cadence.</li>
     * <li>Continuous schedules use the scheduler's persisted next firing. If scheduler
     * state is unavailable, <code>next_run_at</code> is omitted rather than recalculated with
     * new jitter.</li>
     * <li>Paused pipelines remain present with <code>refresh_enabled</code> set to <code>false</code> and no
     * <code>next_run_at</code>.</li>
     * </ul>
     * <p>Use absolute timestamps and the raw statuses to apply the maximum acceptable
     * staleness for your task. The endpoint does not classify datasets or the Harbor
     * as healthy, stale, or unhealthy.</p>
     */
    public PolytomicHttpResponse<HarborStatusEnvelope> getStatus(String harborId) {
        return getStatus(harborId, null);
    }

    /**
     * Returns raw Polytomic refresh evidence for datasets written to a Harbor.
     * <p>Each pipeline corresponds to a bulk sync or model sync that writes at least one
     * dataset to the Harbor's backing Connection. Tables populated outside Polytomic
     * are not included, even when they are queryable through a customer-managed
     * backing Connection.</p>
     * <p>The response groups shared pipeline evidence so schedules and configuration are
     * not repeated for every dataset:</p>
     * <ul>
     * <li>Each entry in <code>pipelines</code> identifies the producer through <code>type</code> and <code>id</code>.
     * Separate pipelines targeting the same physical dataset remain separate
     * entries.</li>
     * <li><code>datasets</code> is keyed by the effective destination dataset name.
     * <code>last_success_at</code> is the start time of the most recent execution in which that
     * dataset completed successfully, including a successful dataset within a bulk
     * execution that completes with errors. The start time is a conservative upper
     * bound because source reads and destination writes happen afterward.</li>
     * <li><code>latest_status</code> preserves the latest Polytomic execution status. Never-run
     * datasets omit this field.</li>
     * <li>Pipeline-level <code>schedules</code> preserves schedule parameters and selectors. A
     * schedule can be manual, event-driven, advanced, selective, or limited to
     * named source schemas, so callers should not reduce the list to one inferred
     * cadence.</li>
     * <li>Continuous schedules use the scheduler's persisted next firing. If scheduler
     * state is unavailable, <code>next_run_at</code> is omitted rather than recalculated with
     * new jitter.</li>
     * <li>Paused pipelines remain present with <code>refresh_enabled</code> set to <code>false</code> and no
     * <code>next_run_at</code>.</li>
     * </ul>
     * <p>Use absolute timestamps and the raw statuses to apply the maximum acceptable
     * staleness for your task. The endpoint does not classify datasets or the Harbor
     * as healthy, stale, or unhealthy.</p>
     */
    public PolytomicHttpResponse<HarborStatusEnvelope> getStatus(String harborId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("status");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborStatusEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Lists Harbor-only users assigned to a Harbor.
     * <p>The response contains only Harbor-only users currently assigned to this Harbor.</p>
     */
    public PolytomicHttpResponse<HarborUserListEnvelope> listUsers(String harborId) {
        return listUsers(harborId, HarborsListUsersRequest.builder().build());
    }

    /**
     * Lists Harbor-only users assigned to a Harbor.
     * <p>The response contains only Harbor-only users currently assigned to this Harbor.</p>
     */
    public PolytomicHttpResponse<HarborUserListEnvelope> listUsers(String harborId, RequestOptions requestOptions) {
        return listUsers(harborId, HarborsListUsersRequest.builder().build(), requestOptions);
    }

    /**
     * Lists Harbor-only users assigned to a Harbor.
     * <p>The response contains only Harbor-only users currently assigned to this Harbor.</p>
     */
    public PolytomicHttpResponse<HarborUserListEnvelope> listUsers(String harborId, HarborsListUsersRequest request) {
        return listUsers(harborId, request, null);
    }

    /**
     * Lists Harbor-only users assigned to a Harbor.
     * <p>The response contains only Harbor-only users currently assigned to this Harbor.</p>
     */
    public PolytomicHttpResponse<HarborUserListEnvelope> listUsers(
            String harborId, HarborsListUsersRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("users");
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getPageToken().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_token", request.getPageToken().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborUserListEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Invites and assigns a new Harbor-only user.
     * <p>The invited account is restricted to assigned Harbors and does not receive regular Polytomic application access.</p>
     */
    public PolytomicHttpResponse<HarborUserEnvelope> inviteUser(String harborId, InviteHarborUserRequest request) {
        return inviteUser(harborId, request, null);
    }

    /**
     * Invites and assigns a new Harbor-only user.
     * <p>The invited account is restricted to assigned Harbors and does not receive regular Polytomic application access.</p>
     */
    public PolytomicHttpResponse<HarborUserEnvelope> inviteUser(
            String harborId, InviteHarborUserRequest request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("users");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborUserEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Assigns an existing Harbor-only user to a Harbor.
     * <p>The assignment is idempotent. Regular Polytomic users cannot be assigned because they already have application access to Harbors.</p>
     */
    public PolytomicHttpResponse<HarborUserEnvelope> assignUser(String harborId, String userId) {
        return assignUser(harborId, userId, null);
    }

    /**
     * Assigns an existing Harbor-only user to a Harbor.
     * <p>The assignment is idempotent. Regular Polytomic users cannot be assigned because they already have application access to Harbors.</p>
     */
    public PolytomicHttpResponse<HarborUserEnvelope> assignUser(
            String harborId, String userId, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("users")
                .addPathSegment(userId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborUserEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Removes a Harbor assignment without deleting the user.
     * <p>This removes only the Harbor assignment. The organization user remains available and may retain assignments to other Harbors.</p>
     */
    public PolytomicHttpResponse<UnassignedHarborUserEnvelope> unassignUser(String harborId, String userId) {
        return unassignUser(harborId, userId, null);
    }

    /**
     * Removes a Harbor assignment without deleting the user.
     * <p>This removes only the Harbor assignment. The organization user remains available and may retain assignments to other Harbors.</p>
     */
    public PolytomicHttpResponse<UnassignedHarborUserEnvelope> unassignUser(
            String harborId, String userId, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/harbors")
                .addPathSegment(harborId)
                .addPathSegments("users")
                .addPathSegment(userId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UnassignedHarborUserEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }
}
