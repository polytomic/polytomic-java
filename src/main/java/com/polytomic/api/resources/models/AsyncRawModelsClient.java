/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.polytomic.api.resources.models;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.polytomic.api.core.ClientOptions;
import com.polytomic.api.core.IdempotentRequestOptions;
import com.polytomic.api.core.MediaTypes;
import com.polytomic.api.core.ObjectMappers;
import com.polytomic.api.core.PolytomicApiException;
import com.polytomic.api.core.PolytomicException;
import com.polytomic.api.core.PolytomicHttpResponse;
import com.polytomic.api.core.QueryStringMapper;
import com.polytomic.api.core.RequestOptions;
import com.polytomic.api.errors.BadRequestError;
import com.polytomic.api.errors.ForbiddenError;
import com.polytomic.api.errors.InternalServerError;
import com.polytomic.api.errors.NotFoundError;
import com.polytomic.api.resources.models.requests.EnrichmentInputFieldsRequest;
import com.polytomic.api.resources.models.requests.ModelsCreateRequest;
import com.polytomic.api.resources.models.requests.ModelsGetEnrichmentSourceRequest;
import com.polytomic.api.resources.models.requests.ModelsGetRequest;
import com.polytomic.api.resources.models.requests.ModelsPreviewRequest;
import com.polytomic.api.resources.models.requests.ModelsRemoveRequest;
import com.polytomic.api.resources.models.requests.ModelsSampleRequest;
import com.polytomic.api.resources.models.requests.UpdateModelRequest;
import com.polytomic.api.types.ApiError;
import com.polytomic.api.types.CreateModelRequest;
import com.polytomic.api.types.GetEnrichmentInputFieldsResponseEnvelope;
import com.polytomic.api.types.GetSyncSourceMetaEnvelope;
import com.polytomic.api.types.ModelListResponseEnvelope;
import com.polytomic.api.types.ModelResponseEnvelope;
import com.polytomic.api.types.ModelSampleResponseEnvelope;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawModelsClient {
    protected final ClientOptions clientOptions;

    public AsyncRawModelsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Describes the enrichment source configuration available on a connection.
     * <p>Not all connections support enrichment. Call this endpoint to determine
     * whether a connection can serve as an enrichment source in a model sync and,
     * if so, what configuration it accepts.</p>
     * <blockquote>
     * <p>⚠️ If the connection does not support enrichment, this endpoint returns
     * <code>404</code>. Check for that status before attempting to configure an enrichment
     * source on a sync.</p>
     * </blockquote>
     * <p>When a connection does support enrichment, the response describes the
     * configuration fields required to set it up. Pass those values in the
     * <code>enrichment</code> block when creating or updating a model sync.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<GetSyncSourceMetaEnvelope>> getEnrichmentSource(String id) {
        return getEnrichmentSource(
                id, ModelsGetEnrichmentSourceRequest.builder().build());
    }

    /**
     * Describes the enrichment source configuration available on a connection.
     * <p>Not all connections support enrichment. Call this endpoint to determine
     * whether a connection can serve as an enrichment source in a model sync and,
     * if so, what configuration it accepts.</p>
     * <blockquote>
     * <p>⚠️ If the connection does not support enrichment, this endpoint returns
     * <code>404</code>. Check for that status before attempting to configure an enrichment
     * source on a sync.</p>
     * </blockquote>
     * <p>When a connection does support enrichment, the response describes the
     * configuration fields required to set it up. Pass those values in the
     * <code>enrichment</code> block when creating or updating a model sync.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<GetSyncSourceMetaEnvelope>> getEnrichmentSource(
            String id, RequestOptions requestOptions) {
        return getEnrichmentSource(
                id, ModelsGetEnrichmentSourceRequest.builder().build(), requestOptions);
    }

    /**
     * Describes the enrichment source configuration available on a connection.
     * <p>Not all connections support enrichment. Call this endpoint to determine
     * whether a connection can serve as an enrichment source in a model sync and,
     * if so, what configuration it accepts.</p>
     * <blockquote>
     * <p>⚠️ If the connection does not support enrichment, this endpoint returns
     * <code>404</code>. Check for that status before attempting to configure an enrichment
     * source on a sync.</p>
     * </blockquote>
     * <p>When a connection does support enrichment, the response describes the
     * configuration fields required to set it up. Pass those values in the
     * <code>enrichment</code> block when creating or updating a model sync.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<GetSyncSourceMetaEnvelope>> getEnrichmentSource(
            String id, ModelsGetEnrichmentSourceRequest request) {
        return getEnrichmentSource(id, request, null);
    }

    /**
     * Describes the enrichment source configuration available on a connection.
     * <p>Not all connections support enrichment. Call this endpoint to determine
     * whether a connection can serve as an enrichment source in a model sync and,
     * if so, what configuration it accepts.</p>
     * <blockquote>
     * <p>⚠️ If the connection does not support enrichment, this endpoint returns
     * <code>404</code>. Check for that status before attempting to configure an enrichment
     * source on a sync.</p>
     * </blockquote>
     * <p>When a connection does support enrichment, the response describes the
     * configuration fields required to set it up. Pass those values in the
     * <code>enrichment</code> block when creating or updating a model sync.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<GetSyncSourceMetaEnvelope>> getEnrichmentSource(
            String id, ModelsGetEnrichmentSourceRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/connections")
                .addPathSegment(id)
                .addPathSegments("modelsync")
                .addPathSegments("enrichment-source");
        if (request.getParams().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "params", request.getParams().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<GetSyncSourceMetaEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, GetSyncSourceMetaEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Returns the valid input field sets for an enrichment configuration on a connection.
     * <p>When configuring an enrichment source in a model sync, use this endpoint to
     * discover which input fields the enrichment connection requires. Pass the
     * proposed enrichment configuration in the request body; the response lists the
     * valid input field sets that map your model's fields to the enrichment service's
     * expected inputs.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<GetEnrichmentInputFieldsResponseEnvelope>> post(
            String connectionId) {
        return post(connectionId, EnrichmentInputFieldsRequest.builder().build());
    }

    /**
     * Returns the valid input field sets for an enrichment configuration on a connection.
     * <p>When configuring an enrichment source in a model sync, use this endpoint to
     * discover which input fields the enrichment connection requires. Pass the
     * proposed enrichment configuration in the request body; the response lists the
     * valid input field sets that map your model's fields to the enrichment service's
     * expected inputs.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<GetEnrichmentInputFieldsResponseEnvelope>> post(
            String connectionId, IdempotentRequestOptions requestOptions) {
        return post(connectionId, EnrichmentInputFieldsRequest.builder().build(), requestOptions);
    }

    /**
     * Returns the valid input field sets for an enrichment configuration on a connection.
     * <p>When configuring an enrichment source in a model sync, use this endpoint to
     * discover which input fields the enrichment connection requires. Pass the
     * proposed enrichment configuration in the request body; the response lists the
     * valid input field sets that map your model's fields to the enrichment service's
     * expected inputs.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<GetEnrichmentInputFieldsResponseEnvelope>> post(
            String connectionId, EnrichmentInputFieldsRequest request) {
        return post(connectionId, request, null);
    }

    /**
     * Returns the valid input field sets for an enrichment configuration on a connection.
     * <p>When configuring an enrichment source in a model sync, use this endpoint to
     * discover which input fields the enrichment connection requires. Pass the
     * proposed enrichment configuration in the request body; the response lists the
     * valid input field sets that map your model's fields to the enrichment service's
     * expected inputs.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<GetEnrichmentInputFieldsResponseEnvelope>> post(
            String connectionId, EnrichmentInputFieldsRequest request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/enrichment")
                .addPathSegment(connectionId)
                .addPathSegments("inputfields");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<GetEnrichmentInputFieldsResponseEnvelope>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, GetEnrichmentInputFieldsResponseEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Submits a job that previews the fields a model would expose without persisting it.
     * <p>The response contains a job ID that resolves to the list of fields the model
     * would expose. Poll the job until it completes to retrieve the field list. The
     * model is not persisted — this endpoint is useful for validating a query or
     * configuration before calling <a href="../../api-reference/models/create"><code>POST /api/models</code></a> to save it.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> preview(CreateModelRequest body) {
        return preview(ModelsPreviewRequest.builder().body(body).build());
    }

    /**
     * Submits a job that previews the fields a model would expose without persisting it.
     * <p>The response contains a job ID that resolves to the list of fields the model
     * would expose. Poll the job until it completes to retrieve the field list. The
     * model is not persisted — this endpoint is useful for validating a query or
     * configuration before calling <a href="../../api-reference/models/create"><code>POST /api/models</code></a> to save it.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> preview(
            CreateModelRequest body, IdempotentRequestOptions requestOptions) {
        return preview(ModelsPreviewRequest.builder().body(body).build(), requestOptions);
    }

    /**
     * Submits a job that previews the fields a model would expose without persisting it.
     * <p>The response contains a job ID that resolves to the list of fields the model
     * would expose. Poll the job until it completes to retrieve the field list. The
     * model is not persisted — this endpoint is useful for validating a query or
     * configuration before calling <a href="../../api-reference/models/create"><code>POST /api/models</code></a> to save it.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> preview(ModelsPreviewRequest request) {
        return preview(request, null);
    }

    /**
     * Submits a job that previews the fields a model would expose without persisting it.
     * <p>The response contains a job ID that resolves to the list of fields the model
     * would expose. Poll the job until it completes to retrieve the field list. The
     * model is not persisted — this endpoint is useful for validating a query or
     * configuration before calling <a href="../../api-reference/models/create"><code>POST /api/models</code></a> to save it.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> preview(
            ModelsPreviewRequest request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/model-preview");
        if (request.getAsync().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "async", request.getAsync().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ModelResponseEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Lists all models in the caller's organization.
     * <p>Results are ordered by <code>updated_at</code> descending, with <code>id</code> used as a tiebreaker.
     * If more results are available, the response includes <code>pagination.next_page_token</code>.
     * Pass that token back unchanged to continue from the last item you received.</p>
     * <p>The token is opaque. Do not construct or edit it yourself.</p>
     * <p>The <code>limit</code> is capped at 50. Values above that cap are reduced to 50, and
     * non-positive values fall back to the same default.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelListResponseEnvelope>> list() {
        return list(null);
    }

    /**
     * Lists all models in the caller's organization.
     * <p>Results are ordered by <code>updated_at</code> descending, with <code>id</code> used as a tiebreaker.
     * If more results are available, the response includes <code>pagination.next_page_token</code>.
     * Pass that token back unchanged to continue from the last item you received.</p>
     * <p>The token is opaque. Do not construct or edit it yourself.</p>
     * <p>The <code>limit</code> is capped at 50. Values above that cap are reduced to 50, and
     * non-positive values fall back to the same default.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelListResponseEnvelope>> list(RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/models");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<ModelListResponseEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, ModelListResponseEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 404) {
                            future.completeExceptionally(new NotFoundError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Creates a new model.
     * <p>A model defines a query or view over a connection's data — for example, a SQL
     * query, a filtered object, or a joined dataset. Models are used as sources when
     * creating model syncs.</p>
     * <p>The connection referenced by <code>connection_id</code> must have source capabilities. Use
     * <a href="../../api-reference/connections/get-connection-type-schema"><code>GET /api/connection_types/{id}</code></a> to check
     * whether a connection type supports use as a source.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> create(CreateModelRequest body) {
        return create(ModelsCreateRequest.builder().body(body).build());
    }

    /**
     * Creates a new model.
     * <p>A model defines a query or view over a connection's data — for example, a SQL
     * query, a filtered object, or a joined dataset. Models are used as sources when
     * creating model syncs.</p>
     * <p>The connection referenced by <code>connection_id</code> must have source capabilities. Use
     * <a href="../../api-reference/connections/get-connection-type-schema"><code>GET /api/connection_types/{id}</code></a> to check
     * whether a connection type supports use as a source.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> create(
            CreateModelRequest body, IdempotentRequestOptions requestOptions) {
        return create(ModelsCreateRequest.builder().body(body).build(), requestOptions);
    }

    /**
     * Creates a new model.
     * <p>A model defines a query or view over a connection's data — for example, a SQL
     * query, a filtered object, or a joined dataset. Models are used as sources when
     * creating model syncs.</p>
     * <p>The connection referenced by <code>connection_id</code> must have source capabilities. Use
     * <a href="../../api-reference/connections/get-connection-type-schema"><code>GET /api/connection_types/{id}</code></a> to check
     * whether a connection type supports use as a source.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> create(ModelsCreateRequest request) {
        return create(request, null);
    }

    /**
     * Creates a new model.
     * <p>A model defines a query or view over a connection's data — for example, a SQL
     * query, a filtered object, or a joined dataset. Models are used as sources when
     * creating model syncs.</p>
     * <p>The connection referenced by <code>connection_id</code> must have source capabilities. Use
     * <a href="../../api-reference/connections/get-connection-type-schema"><code>GET /api/connection_types/{id}</code></a> to check
     * whether a connection type supports use as a source.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> create(
            ModelsCreateRequest request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/models");
        if (request.getAsync().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "async", request.getAsync().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ModelResponseEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Returns a single model by ID, including its source fields, identity, and filters.
     * <p>The response includes the model's source fields, identity column, and any
     * configured filters. To preview the data a model would return without saving
     * changes, use <a href="../../../api-reference/models/sample"><code>GET /api/models/{id}/sample</code></a>.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> get(String id) {
        return get(id, ModelsGetRequest.builder().build());
    }

    /**
     * Returns a single model by ID, including its source fields, identity, and filters.
     * <p>The response includes the model's source fields, identity column, and any
     * configured filters. To preview the data a model would return without saving
     * changes, use <a href="../../../api-reference/models/sample"><code>GET /api/models/{id}/sample</code></a>.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> get(
            String id, RequestOptions requestOptions) {
        return get(id, ModelsGetRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a single model by ID, including its source fields, identity, and filters.
     * <p>The response includes the model's source fields, identity column, and any
     * configured filters. To preview the data a model would return without saving
     * changes, use <a href="../../../api-reference/models/sample"><code>GET /api/models/{id}/sample</code></a>.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> get(String id, ModelsGetRequest request) {
        return get(id, request, null);
    }

    /**
     * Returns a single model by ID, including its source fields, identity, and filters.
     * <p>The response includes the model's source fields, identity column, and any
     * configured filters. To preview the data a model would return without saving
     * changes, use <a href="../../../api-reference/models/sample"><code>GET /api/models/{id}/sample</code></a>.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> get(
            String id, ModelsGetRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/models")
                .addPathSegment(id);
        if (request.getAsync().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "async", request.getAsync().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ModelResponseEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Updates a model's configuration.
     * <p>Updating a model is a <strong>full replacement</strong> of its configuration. Every field in
     * the request body is written to the model; any field you omit is cleared or reset
     * to its default value.</p>
     * <p>To make a partial change, fetch the current model with
     * <a href="../../../api-reference/models/get"><code>GET /api/models/{id}</code></a>, modify the fields you want to change, and send
     * the complete object back in the update request.</p>
     * <p>Changes to source fields, filters, or the identity column take effect on the
     * next sync execution that uses this model.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> update(
            String id, UpdateModelRequest request) {
        return update(id, request, null);
    }

    /**
     * Updates a model's configuration.
     * <p>Updating a model is a <strong>full replacement</strong> of its configuration. Every field in
     * the request body is written to the model; any field you omit is cleared or reset
     * to its default value.</p>
     * <p>To make a partial change, fetch the current model with
     * <a href="../../../api-reference/models/get"><code>GET /api/models/{id}</code></a>, modify the fields you want to change, and send
     * the complete object back in the update request.</p>
     * <p>Changes to source fields, filters, or the identity column take effect on the
     * next sync execution that uses this model.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> update(
            String id, UpdateModelRequest request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/models")
                .addPathSegment(id);
        if (request.getAsync().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "async", request.getAsync().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<ModelResponseEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ModelResponseEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Deletes a model.
     * <blockquote>
     * <p>🚧 Deleting a model used by one or more syncs will break those syncs. Remove
     * or reconfigure any syncs that reference this model before deleting it.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<Void>> remove(String id) {
        return remove(id, ModelsRemoveRequest.builder().build());
    }

    /**
     * Deletes a model.
     * <blockquote>
     * <p>🚧 Deleting a model used by one or more syncs will break those syncs. Remove
     * or reconfigure any syncs that reference this model before deleting it.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<Void>> remove(String id, IdempotentRequestOptions requestOptions) {
        return remove(id, ModelsRemoveRequest.builder().build(), requestOptions);
    }

    /**
     * Deletes a model.
     * <blockquote>
     * <p>🚧 Deleting a model used by one or more syncs will break those syncs. Remove
     * or reconfigure any syncs that reference this model before deleting it.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<Void>> remove(String id, ModelsRemoveRequest request) {
        return remove(id, request, null);
    }

    /**
     * Deletes a model.
     * <blockquote>
     * <p>🚧 Deleting a model used by one or more syncs will break those syncs. Remove
     * or reconfigure any syncs that reference this model before deleting it.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<Void>> remove(
            String id, ModelsRemoveRequest request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/models")
                .addPathSegment(id);
        if (request.getAsync().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "async", request.getAsync().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<Void>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(null, response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Returns a sample of records from a model.
     * <p>Synchronous requests must complete within 10 seconds. If the source query or
     * enrichment step can exceed that budget, use the asynchronous option so the
     * work runs as a background job.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelSampleResponseEnvelope>> sample(String id) {
        return sample(id, ModelsSampleRequest.builder().build());
    }

    /**
     * Returns a sample of records from a model.
     * <p>Synchronous requests must complete within 10 seconds. If the source query or
     * enrichment step can exceed that budget, use the asynchronous option so the
     * work runs as a background job.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelSampleResponseEnvelope>> sample(
            String id, RequestOptions requestOptions) {
        return sample(id, ModelsSampleRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a sample of records from a model.
     * <p>Synchronous requests must complete within 10 seconds. If the source query or
     * enrichment step can exceed that budget, use the asynchronous option so the
     * work runs as a background job.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelSampleResponseEnvelope>> sample(
            String id, ModelsSampleRequest request) {
        return sample(id, request, null);
    }

    /**
     * Returns a sample of records from a model.
     * <p>Synchronous requests must complete within 10 seconds. If the source query or
     * enrichment step can exceed that budget, use the asynchronous option so the
     * work runs as a background job.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ModelSampleResponseEnvelope>> sample(
            String id, ModelsSampleRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/models")
                .addPathSegment(id)
                .addPathSegments("sample");
        if (request.getAsync().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "async", request.getAsync().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<ModelSampleResponseEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, ModelSampleResponseEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
