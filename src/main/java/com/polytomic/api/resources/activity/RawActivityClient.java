/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.polytomic.api.resources.activity;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.polytomic.api.core.ClientOptions;
import com.polytomic.api.core.ObjectMappers;
import com.polytomic.api.core.PolytomicApiException;
import com.polytomic.api.core.PolytomicException;
import com.polytomic.api.core.PolytomicHttpResponse;
import com.polytomic.api.core.QueryStringMapper;
import com.polytomic.api.core.RequestOptions;
import com.polytomic.api.core.RetryInterceptor;
import com.polytomic.api.errors.BadRequestError;
import com.polytomic.api.errors.ForbiddenError;
import com.polytomic.api.errors.GoneError;
import com.polytomic.api.errors.NotFoundError;
import com.polytomic.api.errors.UnauthorizedError;
import com.polytomic.api.errors.UnprocessableEntityError;
import com.polytomic.api.resources.activity.requests.ActivityGetSessionRequest;
import com.polytomic.api.resources.activity.requests.ActivityListRequest;
import com.polytomic.api.types.ApiError;
import com.polytomic.api.types.HarborActivityEventEnvelope;
import com.polytomic.api.types.HarborActivityListEnvelope;
import com.polytomic.api.types.HarborActivitySessionEnvelope;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawActivityClient {
    protected final ClientOptions clientOptions;

    public RawActivityClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Lists Harbor Activity recorded for the caller's organization.
     * <p>You must be an active Organization administrator to use this endpoint.</p>
     * <p><code>start_time</code> is inclusive and <code>end_time</code> is exclusive. Both filter the time at
     * which Polytomic recorded each event, which can differ from <code>occurred_at</code>. A
     * single request can cover at most 90 days.</p>
     * <p>Results are ordered from newest to oldest by default. Set <code>order</code> to <code>asc</code> to
     * retrieve the oldest events first. A page contains at most 100 events. Pass
     * <code>next_page_token</code> from the response as <code>page_token</code> with the same time range,
     * filters, order, and limit to retrieve the next page. The token expires 24 hours
     * after the first page. Pagination uses a live descending cursor, so an event from
     * a transaction that commits between pages can require a fresh traversal.</p>
     * <p>Responses include stable identities, bounded resource snapshots, schema label
     * snapshots with truncation state, field counts, and row counts when available.
     * They do not include internal recorded-order values, submitted SQL, raw field
     * names, detailed provider errors, result rows, or raw pagination tokens.</p>
     * <blockquote>
     * <p>ℹ️ Activity retention</p>
     * <p>Polytomic retains Activity events for at least 90 days. Pagination tokens
     * expire 24 hours after the first page.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<HarborActivityListEnvelope> list(ActivityListRequest request) {
        return list(request, null);
    }

    /**
     * Lists Harbor Activity recorded for the caller's organization.
     * <p>You must be an active Organization administrator to use this endpoint.</p>
     * <p><code>start_time</code> is inclusive and <code>end_time</code> is exclusive. Both filter the time at
     * which Polytomic recorded each event, which can differ from <code>occurred_at</code>. A
     * single request can cover at most 90 days.</p>
     * <p>Results are ordered from newest to oldest by default. Set <code>order</code> to <code>asc</code> to
     * retrieve the oldest events first. A page contains at most 100 events. Pass
     * <code>next_page_token</code> from the response as <code>page_token</code> with the same time range,
     * filters, order, and limit to retrieve the next page. The token expires 24 hours
     * after the first page. Pagination uses a live descending cursor, so an event from
     * a transaction that commits between pages can require a fresh traversal.</p>
     * <p>Responses include stable identities, bounded resource snapshots, schema label
     * snapshots with truncation state, field counts, and row counts when available.
     * They do not include internal recorded-order values, submitted SQL, raw field
     * names, detailed provider errors, result rows, or raw pagination tokens.</p>
     * <blockquote>
     * <p>ℹ️ Activity retention</p>
     * <p>Polytomic retains Activity events for at least 90 days. Pagination tokens
     * expire 24 hours after the first page.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<HarborActivityListEnvelope> list(
            ActivityListRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/organization/harbor-activity");
        QueryStringMapper.addQueryParameter(httpUrl, "start_time", request.getStartTime(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "end_time", request.getEndTime(), false);
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getPageToken().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_token", request.getPageToken().get(), false);
        }
        if (request.getOrder().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "order", request.getOrder().get(), false);
        }
        if (request.getHarborId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "harbor_id", request.getHarborId().get(), true);
        }
        if (request.getEventType().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "event_type", request.getEventType().get(), true);
        }
        if (request.getOutcome().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "outcome", request.getOutcome().get(), true);
        }
        if (request.getOperationId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "operation_id", request.getOperationId().get(), true);
        }
        if (request.getSessionId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "session_id", request.getSessionId().get(), true);
        }
        if (request.getActorId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "actor_id", request.getActorId().get(), true);
        }
        if (request.getCredentialId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "credential_id", request.getCredentialId().get(), true);
        }
        if (request.getConnectionId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "connection_id", request.getConnectionId().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborActivityListEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 410:
                        throw new GoneError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns one Harbor Activity session and its events.
     * <p>You must be an active Organization administrator to use this endpoint. The
     * session must belong to the Organization against which you authenticated.</p>
     * <p>Events are ordered from oldest to newest by default. Set <code>order</code> to <code>desc</code> to
     * retrieve the newest events first. A page contains at most 100 events. Pass
     * <code>next_page_token</code> from the response as <code>page_token</code> with the same order and
     * limit to retrieve the next page.</p>
     * <p><code>history_truncated</code> is <code>true</code> when Activity retention removed events from the
     * beginning of the session. The lifecycle summary remains available while the
     * session itself is retained.</p>
     * <p>The response uses the same event allowlist as the Activity list and detail
     * endpoints. It does not include session tokens, submitted SQL, raw field names,
     * detailed provider errors, result rows, or raw pagination tokens.</p>
     * <blockquote>
     * <p>ℹ️ Activity retention</p>
     * <p>Polytomic retains Activity events for at least 90 days. Pagination tokens
     * expire 24 hours after the first page.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<HarborActivitySessionEnvelope> getSession(String sessionId) {
        return getSession(sessionId, ActivityGetSessionRequest.builder().build());
    }

    /**
     * Returns one Harbor Activity session and its events.
     * <p>You must be an active Organization administrator to use this endpoint. The
     * session must belong to the Organization against which you authenticated.</p>
     * <p>Events are ordered from oldest to newest by default. Set <code>order</code> to <code>desc</code> to
     * retrieve the newest events first. A page contains at most 100 events. Pass
     * <code>next_page_token</code> from the response as <code>page_token</code> with the same order and
     * limit to retrieve the next page.</p>
     * <p><code>history_truncated</code> is <code>true</code> when Activity retention removed events from the
     * beginning of the session. The lifecycle summary remains available while the
     * session itself is retained.</p>
     * <p>The response uses the same event allowlist as the Activity list and detail
     * endpoints. It does not include session tokens, submitted SQL, raw field names,
     * detailed provider errors, result rows, or raw pagination tokens.</p>
     * <blockquote>
     * <p>ℹ️ Activity retention</p>
     * <p>Polytomic retains Activity events for at least 90 days. Pagination tokens
     * expire 24 hours after the first page.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<HarborActivitySessionEnvelope> getSession(
            String sessionId, RequestOptions requestOptions) {
        return getSession(sessionId, ActivityGetSessionRequest.builder().build(), requestOptions);
    }

    /**
     * Returns one Harbor Activity session and its events.
     * <p>You must be an active Organization administrator to use this endpoint. The
     * session must belong to the Organization against which you authenticated.</p>
     * <p>Events are ordered from oldest to newest by default. Set <code>order</code> to <code>desc</code> to
     * retrieve the newest events first. A page contains at most 100 events. Pass
     * <code>next_page_token</code> from the response as <code>page_token</code> with the same order and
     * limit to retrieve the next page.</p>
     * <p><code>history_truncated</code> is <code>true</code> when Activity retention removed events from the
     * beginning of the session. The lifecycle summary remains available while the
     * session itself is retained.</p>
     * <p>The response uses the same event allowlist as the Activity list and detail
     * endpoints. It does not include session tokens, submitted SQL, raw field names,
     * detailed provider errors, result rows, or raw pagination tokens.</p>
     * <blockquote>
     * <p>ℹ️ Activity retention</p>
     * <p>Polytomic retains Activity events for at least 90 days. Pagination tokens
     * expire 24 hours after the first page.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<HarborActivitySessionEnvelope> getSession(
            String sessionId, ActivityGetSessionRequest request) {
        return getSession(sessionId, request, null);
    }

    /**
     * Returns one Harbor Activity session and its events.
     * <p>You must be an active Organization administrator to use this endpoint. The
     * session must belong to the Organization against which you authenticated.</p>
     * <p>Events are ordered from oldest to newest by default. Set <code>order</code> to <code>desc</code> to
     * retrieve the newest events first. A page contains at most 100 events. Pass
     * <code>next_page_token</code> from the response as <code>page_token</code> with the same order and
     * limit to retrieve the next page.</p>
     * <p><code>history_truncated</code> is <code>true</code> when Activity retention removed events from the
     * beginning of the session. The lifecycle summary remains available while the
     * session itself is retained.</p>
     * <p>The response uses the same event allowlist as the Activity list and detail
     * endpoints. It does not include session tokens, submitted SQL, raw field names,
     * detailed provider errors, result rows, or raw pagination tokens.</p>
     * <blockquote>
     * <p>ℹ️ Activity retention</p>
     * <p>Polytomic retains Activity events for at least 90 days. Pagination tokens
     * expire 24 hours after the first page.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<HarborActivitySessionEnvelope> getSession(
            String sessionId, ActivityGetSessionRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/organization/harbor-activity/sessions")
                .addPathSegment(sessionId);
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getPageToken().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_token", request.getPageToken().get(), false);
        }
        if (request.getOrder().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "order", request.getOrder().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborActivitySessionEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 410:
                        throw new GoneError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns one Harbor Activity event from the caller's organization.
     * <p>You must be an active Organization administrator to use this endpoint. The
     * event must belong to the Organization against which you authenticated.</p>
     * <p>The response can include stable identities, bounded resource snapshots, schema
     * label snapshots with truncation state, field counts, and row counts. It does
     * not include internal recorded-order values, submitted SQL, raw field names,
     * detailed provider errors, result rows, or raw pagination tokens.</p>
     * <blockquote>
     * <p>ℹ️ Activity retention</p>
     * <p>Polytomic retains Activity events for at least 90 days.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<HarborActivityEventEnvelope> get(String eventId) {
        return get(eventId, null);
    }

    /**
     * Returns one Harbor Activity event from the caller's organization.
     * <p>You must be an active Organization administrator to use this endpoint. The
     * event must belong to the Organization against which you authenticated.</p>
     * <p>The response can include stable identities, bounded resource snapshots, schema
     * label snapshots with truncation state, field counts, and row counts. It does
     * not include internal recorded-order values, submitted SQL, raw field names,
     * detailed provider errors, result rows, or raw pagination tokens.</p>
     * <blockquote>
     * <p>ℹ️ Activity retention</p>
     * <p>Polytomic retains Activity events for at least 90 days.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<HarborActivityEventEnvelope> get(String eventId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/organization/harbor-activity")
                .addPathSegment(eventId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HarborActivityEventEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }
}
