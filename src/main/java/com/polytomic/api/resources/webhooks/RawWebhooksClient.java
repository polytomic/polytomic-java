/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.polytomic.api.resources.webhooks;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.polytomic.api.core.ClientOptions;
import com.polytomic.api.core.IdempotentRequestOptions;
import com.polytomic.api.core.MediaTypes;
import com.polytomic.api.core.ObjectMappers;
import com.polytomic.api.core.PolytomicApiException;
import com.polytomic.api.core.PolytomicException;
import com.polytomic.api.core.PolytomicHttpResponse;
import com.polytomic.api.core.RequestOptions;
import com.polytomic.api.errors.InternalServerError;
import com.polytomic.api.errors.NotFoundError;
import com.polytomic.api.errors.UnauthorizedError;
import com.polytomic.api.errors.UnprocessableEntityError;
import com.polytomic.api.resources.webhooks.requests.CreateWebhooksSchema;
import com.polytomic.api.resources.webhooks.requests.UpdateWebhooksSchema;
import com.polytomic.api.types.ApiError;
import com.polytomic.api.types.WebhookEnvelope;
import com.polytomic.api.types.WebhookListEnvelope;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawWebhooksClient {
    protected final ClientOptions clientOptions;

    public RawWebhooksClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Lists the webhooks for the caller's organization.
     * <blockquote>
     * <p>📘 One webhook per organization</p>
     * <p>An organization can register a single webhook, which receives every event
     * produced in that organization. See the
     * <a href="../../guides/events">Events documentation</a> for the
     * list of event types and payload shapes.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<WebhookListEnvelope> list() {
        return list(null);
    }

    /**
     * Lists the webhooks for the caller's organization.
     * <blockquote>
     * <p>📘 One webhook per organization</p>
     * <p>An organization can register a single webhook, which receives every event
     * produced in that organization. See the
     * <a href="../../guides/events">Events documentation</a> for the
     * list of event types and payload shapes.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<WebhookListEnvelope> list(RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/webhooks");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, WebhookListEnvelope.class), response);
            }
            try {
                if (response.code() == 500) {
                    throw new InternalServerError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Creates the organization's webhook.
     * <blockquote>
     * <p>📘 One webhook per organization</p>
     * <p>An organization can register a single webhook, which receives every event
     * produced in that organization. See the
     * <a href="../../guides/events">Events documentation</a> for the
     * list of event types and payload shapes.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<WebhookEnvelope> create(CreateWebhooksSchema request) {
        return create(request, null);
    }

    /**
     * Creates the organization's webhook.
     * <blockquote>
     * <p>📘 One webhook per organization</p>
     * <p>An organization can register a single webhook, which receives every event
     * produced in that organization. See the
     * <a href="../../guides/events">Events documentation</a> for the
     * list of event types and payload shapes.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<WebhookEnvelope> create(
            CreateWebhooksSchema request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/webhooks");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, WebhookEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns a single webhook by ID.
     * <blockquote>
     * <p>📘 One webhook per organization</p>
     * <p>An organization can register a single webhook, which receives every event
     * produced in that organization. See the
     * <a href="../../../guides/events">Events documentation</a> for the
     * list of event types and payload shapes.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<WebhookEnvelope> get(String id) {
        return get(id, null);
    }

    /**
     * Returns a single webhook by ID.
     * <blockquote>
     * <p>📘 One webhook per organization</p>
     * <p>An organization can register a single webhook, which receives every event
     * produced in that organization. See the
     * <a href="../../../guides/events">Events documentation</a> for the
     * list of event types and payload shapes.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<WebhookEnvelope> get(String id, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/webhooks")
                .addPathSegment(id);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, WebhookEnvelope.class), response);
            }
            try {
                if (response.code() == 404) {
                    throw new NotFoundError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Updates an existing webhook.
     * <blockquote>
     * <p>📘 One webhook per organization</p>
     * <p>An organization can register a single webhook, which receives every event
     * produced in that organization. See the
     * <a href="../../../guides/events">Events documentation</a> for the
     * list of event types and payload shapes.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<WebhookEnvelope> update(String id, UpdateWebhooksSchema request) {
        return update(id, request, null);
    }

    /**
     * Updates an existing webhook.
     * <blockquote>
     * <p>📘 One webhook per organization</p>
     * <p>An organization can register a single webhook, which receives every event
     * produced in that organization. See the
     * <a href="../../../guides/events">Events documentation</a> for the
     * list of event types and payload shapes.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<WebhookEnvelope> update(
            String id, UpdateWebhooksSchema request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/webhooks")
                .addPathSegment(id);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, WebhookEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Deletes a webhook.
     * <blockquote>
     * <p>📘 One webhook per organization</p>
     * <p>An organization can register a single webhook, which receives every event
     * produced in that organization. See the
     * <a href="../../../guides/events">Events documentation</a> for the
     * list of event types and payload shapes.</p>
     * </blockquote>
     * <p>Deletion is permanent. To stop delivery without losing the webhook
     * configuration, use
     * <a href="../../../api-reference/webhooks/disable"><code>POST /api/webhooks/{id}/disable</code></a> instead.</p>
     */
    public PolytomicHttpResponse<Void> delete(String id) {
        return delete(id, null);
    }

    /**
     * Deletes a webhook.
     * <blockquote>
     * <p>📘 One webhook per organization</p>
     * <p>An organization can register a single webhook, which receives every event
     * produced in that organization. See the
     * <a href="../../../guides/events">Events documentation</a> for the
     * list of event types and payload shapes.</p>
     * </blockquote>
     * <p>Deletion is permanent. To stop delivery without losing the webhook
     * configuration, use
     * <a href="../../../api-reference/webhooks/disable"><code>POST /api/webhooks/{id}/disable</code></a> instead.</p>
     */
    public PolytomicHttpResponse<Void> delete(String id, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/webhooks")
                .addPathSegment(id);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                switch (response.code()) {
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Disables a webhook without deleting it.
     * <blockquote>
     * <p>📘 One webhook per organization</p>
     * <p>An organization can register a single webhook, which receives every event
     * produced in that organization. See the
     * <a href="../../../../guides/events">Events documentation</a> for the
     * list of event types and payload shapes.</p>
     * </blockquote>
     * <p>Events are not queued while the webhook is disabled — any activity that occurs
     * during the disabled period is not delivered retroactively. To resume
     * delivery, re-enable the webhook using
     * <a href="../../../../api-reference/webhooks/enable"><code>POST /api/webhooks/{id}/enable</code></a>.</p>
     */
    public PolytomicHttpResponse<WebhookEnvelope> disable(String id) {
        return disable(id, null);
    }

    /**
     * Disables a webhook without deleting it.
     * <blockquote>
     * <p>📘 One webhook per organization</p>
     * <p>An organization can register a single webhook, which receives every event
     * produced in that organization. See the
     * <a href="../../../../guides/events">Events documentation</a> for the
     * list of event types and payload shapes.</p>
     * </blockquote>
     * <p>Events are not queued while the webhook is disabled — any activity that occurs
     * during the disabled period is not delivered retroactively. To resume
     * delivery, re-enable the webhook using
     * <a href="../../../../api-reference/webhooks/enable"><code>POST /api/webhooks/{id}/enable</code></a>.</p>
     */
    public PolytomicHttpResponse<WebhookEnvelope> disable(String id, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/webhooks")
                .addPathSegment(id)
                .addPathSegments("disable");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, WebhookEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Re-enables a previously disabled webhook.
     * <blockquote>
     * <p>📘 One webhook per organization</p>
     * <p>An organization can register a single webhook, which receives every event
     * produced in that organization. See the
     * <a href="../../../../guides/events">Events documentation</a> for the
     * list of event types and payload shapes.</p>
     * </blockquote>
     * <p>Delivery resumes from the next event generated after this call. Events that
     * occurred while the webhook was disabled are not replayed.</p>
     */
    public PolytomicHttpResponse<WebhookEnvelope> enable(String id) {
        return enable(id, null);
    }

    /**
     * Re-enables a previously disabled webhook.
     * <blockquote>
     * <p>📘 One webhook per organization</p>
     * <p>An organization can register a single webhook, which receives every event
     * produced in that organization. See the
     * <a href="../../../../guides/events">Events documentation</a> for the
     * list of event types and payload shapes.</p>
     * </blockquote>
     * <p>Delivery resumes from the next event generated after this call. Events that
     * occurred while the webhook was disabled are not replayed.</p>
     */
    public PolytomicHttpResponse<WebhookEnvelope> enable(String id, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/webhooks")
                .addPathSegment(id)
                .addPathSegments("enable");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, WebhookEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }
}
