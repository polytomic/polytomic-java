/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.polytomic.api.resources.bulksync.schemas;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.polytomic.api.core.ClientOptions;
import com.polytomic.api.core.IdempotentRequestOptions;
import com.polytomic.api.core.MediaTypes;
import com.polytomic.api.core.ObjectMappers;
import com.polytomic.api.core.PolytomicApiException;
import com.polytomic.api.core.PolytomicException;
import com.polytomic.api.core.PolytomicHttpResponse;
import com.polytomic.api.core.QueryStringMapper;
import com.polytomic.api.core.RequestOptions;
import com.polytomic.api.errors.BadRequestError;
import com.polytomic.api.errors.ForbiddenError;
import com.polytomic.api.errors.InternalServerError;
import com.polytomic.api.errors.NotFoundError;
import com.polytomic.api.resources.bulksync.schemas.requests.SchemasListRequest;
import com.polytomic.api.resources.bulksync.schemas.requests.UpdateBulkSchema;
import com.polytomic.api.resources.bulksync.schemas.requests.UpdateBulkSyncSchemasRequest;
import com.polytomic.api.types.ApiError;
import com.polytomic.api.types.BulkSchemaEnvelope;
import com.polytomic.api.types.CancelBulkSyncResponseEnvelope;
import com.polytomic.api.types.ListBulkSchemaEnvelope;
import com.polytomic.api.types.UpdateBulkSyncSchemasEnvelope;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawSchemasClient {
    protected final ClientOptions clientOptions;

    public AsyncRawSchemasClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Lists the schemas (tables, objects) configured for a bulk sync.
     * <p>This endpoint returns the schemas that have been added to and configured on this
     * specific bulk sync — not the full set of schemas available from the source
     * connection. To discover what the source connection exposes, use the source
     * schemas endpoint for the relevant connection type.</p>
     * <p>Each schema in the response includes its sync mode, field selections, and any
     * custom configuration applied via
     * <a href="../../../../../api-reference/bulk-sync/schemas/patch"><code>PATCH /api/bulk/syncs/{id}/schemas</code></a>
     * or
     * <a href="../../../../../api-reference/bulk-sync/schemas/update"><code>PUT /api/bulk/syncs/{id}/schemas/{schema_id}</code></a>.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ListBulkSchemaEnvelope>> list(String id) {
        return list(id, SchemasListRequest.builder().build());
    }

    /**
     * Lists the schemas (tables, objects) configured for a bulk sync.
     * <p>This endpoint returns the schemas that have been added to and configured on this
     * specific bulk sync — not the full set of schemas available from the source
     * connection. To discover what the source connection exposes, use the source
     * schemas endpoint for the relevant connection type.</p>
     * <p>Each schema in the response includes its sync mode, field selections, and any
     * custom configuration applied via
     * <a href="../../../../../api-reference/bulk-sync/schemas/patch"><code>PATCH /api/bulk/syncs/{id}/schemas</code></a>
     * or
     * <a href="../../../../../api-reference/bulk-sync/schemas/update"><code>PUT /api/bulk/syncs/{id}/schemas/{schema_id}</code></a>.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ListBulkSchemaEnvelope>> list(
            String id, RequestOptions requestOptions) {
        return list(id, SchemasListRequest.builder().build(), requestOptions);
    }

    /**
     * Lists the schemas (tables, objects) configured for a bulk sync.
     * <p>This endpoint returns the schemas that have been added to and configured on this
     * specific bulk sync — not the full set of schemas available from the source
     * connection. To discover what the source connection exposes, use the source
     * schemas endpoint for the relevant connection type.</p>
     * <p>Each schema in the response includes its sync mode, field selections, and any
     * custom configuration applied via
     * <a href="../../../../../api-reference/bulk-sync/schemas/patch"><code>PATCH /api/bulk/syncs/{id}/schemas</code></a>
     * or
     * <a href="../../../../../api-reference/bulk-sync/schemas/update"><code>PUT /api/bulk/syncs/{id}/schemas/{schema_id}</code></a>.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ListBulkSchemaEnvelope>> list(
            String id, SchemasListRequest request) {
        return list(id, request, null);
    }

    /**
     * Lists the schemas (tables, objects) configured for a bulk sync.
     * <p>This endpoint returns the schemas that have been added to and configured on this
     * specific bulk sync — not the full set of schemas available from the source
     * connection. To discover what the source connection exposes, use the source
     * schemas endpoint for the relevant connection type.</p>
     * <p>Each schema in the response includes its sync mode, field selections, and any
     * custom configuration applied via
     * <a href="../../../../../api-reference/bulk-sync/schemas/patch"><code>PATCH /api/bulk/syncs/{id}/schemas</code></a>
     * or
     * <a href="../../../../../api-reference/bulk-sync/schemas/update"><code>PUT /api/bulk/syncs/{id}/schemas/{schema_id}</code></a>.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ListBulkSchemaEnvelope>> list(
            String id, SchemasListRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/bulk/syncs")
                .addPathSegment(id)
                .addPathSegments("schemas");
        if (request.getFilters().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "filters", request.getFilters().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<ListBulkSchemaEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListBulkSchemaEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 404) {
                            future.completeExceptionally(new NotFoundError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Patches one or more schemas on a bulk sync at once.
     * <p>Only schemas explicitly included in the request body are modified; schemas
     * omitted from the request are left unchanged. This makes PATCH the right choice
     * when you want to update a subset of tables without affecting the rest of the
     * sync's schema configuration.</p>
     * <p>Within each provided schema, omitting <code>fields</code> enables all available fields on
     * that schema. To control which fields are enabled, include the <code>fields</code> array
     * with explicit <code>enabled</code> values for each field.</p>
     * <blockquote>
     * <p>📘 To replace a single schema's configuration in full (clearing any fields you
     * omit), use
     * <a href="../../../../../api-reference/bulk-sync/schemas/update"><code>PUT /api/bulk/syncs/{id}/schemas/{schema_id}</code></a>
     * instead.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<UpdateBulkSyncSchemasEnvelope>> patch(String id) {
        return patch(id, UpdateBulkSyncSchemasRequest.builder().build());
    }

    /**
     * Patches one or more schemas on a bulk sync at once.
     * <p>Only schemas explicitly included in the request body are modified; schemas
     * omitted from the request are left unchanged. This makes PATCH the right choice
     * when you want to update a subset of tables without affecting the rest of the
     * sync's schema configuration.</p>
     * <p>Within each provided schema, omitting <code>fields</code> enables all available fields on
     * that schema. To control which fields are enabled, include the <code>fields</code> array
     * with explicit <code>enabled</code> values for each field.</p>
     * <blockquote>
     * <p>📘 To replace a single schema's configuration in full (clearing any fields you
     * omit), use
     * <a href="../../../../../api-reference/bulk-sync/schemas/update"><code>PUT /api/bulk/syncs/{id}/schemas/{schema_id}</code></a>
     * instead.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<UpdateBulkSyncSchemasEnvelope>> patch(
            String id, IdempotentRequestOptions requestOptions) {
        return patch(id, UpdateBulkSyncSchemasRequest.builder().build(), requestOptions);
    }

    /**
     * Patches one or more schemas on a bulk sync at once.
     * <p>Only schemas explicitly included in the request body are modified; schemas
     * omitted from the request are left unchanged. This makes PATCH the right choice
     * when you want to update a subset of tables without affecting the rest of the
     * sync's schema configuration.</p>
     * <p>Within each provided schema, omitting <code>fields</code> enables all available fields on
     * that schema. To control which fields are enabled, include the <code>fields</code> array
     * with explicit <code>enabled</code> values for each field.</p>
     * <blockquote>
     * <p>📘 To replace a single schema's configuration in full (clearing any fields you
     * omit), use
     * <a href="../../../../../api-reference/bulk-sync/schemas/update"><code>PUT /api/bulk/syncs/{id}/schemas/{schema_id}</code></a>
     * instead.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<UpdateBulkSyncSchemasEnvelope>> patch(
            String id, UpdateBulkSyncSchemasRequest request) {
        return patch(id, request, null);
    }

    /**
     * Patches one or more schemas on a bulk sync at once.
     * <p>Only schemas explicitly included in the request body are modified; schemas
     * omitted from the request are left unchanged. This makes PATCH the right choice
     * when you want to update a subset of tables without affecting the rest of the
     * sync's schema configuration.</p>
     * <p>Within each provided schema, omitting <code>fields</code> enables all available fields on
     * that schema. To control which fields are enabled, include the <code>fields</code> array
     * with explicit <code>enabled</code> values for each field.</p>
     * <blockquote>
     * <p>📘 To replace a single schema's configuration in full (clearing any fields you
     * omit), use
     * <a href="../../../../../api-reference/bulk-sync/schemas/update"><code>PUT /api/bulk/syncs/{id}/schemas/{schema_id}</code></a>
     * instead.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<UpdateBulkSyncSchemasEnvelope>> patch(
            String id, UpdateBulkSyncSchemasRequest request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/bulk/syncs")
                .addPathSegment(id)
                .addPathSegments("schemas");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<UpdateBulkSyncSchemasEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, UpdateBulkSyncSchemasEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Returns the configuration of a single schema on a bulk sync.
     * <p>Returns the sync mode, field selections, and any other configuration applied to
     * this schema on the bulk sync.</p>
     * <p>To modify the configuration, use
     * <a href="../../../../../../api-reference/bulk-sync/schemas/patch"><code>PATCH /api/bulk/syncs/{id}/schemas</code></a>
     * for a partial update across multiple schemas, or
     * <a href="../../../../../../api-reference/bulk-sync/schemas/update"><code>PUT /api/bulk/syncs/{id}/schemas/{schema_id}</code></a>
     * to fully replace this schema's configuration.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<BulkSchemaEnvelope>> get(String id, String schemaId) {
        return get(id, schemaId, null);
    }

    /**
     * Returns the configuration of a single schema on a bulk sync.
     * <p>Returns the sync mode, field selections, and any other configuration applied to
     * this schema on the bulk sync.</p>
     * <p>To modify the configuration, use
     * <a href="../../../../../../api-reference/bulk-sync/schemas/patch"><code>PATCH /api/bulk/syncs/{id}/schemas</code></a>
     * for a partial update across multiple schemas, or
     * <a href="../../../../../../api-reference/bulk-sync/schemas/update"><code>PUT /api/bulk/syncs/{id}/schemas/{schema_id}</code></a>
     * to fully replace this schema's configuration.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<BulkSchemaEnvelope>> get(
            String id, String schemaId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/bulk/syncs")
                .addPathSegment(id)
                .addPathSegments("schemas")
                .addPathSegment(schemaId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<BulkSchemaEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BulkSchemaEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 404) {
                            future.completeExceptionally(new NotFoundError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Replaces the configuration of a single schema on a bulk sync.
     * <p>This is a full replacement: every field in the request body is written to the
     * schema, and any field you omit is cleared or reset to its default. Fetch the
     * current configuration with
     * <a href="../../../../../../api-reference/bulk-sync/schemas/get"><code>GET /api/bulk/syncs/{id}/schemas/{schema_id}</code></a>
     * first if you want to preserve existing settings while changing only a subset.</p>
     * <p>Omitting <code>fields</code> enables all available fields on the schema. To control which
     * fields are enabled, include the <code>fields</code> array with explicit <code>enabled</code> values.</p>
     * <blockquote>
     * <p>📘 To update multiple schemas in a single request without affecting others,
     * use the partial-update endpoint
     * <a href="../../../../../../api-reference/bulk-sync/schemas/patch"><code>PATCH /api/bulk/syncs/{id}/schemas</code></a>
     * instead.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<BulkSchemaEnvelope>> update(String id, String schemaId) {
        return update(id, schemaId, UpdateBulkSchema.builder().build());
    }

    /**
     * Replaces the configuration of a single schema on a bulk sync.
     * <p>This is a full replacement: every field in the request body is written to the
     * schema, and any field you omit is cleared or reset to its default. Fetch the
     * current configuration with
     * <a href="../../../../../../api-reference/bulk-sync/schemas/get"><code>GET /api/bulk/syncs/{id}/schemas/{schema_id}</code></a>
     * first if you want to preserve existing settings while changing only a subset.</p>
     * <p>Omitting <code>fields</code> enables all available fields on the schema. To control which
     * fields are enabled, include the <code>fields</code> array with explicit <code>enabled</code> values.</p>
     * <blockquote>
     * <p>📘 To update multiple schemas in a single request without affecting others,
     * use the partial-update endpoint
     * <a href="../../../../../../api-reference/bulk-sync/schemas/patch"><code>PATCH /api/bulk/syncs/{id}/schemas</code></a>
     * instead.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<BulkSchemaEnvelope>> update(
            String id, String schemaId, IdempotentRequestOptions requestOptions) {
        return update(id, schemaId, UpdateBulkSchema.builder().build(), requestOptions);
    }

    /**
     * Replaces the configuration of a single schema on a bulk sync.
     * <p>This is a full replacement: every field in the request body is written to the
     * schema, and any field you omit is cleared or reset to its default. Fetch the
     * current configuration with
     * <a href="../../../../../../api-reference/bulk-sync/schemas/get"><code>GET /api/bulk/syncs/{id}/schemas/{schema_id}</code></a>
     * first if you want to preserve existing settings while changing only a subset.</p>
     * <p>Omitting <code>fields</code> enables all available fields on the schema. To control which
     * fields are enabled, include the <code>fields</code> array with explicit <code>enabled</code> values.</p>
     * <blockquote>
     * <p>📘 To update multiple schemas in a single request without affecting others,
     * use the partial-update endpoint
     * <a href="../../../../../../api-reference/bulk-sync/schemas/patch"><code>PATCH /api/bulk/syncs/{id}/schemas</code></a>
     * instead.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<BulkSchemaEnvelope>> update(
            String id, String schemaId, UpdateBulkSchema request) {
        return update(id, schemaId, request, null);
    }

    /**
     * Replaces the configuration of a single schema on a bulk sync.
     * <p>This is a full replacement: every field in the request body is written to the
     * schema, and any field you omit is cleared or reset to its default. Fetch the
     * current configuration with
     * <a href="../../../../../../api-reference/bulk-sync/schemas/get"><code>GET /api/bulk/syncs/{id}/schemas/{schema_id}</code></a>
     * first if you want to preserve existing settings while changing only a subset.</p>
     * <p>Omitting <code>fields</code> enables all available fields on the schema. To control which
     * fields are enabled, include the <code>fields</code> array with explicit <code>enabled</code> values.</p>
     * <blockquote>
     * <p>📘 To update multiple schemas in a single request without affecting others,
     * use the partial-update endpoint
     * <a href="../../../../../../api-reference/bulk-sync/schemas/patch"><code>PATCH /api/bulk/syncs/{id}/schemas</code></a>
     * instead.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<BulkSchemaEnvelope>> update(
            String id, String schemaId, UpdateBulkSchema request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/bulk/syncs")
                .addPathSegment(id)
                .addPathSegments("schemas")
                .addPathSegment(schemaId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<BulkSchemaEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BulkSchemaEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Requests cancellation of any running executions for a specific schema on a bulk sync.
     * <p>Cancellation is asynchronous. A successful response means the cancellation
     * signal for this schema has been queued; the schema's in-flight work continues
     * until the signal is processed. Poll
     * <code>GET /api/bulk/syncs/{id}/schemas/{schema_id}</code> and the parent execution via
     * <code>GET /api/bulk/syncs/{id}/status</code> to confirm the schema has reached a terminal
     * state.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<CancelBulkSyncResponseEnvelope>> cancel(String id, String schemaId) {
        return cancel(id, schemaId, null);
    }

    /**
     * Requests cancellation of any running executions for a specific schema on a bulk sync.
     * <p>Cancellation is asynchronous. A successful response means the cancellation
     * signal for this schema has been queued; the schema's in-flight work continues
     * until the signal is processed. Poll
     * <code>GET /api/bulk/syncs/{id}/schemas/{schema_id}</code> and the parent execution via
     * <code>GET /api/bulk/syncs/{id}/status</code> to confirm the schema has reached a terminal
     * state.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<CancelBulkSyncResponseEnvelope>> cancel(
            String id, String schemaId, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/bulk/syncs")
                .addPathSegment(id)
                .addPathSegments("schemas")
                .addPathSegment(schemaId)
                .addPathSegments("cancel");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<CancelBulkSyncResponseEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, CancelBulkSyncResponseEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
