/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.polytomic.api.resources.bulksync.executions;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.polytomic.api.core.ClientOptions;
import com.polytomic.api.core.IdempotentRequestOptions;
import com.polytomic.api.core.ObjectMappers;
import com.polytomic.api.core.PolytomicApiException;
import com.polytomic.api.core.PolytomicException;
import com.polytomic.api.core.PolytomicHttpResponse;
import com.polytomic.api.core.QueryStringMapper;
import com.polytomic.api.core.RequestOptions;
import com.polytomic.api.errors.BadRequestError;
import com.polytomic.api.errors.ForbiddenError;
import com.polytomic.api.errors.InternalServerError;
import com.polytomic.api.errors.NotFoundError;
import com.polytomic.api.errors.RequestTimeoutError;
import com.polytomic.api.resources.bulksync.executions.requests.ExecutionsExportLogsRequest;
import com.polytomic.api.resources.bulksync.executions.requests.ExecutionsGetConsoleLogsRequest;
import com.polytomic.api.resources.bulksync.executions.requests.ExecutionsGetSchemaConsoleLogsRequest;
import com.polytomic.api.resources.bulksync.executions.requests.ExecutionsListRequest;
import com.polytomic.api.resources.bulksync.executions.requests.ExecutionsListStatusRequest;
import com.polytomic.api.types.ApiError;
import com.polytomic.api.types.BulkSyncExecutionEnvelope;
import com.polytomic.api.types.BulkSyncExecutionLogsEnvelope;
import com.polytomic.api.types.CancelBulkSyncResponseEnvelope;
import com.polytomic.api.types.ExecutionConsoleLogsResponseEnvelope;
import com.polytomic.api.types.ExportSyncLogsEnvelope;
import com.polytomic.api.types.ListBulkSyncExecutionsEnvelope;
import com.polytomic.api.types.ListBulkSyncExecutionsStatusEnvelope;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawExecutionsClient {
    protected final ClientOptions clientOptions;

    public AsyncRawExecutionsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns a concise per-schema status for one or more bulk syncs.
     * <p>This endpoint is a summary view, not an execution-history view. Each schema is
     * represented at most once with its most recent execution status, and running
     * executions are preferred over older terminal ones.</p>
     * <p>Use this endpoint when you want a dashboard-style answer to &quot;what is each sync
     * doing now?&quot; If you need the full execution history or a single execution's
     * details, use <a href="../../../../api-reference/bulk-sync/executions/list"><code>GET /api/bulk/syncs/{id}/executions</code></a> or
     * <a href="../../../../api-reference/bulk-sync/executions/get"><code>GET /api/bulk/syncs/{id}/executions/{exec_id}</code></a> instead.</p>
     * <p>Setting <code>all=true</code> or <code>active=true</code> ignores any explicit <code>sync_id</code> filters and
     * expands the request to the caller's organization scope.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ListBulkSyncExecutionsStatusEnvelope>> listStatus() {
        return listStatus(ExecutionsListStatusRequest.builder().build());
    }

    /**
     * Returns a concise per-schema status for one or more bulk syncs.
     * <p>This endpoint is a summary view, not an execution-history view. Each schema is
     * represented at most once with its most recent execution status, and running
     * executions are preferred over older terminal ones.</p>
     * <p>Use this endpoint when you want a dashboard-style answer to &quot;what is each sync
     * doing now?&quot; If you need the full execution history or a single execution's
     * details, use <a href="../../../../api-reference/bulk-sync/executions/list"><code>GET /api/bulk/syncs/{id}/executions</code></a> or
     * <a href="../../../../api-reference/bulk-sync/executions/get"><code>GET /api/bulk/syncs/{id}/executions/{exec_id}</code></a> instead.</p>
     * <p>Setting <code>all=true</code> or <code>active=true</code> ignores any explicit <code>sync_id</code> filters and
     * expands the request to the caller's organization scope.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ListBulkSyncExecutionsStatusEnvelope>> listStatus(
            RequestOptions requestOptions) {
        return listStatus(ExecutionsListStatusRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a concise per-schema status for one or more bulk syncs.
     * <p>This endpoint is a summary view, not an execution-history view. Each schema is
     * represented at most once with its most recent execution status, and running
     * executions are preferred over older terminal ones.</p>
     * <p>Use this endpoint when you want a dashboard-style answer to &quot;what is each sync
     * doing now?&quot; If you need the full execution history or a single execution's
     * details, use <a href="../../../../api-reference/bulk-sync/executions/list"><code>GET /api/bulk/syncs/{id}/executions</code></a> or
     * <a href="../../../../api-reference/bulk-sync/executions/get"><code>GET /api/bulk/syncs/{id}/executions/{exec_id}</code></a> instead.</p>
     * <p>Setting <code>all=true</code> or <code>active=true</code> ignores any explicit <code>sync_id</code> filters and
     * expands the request to the caller's organization scope.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ListBulkSyncExecutionsStatusEnvelope>> listStatus(
            ExecutionsListStatusRequest request) {
        return listStatus(request, null);
    }

    /**
     * Returns a concise per-schema status for one or more bulk syncs.
     * <p>This endpoint is a summary view, not an execution-history view. Each schema is
     * represented at most once with its most recent execution status, and running
     * executions are preferred over older terminal ones.</p>
     * <p>Use this endpoint when you want a dashboard-style answer to &quot;what is each sync
     * doing now?&quot; If you need the full execution history or a single execution's
     * details, use <a href="../../../../api-reference/bulk-sync/executions/list"><code>GET /api/bulk/syncs/{id}/executions</code></a> or
     * <a href="../../../../api-reference/bulk-sync/executions/get"><code>GET /api/bulk/syncs/{id}/executions/{exec_id}</code></a> instead.</p>
     * <p>Setting <code>all=true</code> or <code>active=true</code> ignores any explicit <code>sync_id</code> filters and
     * expands the request to the caller's organization scope.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ListBulkSyncExecutionsStatusEnvelope>> listStatus(
            ExecutionsListStatusRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/bulk/syncs/status");
        if (request.getAll().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "all", request.getAll().get(), false);
        }
        if (request.getActive().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "active", request.getActive().get(), false);
        }
        if (request.getSyncId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "sync_id", request.getSyncId().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<ListBulkSyncExecutionsStatusEnvelope>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, ListBulkSyncExecutionsStatusEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 404) {
                            future.completeExceptionally(new NotFoundError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Lists executions for a bulk sync.
     * <p>Results are ordered by start time descending by default. When more results are
     * available, the response includes an opaque <code>pagination.next_page_token</code>; pass it
     * back as the <code>page_token</code> query parameter to retrieve the next page. The <code>limit</code>
     * parameter is optional, and the maximum page size is 100 executions.</p>
     * <p>Use <code>only_terminal=true</code> to return only finished executions. In that mode,
     * executions are ordered by <code>updated_at</code> so recently completed runs appear first.</p>
     * <p>Use <code>ascending=true</code> to walk forward from the oldest execution instead of
     * starting with the newest execution.</p>
     * <p>For the full details of a single run — including per-schema execution status —
     * use <a href="../../../../../api-reference/bulk-sync/executions/get"><code>GET /api/bulk/syncs/{id}/executions/{exec_id}</code></a>.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ListBulkSyncExecutionsEnvelope>> list(String id) {
        return list(id, ExecutionsListRequest.builder().build());
    }

    /**
     * Lists executions for a bulk sync.
     * <p>Results are ordered by start time descending by default. When more results are
     * available, the response includes an opaque <code>pagination.next_page_token</code>; pass it
     * back as the <code>page_token</code> query parameter to retrieve the next page. The <code>limit</code>
     * parameter is optional, and the maximum page size is 100 executions.</p>
     * <p>Use <code>only_terminal=true</code> to return only finished executions. In that mode,
     * executions are ordered by <code>updated_at</code> so recently completed runs appear first.</p>
     * <p>Use <code>ascending=true</code> to walk forward from the oldest execution instead of
     * starting with the newest execution.</p>
     * <p>For the full details of a single run — including per-schema execution status —
     * use <a href="../../../../../api-reference/bulk-sync/executions/get"><code>GET /api/bulk/syncs/{id}/executions/{exec_id}</code></a>.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ListBulkSyncExecutionsEnvelope>> list(
            String id, RequestOptions requestOptions) {
        return list(id, ExecutionsListRequest.builder().build(), requestOptions);
    }

    /**
     * Lists executions for a bulk sync.
     * <p>Results are ordered by start time descending by default. When more results are
     * available, the response includes an opaque <code>pagination.next_page_token</code>; pass it
     * back as the <code>page_token</code> query parameter to retrieve the next page. The <code>limit</code>
     * parameter is optional, and the maximum page size is 100 executions.</p>
     * <p>Use <code>only_terminal=true</code> to return only finished executions. In that mode,
     * executions are ordered by <code>updated_at</code> so recently completed runs appear first.</p>
     * <p>Use <code>ascending=true</code> to walk forward from the oldest execution instead of
     * starting with the newest execution.</p>
     * <p>For the full details of a single run — including per-schema execution status —
     * use <a href="../../../../../api-reference/bulk-sync/executions/get"><code>GET /api/bulk/syncs/{id}/executions/{exec_id}</code></a>.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ListBulkSyncExecutionsEnvelope>> list(
            String id, ExecutionsListRequest request) {
        return list(id, request, null);
    }

    /**
     * Lists executions for a bulk sync.
     * <p>Results are ordered by start time descending by default. When more results are
     * available, the response includes an opaque <code>pagination.next_page_token</code>; pass it
     * back as the <code>page_token</code> query parameter to retrieve the next page. The <code>limit</code>
     * parameter is optional, and the maximum page size is 100 executions.</p>
     * <p>Use <code>only_terminal=true</code> to return only finished executions. In that mode,
     * executions are ordered by <code>updated_at</code> so recently completed runs appear first.</p>
     * <p>Use <code>ascending=true</code> to walk forward from the oldest execution instead of
     * starting with the newest execution.</p>
     * <p>For the full details of a single run — including per-schema execution status —
     * use <a href="../../../../../api-reference/bulk-sync/executions/get"><code>GET /api/bulk/syncs/{id}/executions/{exec_id}</code></a>.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<ListBulkSyncExecutionsEnvelope>> list(
            String id, ExecutionsListRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/bulk/syncs")
                .addPathSegment(id)
                .addPathSegments("executions");
        if (request.getPageToken().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_token", request.getPageToken().get(), false);
        }
        if (request.getOnlyTerminal().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "only_terminal", request.getOnlyTerminal().get(), false);
        }
        if (request.getAscending().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "ascending", request.getAscending().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<ListBulkSyncExecutionsEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, ListBulkSyncExecutionsEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 404) {
                            future.completeExceptionally(new NotFoundError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Returns a single bulk sync execution, including per-schema execution status.
     * <p>The response includes a breakdown of each schema (table or object) that
     * participated in the execution, with its individual status, row counts, and any
     * error details. This makes it suitable for diagnosing partial failures where
     * some schemas succeeded while others did not.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<BulkSyncExecutionEnvelope>> get(String id, String execId) {
        return get(id, execId, null);
    }

    /**
     * Returns a single bulk sync execution, including per-schema execution status.
     * <p>The response includes a breakdown of each schema (table or object) that
     * participated in the execution, with its individual status, row counts, and any
     * error details. This makes it suitable for diagnosing partial failures where
     * some schemas succeeded while others did not.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<BulkSyncExecutionEnvelope>> get(
            String id, String execId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/bulk/syncs")
                .addPathSegment(id)
                .addPathSegments("executions")
                .addPathSegment(execId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<BulkSyncExecutionEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, BulkSyncExecutionEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 404) {
                            future.completeExceptionally(new NotFoundError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Requests cancellation of a specific bulk sync execution.
     * <p>Cancellation is asynchronous. A successful response means the cancellation
     * signal has been queued; the execution continues to run until the signal is
     * processed. Poll <code>GET /api/bulk/syncs/{id}/executions/{exec_id}</code> until the
     * execution reaches a terminal state (<code>completed</code>, <code>canceled</code>, or <code>failed</code>) to
     * confirm cancellation has taken effect.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<CancelBulkSyncResponseEnvelope>> cancel(String id, String execId) {
        return cancel(id, execId, null);
    }

    /**
     * Requests cancellation of a specific bulk sync execution.
     * <p>Cancellation is asynchronous. A successful response means the cancellation
     * signal has been queued; the execution continues to run until the signal is
     * processed. Poll <code>GET /api/bulk/syncs/{id}/executions/{exec_id}</code> until the
     * execution reaches a terminal state (<code>completed</code>, <code>canceled</code>, or <code>failed</code>) to
     * confirm cancellation has taken effect.</p>
     */
    public CompletableFuture<PolytomicHttpResponse<CancelBulkSyncResponseEnvelope>> cancel(
            String id, String execId, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/bulk/syncs")
                .addPathSegment(id)
                .addPathSegments("executions")
                .addPathSegment(execId)
                .addPathSegments("cancel");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<CancelBulkSyncResponseEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, CancelBulkSyncResponseEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Fetch the latest console log entries for a bulk sync execution. Returns at most the most recent 50 entries retained in Redis.
     */
    public CompletableFuture<PolytomicHttpResponse<ExecutionConsoleLogsResponseEnvelope>> getConsoleLogs(
            String syncId, String executionId) {
        return getConsoleLogs(
                syncId, executionId, ExecutionsGetConsoleLogsRequest.builder().build());
    }

    /**
     * Fetch the latest console log entries for a bulk sync execution. Returns at most the most recent 50 entries retained in Redis.
     */
    public CompletableFuture<PolytomicHttpResponse<ExecutionConsoleLogsResponseEnvelope>> getConsoleLogs(
            String syncId, String executionId, RequestOptions requestOptions) {
        return getConsoleLogs(
                syncId, executionId, ExecutionsGetConsoleLogsRequest.builder().build(), requestOptions);
    }

    /**
     * Fetch the latest console log entries for a bulk sync execution. Returns at most the most recent 50 entries retained in Redis.
     */
    public CompletableFuture<PolytomicHttpResponse<ExecutionConsoleLogsResponseEnvelope>> getConsoleLogs(
            String syncId, String executionId, ExecutionsGetConsoleLogsRequest request) {
        return getConsoleLogs(syncId, executionId, request, null);
    }

    /**
     * Fetch the latest console log entries for a bulk sync execution. Returns at most the most recent 50 entries retained in Redis.
     */
    public CompletableFuture<PolytomicHttpResponse<ExecutionConsoleLogsResponseEnvelope>> getConsoleLogs(
            String syncId, String executionId, ExecutionsGetConsoleLogsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/bulk/syncs")
                .addPathSegment(syncId)
                .addPathSegments("executions")
                .addPathSegment(executionId)
                .addPathSegments("consolelog");
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<ExecutionConsoleLogsResponseEnvelope>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, ExecutionConsoleLogsResponseEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 408:
                                future.completeExceptionally(new RequestTimeoutError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Returns signed URLs for the log files produced by a single bulk sync execution.
     * <p>Each URL in the response is pre-signed and grants temporary read access to the
     * corresponding log file. URLs expire after a short period; if you need to access
     * a file after the URL has expired, call this endpoint again to obtain a fresh set
     * of signed URLs.</p>
     * <blockquote>
     * <p>📘 To export logs asynchronously to a destination of your choice, use
     * <a href="../../../../../../../api-reference/bulk-sync/executions/export-logs"><code>POST /api/bulk/syncs/{sync_id}/executions/{execution_id}/logs/export</code></a>
     * instead.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<BulkSyncExecutionLogsEnvelope>> getLogs(
            String syncId, String executionId) {
        return getLogs(syncId, executionId, null);
    }

    /**
     * Returns signed URLs for the log files produced by a single bulk sync execution.
     * <p>Each URL in the response is pre-signed and grants temporary read access to the
     * corresponding log file. URLs expire after a short period; if you need to access
     * a file after the URL has expired, call this endpoint again to obtain a fresh set
     * of signed URLs.</p>
     * <blockquote>
     * <p>📘 To export logs asynchronously to a destination of your choice, use
     * <a href="../../../../../../../api-reference/bulk-sync/executions/export-logs"><code>POST /api/bulk/syncs/{sync_id}/executions/{execution_id}/logs/export</code></a>
     * instead.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<BulkSyncExecutionLogsEnvelope>> getLogs(
            String syncId, String executionId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/bulk/syncs")
                .addPathSegment(syncId)
                .addPathSegments("executions")
                .addPathSegment(executionId)
                .addPathSegments("logs");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<BulkSyncExecutionLogsEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, BulkSyncExecutionLogsEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 404) {
                            future.completeExceptionally(new NotFoundError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Starts an asynchronous job that packages the log files for a single bulk sync execution into a downloadable archive.
     * <blockquote>
     * <p>📘 Log export is asynchronous</p>
     * <p>This endpoint starts a background job that packages an execution's log
     * files into a downloadable archive. The first call typically returns a
     * <code>job</code> descriptor instead of a completed result. Poll
     * <a href="../../../../../../../../api-reference/jobs/get"><code>GET /api/jobs/exportlogs/{id}</code></a>
     * with the returned <code>job_id</code> until <code>status</code> is <code>done</code>; the final response
     * contains a signed <code>url</code> that can be used to download the archive.</p>
     * <p>Set <code>notify=true</code> to also email the requesting user when the archive is
     * ready.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<ExportSyncLogsEnvelope>> exportLogs(
            String syncId, String executionId) {
        return exportLogs(
                syncId, executionId, ExecutionsExportLogsRequest.builder().build());
    }

    /**
     * Starts an asynchronous job that packages the log files for a single bulk sync execution into a downloadable archive.
     * <blockquote>
     * <p>📘 Log export is asynchronous</p>
     * <p>This endpoint starts a background job that packages an execution's log
     * files into a downloadable archive. The first call typically returns a
     * <code>job</code> descriptor instead of a completed result. Poll
     * <a href="../../../../../../../../api-reference/jobs/get"><code>GET /api/jobs/exportlogs/{id}</code></a>
     * with the returned <code>job_id</code> until <code>status</code> is <code>done</code>; the final response
     * contains a signed <code>url</code> that can be used to download the archive.</p>
     * <p>Set <code>notify=true</code> to also email the requesting user when the archive is
     * ready.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<ExportSyncLogsEnvelope>> exportLogs(
            String syncId, String executionId, IdempotentRequestOptions requestOptions) {
        return exportLogs(
                syncId, executionId, ExecutionsExportLogsRequest.builder().build(), requestOptions);
    }

    /**
     * Starts an asynchronous job that packages the log files for a single bulk sync execution into a downloadable archive.
     * <blockquote>
     * <p>📘 Log export is asynchronous</p>
     * <p>This endpoint starts a background job that packages an execution's log
     * files into a downloadable archive. The first call typically returns a
     * <code>job</code> descriptor instead of a completed result. Poll
     * <a href="../../../../../../../../api-reference/jobs/get"><code>GET /api/jobs/exportlogs/{id}</code></a>
     * with the returned <code>job_id</code> until <code>status</code> is <code>done</code>; the final response
     * contains a signed <code>url</code> that can be used to download the archive.</p>
     * <p>Set <code>notify=true</code> to also email the requesting user when the archive is
     * ready.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<ExportSyncLogsEnvelope>> exportLogs(
            String syncId, String executionId, ExecutionsExportLogsRequest request) {
        return exportLogs(syncId, executionId, request, null);
    }

    /**
     * Starts an asynchronous job that packages the log files for a single bulk sync execution into a downloadable archive.
     * <blockquote>
     * <p>📘 Log export is asynchronous</p>
     * <p>This endpoint starts a background job that packages an execution's log
     * files into a downloadable archive. The first call typically returns a
     * <code>job</code> descriptor instead of a completed result. Poll
     * <a href="../../../../../../../../api-reference/jobs/get"><code>GET /api/jobs/exportlogs/{id}</code></a>
     * with the returned <code>job_id</code> until <code>status</code> is <code>done</code>; the final response
     * contains a signed <code>url</code> that can be used to download the archive.</p>
     * <p>Set <code>notify=true</code> to also email the requesting user when the archive is
     * ready.</p>
     * </blockquote>
     */
    public CompletableFuture<PolytomicHttpResponse<ExportSyncLogsEnvelope>> exportLogs(
            String syncId,
            String executionId,
            ExecutionsExportLogsRequest request,
            IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/bulk/syncs")
                .addPathSegment(syncId)
                .addPathSegments("executions")
                .addPathSegment(executionId)
                .addPathSegments("logs")
                .addPathSegments("export");
        if (request.getNotify().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "notify", request.getNotify().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<ExportSyncLogsEnvelope>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ExportSyncLogsEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Fetch the latest console log entries for a schema within a bulk sync execution. Returns at most the most recent 50 entries retained in Redis.
     */
    public CompletableFuture<PolytomicHttpResponse<ExecutionConsoleLogsResponseEnvelope>> getSchemaConsoleLogs(
            String syncId, String executionId, String schemaId) {
        return getSchemaConsoleLogs(
                syncId,
                executionId,
                schemaId,
                ExecutionsGetSchemaConsoleLogsRequest.builder().build());
    }

    /**
     * Fetch the latest console log entries for a schema within a bulk sync execution. Returns at most the most recent 50 entries retained in Redis.
     */
    public CompletableFuture<PolytomicHttpResponse<ExecutionConsoleLogsResponseEnvelope>> getSchemaConsoleLogs(
            String syncId, String executionId, String schemaId, RequestOptions requestOptions) {
        return getSchemaConsoleLogs(
                syncId,
                executionId,
                schemaId,
                ExecutionsGetSchemaConsoleLogsRequest.builder().build(),
                requestOptions);
    }

    /**
     * Fetch the latest console log entries for a schema within a bulk sync execution. Returns at most the most recent 50 entries retained in Redis.
     */
    public CompletableFuture<PolytomicHttpResponse<ExecutionConsoleLogsResponseEnvelope>> getSchemaConsoleLogs(
            String syncId, String executionId, String schemaId, ExecutionsGetSchemaConsoleLogsRequest request) {
        return getSchemaConsoleLogs(syncId, executionId, schemaId, request, null);
    }

    /**
     * Fetch the latest console log entries for a schema within a bulk sync execution. Returns at most the most recent 50 entries retained in Redis.
     */
    public CompletableFuture<PolytomicHttpResponse<ExecutionConsoleLogsResponseEnvelope>> getSchemaConsoleLogs(
            String syncId,
            String executionId,
            String schemaId,
            ExecutionsGetSchemaConsoleLogsRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/bulk/syncs")
                .addPathSegment(syncId)
                .addPathSegments("executions")
                .addPathSegment(executionId)
                .addPathSegments("schemas")
                .addPathSegment(schemaId)
                .addPathSegments("consolelog");
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<PolytomicHttpResponse<ExecutionConsoleLogsResponseEnvelope>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new PolytomicHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, ExecutionConsoleLogsResponseEnvelope.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 408:
                                future.completeExceptionally(new RequestTimeoutError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new PolytomicApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new PolytomicException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
