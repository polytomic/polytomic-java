/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.polytomic.api.resources.temporarycredentials;

import com.polytomic.api.core.ClientOptions;
import com.polytomic.api.core.IdempotentRequestOptions;
import com.polytomic.api.resources.temporarycredentials.requests.CreateTemporaryCredentialRequest;
import com.polytomic.api.types.TemporaryCredentialResponseEnvelope;

public class TemporaryCredentialsClient {
    protected final ClientOptions clientOptions;

    private final RawTemporaryCredentialsClient rawClient;

    public TemporaryCredentialsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawTemporaryCredentialsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawTemporaryCredentialsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Issues a non-renewable credential with a bounded lifetime for a user or Agent Data profile.
     * <p>The response contains the credential secret once. Store it securely and send it
     * as a Bearer token in the <code>Authorization</code> header.</p>
     * <p>Set <code>subject.type</code> to <code>user</code> to issue a credential for your authenticated user.
     * Omit <code>organization_id</code> and <code>user_id</code>; Polytomic derives both values from your
     * credential. Set <code>mode</code> to <code>read_only</code> to limit the credential to the
     * intersection of the user's current permissions and read-only actions. A
     * read-only caller can issue only read-only credentials.</p>
     * <p>Partner callers must provide both <code>organization_id</code> and <code>user_id</code>. The target
     * must be an active user in an organization owned by the partner. User subjects
     * must be application users; Agent Data portal-only users continue to use profile
     * credentials.</p>
     * <p>User credentials resolve the subject's current permissions on every request.
     * Permission changes take effect immediately, and deleting the user invalidates
     * the credential.</p>
     * <p>Set <code>subject.type</code> to <code>profile</code> and provide the Agent Data profile ID. The
     * credential uses the profile's current connection access on every request;
     * changes take effect immediately, and deleting the profile invalidates the
     * credential.</p>
     * <p>A temporary credential stops authenticating at <code>expires_at</code>. It cannot be
     * refreshed, extended, or used to create another temporary credential. Create a
     * new credential with a durable authorized credential when you need a later
     * expiration.</p>
     * <p>Each organization may have up to 1,000 active temporary credentials. The
     * endpoint returns <code>429 Too Many Requests</code> at the limit. Expired credentials stop
     * counting toward the limit immediately, before periodic cleanup removes them.</p>
     * <blockquote>
     * <p>⚠️ Session names are audit labels</p>
     * <p>Use <code>session_name</code> only for non-sensitive job or agent-session correlation.
     * Do not include secrets or personal data.</p>
     * </blockquote>
     * <p>Polytomic periodically removes expired credential records. API usage history
     * keeps its credential ID according to the normal API usage retention period.</p>
     */
    public TemporaryCredentialResponseEnvelope create(CreateTemporaryCredentialRequest request) {
        return this.rawClient.create(request).body();
    }

    /**
     * Issues a non-renewable credential with a bounded lifetime for a user or Agent Data profile.
     * <p>The response contains the credential secret once. Store it securely and send it
     * as a Bearer token in the <code>Authorization</code> header.</p>
     * <p>Set <code>subject.type</code> to <code>user</code> to issue a credential for your authenticated user.
     * Omit <code>organization_id</code> and <code>user_id</code>; Polytomic derives both values from your
     * credential. Set <code>mode</code> to <code>read_only</code> to limit the credential to the
     * intersection of the user's current permissions and read-only actions. A
     * read-only caller can issue only read-only credentials.</p>
     * <p>Partner callers must provide both <code>organization_id</code> and <code>user_id</code>. The target
     * must be an active user in an organization owned by the partner. User subjects
     * must be application users; Agent Data portal-only users continue to use profile
     * credentials.</p>
     * <p>User credentials resolve the subject's current permissions on every request.
     * Permission changes take effect immediately, and deleting the user invalidates
     * the credential.</p>
     * <p>Set <code>subject.type</code> to <code>profile</code> and provide the Agent Data profile ID. The
     * credential uses the profile's current connection access on every request;
     * changes take effect immediately, and deleting the profile invalidates the
     * credential.</p>
     * <p>A temporary credential stops authenticating at <code>expires_at</code>. It cannot be
     * refreshed, extended, or used to create another temporary credential. Create a
     * new credential with a durable authorized credential when you need a later
     * expiration.</p>
     * <p>Each organization may have up to 1,000 active temporary credentials. The
     * endpoint returns <code>429 Too Many Requests</code> at the limit. Expired credentials stop
     * counting toward the limit immediately, before periodic cleanup removes them.</p>
     * <blockquote>
     * <p>⚠️ Session names are audit labels</p>
     * <p>Use <code>session_name</code> only for non-sensitive job or agent-session correlation.
     * Do not include secrets or personal data.</p>
     * </blockquote>
     * <p>Polytomic periodically removes expired credential records. API usage history
     * keeps its credential ID according to the normal API usage retention period.</p>
     */
    public TemporaryCredentialResponseEnvelope create(
            CreateTemporaryCredentialRequest request, IdempotentRequestOptions requestOptions) {
        return this.rawClient.create(request, requestOptions).body();
    }
}
