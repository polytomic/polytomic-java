/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.polytomic.api.resources.connections.proxy;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.polytomic.api.core.ClientOptions;
import com.polytomic.api.core.IdempotentRequestOptions;
import com.polytomic.api.core.MediaTypes;
import com.polytomic.api.core.ObjectMappers;
import com.polytomic.api.core.PolytomicApiException;
import com.polytomic.api.core.PolytomicException;
import com.polytomic.api.core.PolytomicHttpResponse;
import com.polytomic.api.core.RequestOptions;
import com.polytomic.api.core.RetryInterceptor;
import com.polytomic.api.errors.BadGatewayError;
import com.polytomic.api.errors.BadRequestError;
import com.polytomic.api.errors.ForbiddenError;
import com.polytomic.api.errors.GatewayTimeoutError;
import com.polytomic.api.errors.InternalServerError;
import com.polytomic.api.errors.NotFoundError;
import com.polytomic.api.errors.TooManyRequestsError;
import com.polytomic.api.errors.UnauthorizedError;
import com.polytomic.api.resources.connections.proxy.requests.ExecuteConnectionProxyRequest;
import com.polytomic.api.resources.connections.proxy.requests.UpdateConnectionProxySettingsRequest;
import com.polytomic.api.types.ApiError;
import com.polytomic.api.types.ConnectionProxySettingsEnvelope;
import com.polytomic.api.types.ExecuteConnectionProxyEnvelope;
import com.polytomic.api.types.GetConnectionProxyInfoEnvelope;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawProxyClient {
    protected final ClientOptions clientOptions;

    public RawProxyClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Proxies an HTTP request to a connection's underlying API using the connection's stored credentials, subject to per-connection rate limits and size caps.
     * <p>This endpoint is intended for controlled passthrough use, not as a general
     * replacement for Polytomic's modeled endpoints. The request is executed with the
     * connection's stored credentials and inherited base URL, headers, and query
     * parameters.</p>
     * <p>Before building requests dynamically, call
     * <a href="../../../../api-reference/connections/get-proxy-info"><code>GET /api/connections/{id}/proxy/info</code></a>
     * to inspect the inherited base URL, blocked headers, accepted body types, and
     * size and rate limits.</p>
     * <h2>Important behavior</h2>
     * <ul>
     * <li><code>request.path</code> must be relative and start with <code>/</code>.</li>
     * <li>Use either <code>request.query</code> or <code>request.rawQuery</code>, not both.</li>
     * <li>Caller-supplied headers are merged with inherited headers, but inherited auth
     * headers cannot be overridden.</li>
     * <li>The proxy strips a fixed set of request and response headers for safety.</li>
     * <li>Response bodies larger than the configured maximum are truncated, and
     * <code>truncated</code> is set to <code>true</code>.</li>
     * </ul>
     * <p>To run a <code>GET</code> request asynchronously, set <code>async</code> to <code>true</code>. The initial
     * response returns <code>status: 202</code>, <code>jobId</code>, <code>jobStatus</code>, and <code>jobUrl</code>. Poll
     * <a href="../../../../api-reference/jobs/get-job"><code>GET /api/jobs/{type}/{id}</code></a> with
     * <code>type=connectionproxy</code> and the returned <code>jobId</code> until the job is complete. The
     * completed job result includes the upstream <code>status</code>, sanitized <code>headers</code>,
     * <code>contentType</code>, <code>contentLength</code>, <code>latencyMs</code>, and a short-lived
     * <code>bodyDownloadUrl</code> for the upstream response body.</p>
     * <p>The response includes <code>proxyCallId</code>, which you can use to correlate the call
     * with audit logs.</p>
     */
    public PolytomicHttpResponse<ExecuteConnectionProxyEnvelope> executeProxy(
            String id, ExecuteConnectionProxyRequest request) {
        return executeProxy(id, request, null);
    }

    /**
     * Proxies an HTTP request to a connection's underlying API using the connection's stored credentials, subject to per-connection rate limits and size caps.
     * <p>This endpoint is intended for controlled passthrough use, not as a general
     * replacement for Polytomic's modeled endpoints. The request is executed with the
     * connection's stored credentials and inherited base URL, headers, and query
     * parameters.</p>
     * <p>Before building requests dynamically, call
     * <a href="../../../../api-reference/connections/get-proxy-info"><code>GET /api/connections/{id}/proxy/info</code></a>
     * to inspect the inherited base URL, blocked headers, accepted body types, and
     * size and rate limits.</p>
     * <h2>Important behavior</h2>
     * <ul>
     * <li><code>request.path</code> must be relative and start with <code>/</code>.</li>
     * <li>Use either <code>request.query</code> or <code>request.rawQuery</code>, not both.</li>
     * <li>Caller-supplied headers are merged with inherited headers, but inherited auth
     * headers cannot be overridden.</li>
     * <li>The proxy strips a fixed set of request and response headers for safety.</li>
     * <li>Response bodies larger than the configured maximum are truncated, and
     * <code>truncated</code> is set to <code>true</code>.</li>
     * </ul>
     * <p>To run a <code>GET</code> request asynchronously, set <code>async</code> to <code>true</code>. The initial
     * response returns <code>status: 202</code>, <code>jobId</code>, <code>jobStatus</code>, and <code>jobUrl</code>. Poll
     * <a href="../../../../api-reference/jobs/get-job"><code>GET /api/jobs/{type}/{id}</code></a> with
     * <code>type=connectionproxy</code> and the returned <code>jobId</code> until the job is complete. The
     * completed job result includes the upstream <code>status</code>, sanitized <code>headers</code>,
     * <code>contentType</code>, <code>contentLength</code>, <code>latencyMs</code>, and a short-lived
     * <code>bodyDownloadUrl</code> for the upstream response body.</p>
     * <p>The response includes <code>proxyCallId</code>, which you can use to correlate the call
     * with audit logs.</p>
     */
    public PolytomicHttpResponse<ExecuteConnectionProxyEnvelope> executeProxy(
            String id, ExecuteConnectionProxyRequest request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/connections")
                .addPathSegment(id)
                .addPathSegments("proxy");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ExecuteConnectionProxyEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 502:
                        throw new BadGatewayError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 504:
                        throw new GatewayTimeoutError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns the proxy contract for a connection.
     * <p>Use this endpoint before calling
     * <a href="../../../../../api-reference/connections/execute-proxy"><code>POST /api/connections/{id}/proxy</code></a>
     * when you need to build requests programmatically. The response shows:</p>
     * <ul>
     * <li>the inherited base URL that all proxied requests are sent to</li>
     * <li>locked headers and query parameters that are attached automatically</li>
     * <li>blocked request and response headers</li>
     * <li>allowed HTTP methods and body shapes</li>
     * <li>timeout, rate-limit, and payload-size limits</li>
     * </ul>
     * <p>Sensitive inherited header and query values are redacted in the response. The
     * contract is still useful for discovering which keys are fixed by the
     * connection, even though their raw values are not exposed.</p>
     */
    public PolytomicHttpResponse<GetConnectionProxyInfoEnvelope> getProxyInfo(String id) {
        return getProxyInfo(id, null);
    }

    /**
     * Returns the proxy contract for a connection.
     * <p>Use this endpoint before calling
     * <a href="../../../../../api-reference/connections/execute-proxy"><code>POST /api/connections/{id}/proxy</code></a>
     * when you need to build requests programmatically. The response shows:</p>
     * <ul>
     * <li>the inherited base URL that all proxied requests are sent to</li>
     * <li>locked headers and query parameters that are attached automatically</li>
     * <li>blocked request and response headers</li>
     * <li>allowed HTTP methods and body shapes</li>
     * <li>timeout, rate-limit, and payload-size limits</li>
     * </ul>
     * <p>Sensitive inherited header and query values are redacted in the response. The
     * contract is still useful for discovering which keys are fixed by the
     * connection, even though their raw values are not exposed.</p>
     */
    public PolytomicHttpResponse<GetConnectionProxyInfoEnvelope> getProxyInfo(
            String id, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/connections")
                .addPathSegment(id)
                .addPathSegments("proxy")
                .addPathSegments("info");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetConnectionProxyInfoEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns whether a connection can be used through the Connection Proxy API.
     * <p>The setting is stored on the parent connection. When you request settings for a
     * shared connection, the response includes both the requested <code>connectionId</code> and the
     * <code>parentConnectionId</code> that controls proxy access. For non-shared connections,
     * <code>parentConnectionId</code> is omitted.</p>
     */
    public PolytomicHttpResponse<ConnectionProxySettingsEnvelope> getProxySettings(String id) {
        return getProxySettings(id, null);
    }

    /**
     * Returns whether a connection can be used through the Connection Proxy API.
     * <p>The setting is stored on the parent connection. When you request settings for a
     * shared connection, the response includes both the requested <code>connectionId</code> and the
     * <code>parentConnectionId</code> that controls proxy access. For non-shared connections,
     * <code>parentConnectionId</code> is omitted.</p>
     */
    public PolytomicHttpResponse<ConnectionProxySettingsEnvelope> getProxySettings(
            String id, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/connections")
                .addPathSegment(id)
                .addPathSegments("proxy")
                .addPathSegments("settings");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ConnectionProxySettingsEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Enables or disables use of a connection through the Connection Proxy API.
     * <p>The setting is stored on the parent connection. To update proxy access for a
     * shared connection, the caller must have edit permission for the parent
     * connection.</p>
     * <p>Enabling proxy access requires a backend that supports the Connection Proxy API.
     * If the connection backend is unsupported, the request returns <code>400 Bad Request</code>.
     * Disabling proxy access is allowed for any connection the caller can edit.</p>
     * <p>Setting <code>enabled</code> to <code>false</code> prevents proxy calls for the connection.</p>
     */
    public PolytomicHttpResponse<ConnectionProxySettingsEnvelope> updateProxySettings(
            String id, UpdateConnectionProxySettingsRequest request) {
        return updateProxySettings(id, request, null);
    }

    /**
     * Enables or disables use of a connection through the Connection Proxy API.
     * <p>The setting is stored on the parent connection. To update proxy access for a
     * shared connection, the caller must have edit permission for the parent
     * connection.</p>
     * <p>Enabling proxy access requires a backend that supports the Connection Proxy API.
     * If the connection backend is unsupported, the request returns <code>400 Bad Request</code>.
     * Disabling proxy access is allowed for any connection the caller can edit.</p>
     * <p>Setting <code>enabled</code> to <code>false</code> prevents proxy calls for the connection.</p>
     */
    public PolytomicHttpResponse<ConnectionProxySettingsEnvelope> updateProxySettings(
            String id, UpdateConnectionProxySettingsRequest request, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/connections")
                .addPathSegment(id)
                .addPathSegments("proxy")
                .addPathSegments("settings");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ConnectionProxySettingsEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PolytomicException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }
}
