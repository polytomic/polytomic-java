/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.polytomic.api.resources.connections;

import com.polytomic.api.core.ClientOptions;
import com.polytomic.api.core.IdempotentRequestOptions;
import com.polytomic.api.core.RequestOptions;
import com.polytomic.api.core.Suppliers;
import com.polytomic.api.resources.connections.proxy.AsyncProxyClient;
import com.polytomic.api.resources.connections.requests.ConnectCardRequest;
import com.polytomic.api.resources.connections.requests.ConnectionsDeleteRequest;
import com.polytomic.api.resources.connections.requests.CreateConnectionRequestSchema;
import com.polytomic.api.resources.connections.requests.GetConnectionTypeParameterValuesRequestSchema;
import com.polytomic.api.resources.connections.requests.TestConnectionRequest;
import com.polytomic.api.resources.connections.requests.UpdateConnectionRequestSchema;
import com.polytomic.api.resources.connections.sharedconnections.AsyncSharedConnectionsClient;
import com.polytomic.api.types.ConnectCardResponseEnvelope;
import com.polytomic.api.types.ConnectSessionResponseEnvelope;
import com.polytomic.api.types.ConnectionListResponseEnvelope;
import com.polytomic.api.types.ConnectionParameterValuesResponseEnvelope;
import com.polytomic.api.types.ConnectionResponseEnvelope;
import com.polytomic.api.types.ConnectionTypeResponseEnvelope;
import com.polytomic.api.types.CreateConnectionResponseEnvelope;
import com.polytomic.api.types.GetConnectionUsageEnvelope;
import com.polytomic.api.types.JsonschemaSchema;
import java.util.concurrent.CompletableFuture;
import java.util.function.Supplier;

public class AsyncConnectionsClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawConnectionsClient rawClient;

    protected final Supplier<AsyncProxyClient> proxyClient;

    protected final Supplier<AsyncSharedConnectionsClient> sharedConnectionsClient;

    public AsyncConnectionsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawConnectionsClient(clientOptions);
        this.proxyClient = Suppliers.memoize(() -> new AsyncProxyClient(clientOptions));
        this.sharedConnectionsClient = Suppliers.memoize(() -> new AsyncSharedConnectionsClient(clientOptions));
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawConnectionsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Lists all connection types supported by this deployment.
     * <p>Each entry includes per-type metadata:</p>
     * <ul>
     * <li>The available operations the connection type supports.</li>
     * <li>Its category.</li>
     * <li>Whether the connection type is enabled for the caller's organization.</li>
     * <li>Which modes (source, destination, enrichment) it can act as.</li>
     * </ul>
     */
    public CompletableFuture<ConnectionTypeResponseEnvelope> getTypes() {
        return this.rawClient.getTypes().thenApply(response -> response.body());
    }

    /**
     * Lists all connection types supported by this deployment.
     * <p>Each entry includes per-type metadata:</p>
     * <ul>
     * <li>The available operations the connection type supports.</li>
     * <li>Its category.</li>
     * <li>Whether the connection type is enabled for the caller's organization.</li>
     * <li>Which modes (source, destination, enrichment) it can act as.</li>
     * </ul>
     */
    public CompletableFuture<ConnectionTypeResponseEnvelope> getTypes(RequestOptions requestOptions) {
        return this.rawClient.getTypes(requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns the JSON schema for a connection type.
     * <p>This schema is intended for building forms or validating configuration payloads
     * client-side. It describes the structure Polytomic expects when you create or
     * update a connection of the given type.</p>
     * <p>The response is metadata about the shape of the configuration, not a live
     * connection instance and not a set of current credential values.</p>
     */
    public CompletableFuture<JsonschemaSchema> getConnectionTypeSchema(String id) {
        return this.rawClient.getConnectionTypeSchema(id).thenApply(response -> response.body());
    }

    /**
     * Returns the JSON schema for a connection type.
     * <p>This schema is intended for building forms or validating configuration payloads
     * client-side. It describes the structure Polytomic expects when you create or
     * update a connection of the given type.</p>
     * <p>The response is metadata about the shape of the configuration, not a live
     * connection instance and not a set of current credential values.</p>
     */
    public CompletableFuture<JsonschemaSchema> getConnectionTypeSchema(String id, RequestOptions requestOptions) {
        return this.rawClient.getConnectionTypeSchema(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns completion values for parameter fields on a connection type.
     * <p>This endpoint is useful during connection setup, before a connection exists or
     * before you want to persist it. The supplied <code>parameters</code> are applied to a
     * temporary in-memory connection shape and used to resolve dependent options.</p>
     * <p>When an endpoint requires upstream authorization before it can return values,
     * Polytomic returns an error instead of guessing. In that case, complete the
     * authorization flow first and call the endpoint again.</p>
     */
    public CompletableFuture<ConnectionParameterValuesResponseEnvelope> getTypeParameterValues(
            String type, GetConnectionTypeParameterValuesRequestSchema request) {
        return this.rawClient.getTypeParameterValues(type, request).thenApply(response -> response.body());
    }

    /**
     * Returns completion values for parameter fields on a connection type.
     * <p>This endpoint is useful during connection setup, before a connection exists or
     * before you want to persist it. The supplied <code>parameters</code> are applied to a
     * temporary in-memory connection shape and used to resolve dependent options.</p>
     * <p>When an endpoint requires upstream authorization before it can return values,
     * Polytomic returns an error instead of guessing. In that case, complete the
     * authorization flow first and call the endpoint again.</p>
     */
    public CompletableFuture<ConnectionParameterValuesResponseEnvelope> getTypeParameterValues(
            String type,
            GetConnectionTypeParameterValuesRequestSchema request,
            IdempotentRequestOptions requestOptions) {
        return this.rawClient
                .getTypeParameterValues(type, request, requestOptions)
                .thenApply(response -> response.body());
    }

    /**
     * Lists every connection in the caller's organization, with sensitive fields redacted.
     * <p>Sensitive configuration values — passwords, API tokens, private keys — are
     * redacted from all responses. To understand which fields a connection type
     * exposes, consult the parameter schema returned by
     * <a href="../../api-reference/connections/get-types"><code>GET /api/connection_types</code></a>.</p>
     * <p>To inspect the data objects available on a specific connection, use
     * <a href="../../api-reference/schemas/refresh"><code>POST /api/connections/{id}/schemas/refresh</code></a>
     * followed by <a href="../../api-reference/schemas/get-status"><code>GET /api/connections/{id}/schemas/status</code></a>.</p>
     */
    public CompletableFuture<ConnectionListResponseEnvelope> list() {
        return this.rawClient.list().thenApply(response -> response.body());
    }

    /**
     * Lists every connection in the caller's organization, with sensitive fields redacted.
     * <p>Sensitive configuration values — passwords, API tokens, private keys — are
     * redacted from all responses. To understand which fields a connection type
     * exposes, consult the parameter schema returned by
     * <a href="../../api-reference/connections/get-types"><code>GET /api/connection_types</code></a>.</p>
     * <p>To inspect the data objects available on a specific connection, use
     * <a href="../../api-reference/schemas/refresh"><code>POST /api/connections/{id}/schemas/refresh</code></a>
     * followed by <a href="../../api-reference/schemas/get-status"><code>GET /api/connections/{id}/schemas/status</code></a>.</p>
     */
    public CompletableFuture<ConnectionListResponseEnvelope> list(RequestOptions requestOptions) {
        return this.rawClient.list(requestOptions).thenApply(response -> response.body());
    }

    /**
     * Creates a new connection of the specified type.
     * <p>Use <a href="../../api-reference/connections/get-types"><code>GET /api/connection_types</code></a> to retrieve the
     * list of available types and their parameter schemas. The <code>configuration</code>
     * object is type-specific; consult the <a href="../../guides/configuring-your-connections/overview">integration
     * guides</a>
     * for the required and optional fields for each type.</p>
     * <blockquote>
     * <p>📘 Polytomic validates the connection against the upstream service
     * immediately on creation. The request will fail if the credentials or
     * endpoint cannot be reached.</p>
     * </blockquote>
     */
    public CompletableFuture<CreateConnectionResponseEnvelope> create(CreateConnectionRequestSchema request) {
        return this.rawClient.create(request).thenApply(response -> response.body());
    }

    /**
     * Creates a new connection of the specified type.
     * <p>Use <a href="../../api-reference/connections/get-types"><code>GET /api/connection_types</code></a> to retrieve the
     * list of available types and their parameter schemas. The <code>configuration</code>
     * object is type-specific; consult the <a href="../../guides/configuring-your-connections/overview">integration
     * guides</a>
     * for the required and optional fields for each type.</p>
     * <blockquote>
     * <p>📘 Polytomic validates the connection against the upstream service
     * immediately on creation. The request will fail if the credentials or
     * endpoint cannot be reached.</p>
     * </blockquote>
     */
    public CompletableFuture<CreateConnectionResponseEnvelope> create(
            CreateConnectionRequestSchema request, IdempotentRequestOptions requestOptions) {
        return this.rawClient.create(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Creates a Polytomic Connect session and returns a URL for creating or reconnecting a Connection.
     * <p>Open the returned URL, or send it to the person who will set up the Connection.
     * Polytomic Connect guides them through authentication and configuration, then
     * redirects them to <code>redirect_url</code>.</p>
     * <p>Each session can create or reconnect one Connection.</p>
     * <p>See also:</p>
     * <ul>
     * <li><a href="../../../guides/embedding-authentication">Embedding authentication</a>, a guide to using Polytomic Connect.</li>
     * </ul>
     */
    public CompletableFuture<ConnectCardResponseEnvelope> connect(ConnectCardRequest request) {
        return this.rawClient.connect(request).thenApply(response -> response.body());
    }

    /**
     * Creates a Polytomic Connect session and returns a URL for creating or reconnecting a Connection.
     * <p>Open the returned URL, or send it to the person who will set up the Connection.
     * Polytomic Connect guides them through authentication and configuration, then
     * redirects them to <code>redirect_url</code>.</p>
     * <p>Each session can create or reconnect one Connection.</p>
     * <p>See also:</p>
     * <ul>
     * <li><a href="../../../guides/embedding-authentication">Embedding authentication</a>, a guide to using Polytomic Connect.</li>
     * </ul>
     */
    public CompletableFuture<ConnectCardResponseEnvelope> connect(
            ConnectCardRequest request, IdempotentRequestOptions requestOptions) {
        return this.rawClient.connect(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns trusted metadata for the authenticated Polytomic Connect session.
     * <p>Returns the trusted metadata stored for a Polytomic Connect session. Authenticate with the opaque Connect token in the <code>token</code> query parameter.</p>
     * <p>The response includes the server-enforced connection name, fixed type or whitelist, bound connection ID, completion redirect, branding, and absolute expiration time.</p>
     */
    public CompletableFuture<ConnectSessionResponseEnvelope> getConnectSession() {
        return this.rawClient.getConnectSession().thenApply(response -> response.body());
    }

    /**
     * Returns trusted metadata for the authenticated Polytomic Connect session.
     * <p>Returns the trusted metadata stored for a Polytomic Connect session. Authenticate with the opaque Connect token in the <code>token</code> query parameter.</p>
     * <p>The response includes the server-enforced connection name, fixed type or whitelist, bound connection ID, completion redirect, branding, and absolute expiration time.</p>
     */
    public CompletableFuture<ConnectSessionResponseEnvelope> getConnectSession(RequestOptions requestOptions) {
        return this.rawClient.getConnectSession(requestOptions).thenApply(response -> response.body());
    }

    /**
     * Tests a connection configuration.
     * <p>This endpoint is useful for setup flows that want to verify credentials before
     * persisting them.</p>
     * <p>If you provide <code>connection_id</code>, Polytomic starts from the saved configuration
     * for that connection and then applies the request's <code>configuration</code> values on
     * top. This lets callers test a partial change without resending every existing
     * field.</p>
     * <p>The request does not persist any configuration changes even when validation
     * succeeds.</p>
     */
    public CompletableFuture<Void> testConnection(TestConnectionRequest request) {
        return this.rawClient.testConnection(request).thenApply(response -> response.body());
    }

    /**
     * Tests a connection configuration.
     * <p>This endpoint is useful for setup flows that want to verify credentials before
     * persisting them.</p>
     * <p>If you provide <code>connection_id</code>, Polytomic starts from the saved configuration
     * for that connection and then applies the request's <code>configuration</code> values on
     * top. This lets callers test a partial change without resending every existing
     * field.</p>
     * <p>The request does not persist any configuration changes even when validation
     * succeeds.</p>
     */
    public CompletableFuture<Void> testConnection(
            TestConnectionRequest request, IdempotentRequestOptions requestOptions) {
        return this.rawClient.testConnection(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns a single connection by ID, with sensitive fields redacted.
     * <p>To inspect the schemas available on this connection, trigger a refresh with
     * <a href="../../../api-reference/schemas/refresh"><code>POST /api/connections/{id}/schemas/refresh</code></a> and
     * track progress via
     * <a href="../../../api-reference/schemas/get-status"><code>GET /api/connections/{id}/schemas/status</code></a>.</p>
     */
    public CompletableFuture<ConnectionResponseEnvelope> get(String id) {
        return this.rawClient.get(id).thenApply(response -> response.body());
    }

    /**
     * Returns a single connection by ID, with sensitive fields redacted.
     * <p>To inspect the schemas available on this connection, trigger a refresh with
     * <a href="../../../api-reference/schemas/refresh"><code>POST /api/connections/{id}/schemas/refresh</code></a> and
     * track progress via
     * <a href="../../../api-reference/schemas/get-status"><code>GET /api/connections/{id}/schemas/status</code></a>.</p>
     */
    public CompletableFuture<ConnectionResponseEnvelope> get(String id, RequestOptions requestOptions) {
        return this.rawClient.get(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Updates a connection's configuration.
     * <p>Updating a connection is a <strong>full replacement</strong> of its configuration. Any
     * <code>configuration</code> field you omit is cleared. To make a partial change, fetch
     * the current connection with
     * <a href="../../../api-reference/connections/get"><code>GET /api/connections/{id}</code></a>, apply your edits, and send the
     * complete object back.</p>
     * <blockquote>
     * <p>📘 The connection is re-validated against the upstream service after every
     * update. The request will fail if the new credentials or endpoint cannot be
     * reached.</p>
     * </blockquote>
     * <p>Syncs that are already running when the update is submitted are not
     * interrupted; the updated configuration takes effect on their next execution.</p>
     */
    public CompletableFuture<CreateConnectionResponseEnvelope> update(
            String id, UpdateConnectionRequestSchema request) {
        return this.rawClient.update(id, request).thenApply(response -> response.body());
    }

    /**
     * Updates a connection's configuration.
     * <p>Updating a connection is a <strong>full replacement</strong> of its configuration. Any
     * <code>configuration</code> field you omit is cleared. To make a partial change, fetch
     * the current connection with
     * <a href="../../../api-reference/connections/get"><code>GET /api/connections/{id}</code></a>, apply your edits, and send the
     * complete object back.</p>
     * <blockquote>
     * <p>📘 The connection is re-validated against the upstream service after every
     * update. The request will fail if the new credentials or endpoint cannot be
     * reached.</p>
     * </blockquote>
     * <p>Syncs that are already running when the update is submitted are not
     * interrupted; the updated configuration takes effect on their next execution.</p>
     */
    public CompletableFuture<CreateConnectionResponseEnvelope> update(
            String id, UpdateConnectionRequestSchema request, IdempotentRequestOptions requestOptions) {
        return this.rawClient.update(id, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Deletes a connection.
     * <blockquote>
     * <p>🚧 Deleting a connection that is referenced by fieldsets, syncs, bulk
     * syncs, or schedules returns <code>422 connection in use</code> unless you pass
     * <code>force=true</code>. With <code>force=true</code>, the API deletes those dependent
     * resources before removing the connection.</p>
     * </blockquote>
     */
    public CompletableFuture<Void> delete(String id) {
        return this.rawClient.delete(id).thenApply(response -> response.body());
    }

    /**
     * Deletes a connection.
     * <blockquote>
     * <p>🚧 Deleting a connection that is referenced by fieldsets, syncs, bulk
     * syncs, or schedules returns <code>422 connection in use</code> unless you pass
     * <code>force=true</code>. With <code>force=true</code>, the API deletes those dependent
     * resources before removing the connection.</p>
     * </blockquote>
     */
    public CompletableFuture<Void> delete(String id, IdempotentRequestOptions requestOptions) {
        return this.rawClient.delete(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Deletes a connection.
     * <blockquote>
     * <p>🚧 Deleting a connection that is referenced by fieldsets, syncs, bulk
     * syncs, or schedules returns <code>422 connection in use</code> unless you pass
     * <code>force=true</code>. With <code>force=true</code>, the API deletes those dependent
     * resources before removing the connection.</p>
     * </blockquote>
     */
    public CompletableFuture<Void> delete(String id, ConnectionsDeleteRequest request) {
        return this.rawClient.delete(id, request).thenApply(response -> response.body());
    }

    /**
     * Deletes a connection.
     * <blockquote>
     * <p>🚧 Deleting a connection that is referenced by fieldsets, syncs, bulk
     * syncs, or schedules returns <code>422 connection in use</code> unless you pass
     * <code>force=true</code>. With <code>force=true</code>, the API deletes those dependent
     * resources before removing the connection.</p>
     * </blockquote>
     */
    public CompletableFuture<Void> delete(
            String id, ConnectionsDeleteRequest request, IdempotentRequestOptions requestOptions) {
        return this.rawClient.delete(id, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns completion values for parameter fields on a persisted connection.
     * <p>Use this endpoint when the available options for one parameter depend on the
     * connection's saved credentials or previously selected settings. For example,
     * after a connection is authorized, the upstream service may be able to return
     * lists of databases, schemas, or similar selectable values.</p>
     * <p>For new setup flows, prefer
     * <a href="../../../../api-reference/connections/get-type-parameter-values"><code>POST /api/connection_types/{type}/parameter_values</code></a>,
     * which lets you resolve completions before the connection has been created.</p>
     */
    public CompletableFuture<ConnectionParameterValuesResponseEnvelope> getParameterValues(String id) {
        return this.rawClient.getParameterValues(id).thenApply(response -> response.body());
    }

    /**
     * Returns completion values for parameter fields on a persisted connection.
     * <p>Use this endpoint when the available options for one parameter depend on the
     * connection's saved credentials or previously selected settings. For example,
     * after a connection is authorized, the upstream service may be able to return
     * lists of databases, schemas, or similar selectable values.</p>
     * <p>For new setup flows, prefer
     * <a href="../../../../api-reference/connections/get-type-parameter-values"><code>POST /api/connection_types/{type}/parameter_values</code></a>,
     * which lets you resolve completions before the connection has been created.</p>
     */
    public CompletableFuture<ConnectionParameterValuesResponseEnvelope> getParameterValues(
            String id, RequestOptions requestOptions) {
        return this.rawClient.getParameterValues(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns the connection's API consumption over the last 24 hours, broken down by sync when the backend supports it.
     * <p>Not all integrations support usage reporting.</p>
     * <ul>
     * <li><code>callsLast24h</code> is null when the backend does not expose a usage count.</li>
     * <li><code>reportsSyncStats</code> is <code>false</code>, and <code>bySync</code> is empty, when the backend
     * reports a total but cannot attribute calls to individual syncs.</li>
     * </ul>
     * <p>When per-sync stats are available, each entry in <code>bySync</code> carries a
     * <code>categories</code> breakdown. <strong>Category keys and labels are integration-specific.</strong>
     * For example, Salesforce reports <code>rest</code> and <code>bulk</code> categories
     * (collapsing Bulk API v1 and v2 into a single <code>bulk</code> bucket), while another
     * integration may report an entirely different set or none at all. Treat <code>key</code>
     * as an opaque, backend-defined identifier and use <code>label</code> for display; do not
     * assume a fixed vocabulary across connection types.</p>
     */
    public CompletableFuture<GetConnectionUsageEnvelope> getUsage(String id) {
        return this.rawClient.getUsage(id).thenApply(response -> response.body());
    }

    /**
     * Returns the connection's API consumption over the last 24 hours, broken down by sync when the backend supports it.
     * <p>Not all integrations support usage reporting.</p>
     * <ul>
     * <li><code>callsLast24h</code> is null when the backend does not expose a usage count.</li>
     * <li><code>reportsSyncStats</code> is <code>false</code>, and <code>bySync</code> is empty, when the backend
     * reports a total but cannot attribute calls to individual syncs.</li>
     * </ul>
     * <p>When per-sync stats are available, each entry in <code>bySync</code> carries a
     * <code>categories</code> breakdown. <strong>Category keys and labels are integration-specific.</strong>
     * For example, Salesforce reports <code>rest</code> and <code>bulk</code> categories
     * (collapsing Bulk API v1 and v2 into a single <code>bulk</code> bucket), while another
     * integration may report an entirely different set or none at all. Treat <code>key</code>
     * as an opaque, backend-defined identifier and use <code>label</code> for display; do not
     * assume a fixed vocabulary across connection types.</p>
     */
    public CompletableFuture<GetConnectionUsageEnvelope> getUsage(String id, RequestOptions requestOptions) {
        return this.rawClient.getUsage(id, requestOptions).thenApply(response -> response.body());
    }

    public AsyncProxyClient proxy() {
        return this.proxyClient.get();
    }

    public AsyncSharedConnectionsClient sharedConnections() {
        return this.sharedConnectionsClient.get();
    }
}
