/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.polytomic.api.resources.events;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.polytomic.api.core.ClientOptions;
import com.polytomic.api.core.ObjectMappers;
import com.polytomic.api.core.PolytomicApiException;
import com.polytomic.api.core.PolytomicException;
import com.polytomic.api.core.PolytomicHttpResponse;
import com.polytomic.api.core.QueryStringMapper;
import com.polytomic.api.core.RequestOptions;
import com.polytomic.api.errors.InternalServerError;
import com.polytomic.api.errors.UnprocessableEntityError;
import com.polytomic.api.resources.events.requests.EventsListRequest;
import com.polytomic.api.types.ApiError;
import com.polytomic.api.types.EventTypesEnvelope;
import com.polytomic.api.types.EventsEnvelope;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawEventsClient {
    protected final ClientOptions clientOptions;

    public RawEventsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Lists audit events for the caller's organization.
     * <p>Results are paginated. If more events are available, the response includes
     * <code>pagination.next_page_token</code>; pass that token back unchanged to continue from
     * the last item you received.</p>
     * <p>Filter by event type using the <code>event_type</code> query parameter. Pass one of the
     * identifiers returned by <a href="../../api-reference/events/get-types"><code>GET /api/events_types</code></a> to
     * narrow results to a specific category of activity.</p>
     * <blockquote>
     * <p>📘 Events reflect audit activity scoped to the caller's organization.
     * The log captures both user-initiated and API-initiated actions.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<EventsEnvelope> list() {
        return list(EventsListRequest.builder().build());
    }

    /**
     * Lists audit events for the caller's organization.
     * <p>Results are paginated. If more events are available, the response includes
     * <code>pagination.next_page_token</code>; pass that token back unchanged to continue from
     * the last item you received.</p>
     * <p>Filter by event type using the <code>event_type</code> query parameter. Pass one of the
     * identifiers returned by <a href="../../api-reference/events/get-types"><code>GET /api/events_types</code></a> to
     * narrow results to a specific category of activity.</p>
     * <blockquote>
     * <p>📘 Events reflect audit activity scoped to the caller's organization.
     * The log captures both user-initiated and API-initiated actions.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<EventsEnvelope> list(RequestOptions requestOptions) {
        return list(EventsListRequest.builder().build(), requestOptions);
    }

    /**
     * Lists audit events for the caller's organization.
     * <p>Results are paginated. If more events are available, the response includes
     * <code>pagination.next_page_token</code>; pass that token back unchanged to continue from
     * the last item you received.</p>
     * <p>Filter by event type using the <code>event_type</code> query parameter. Pass one of the
     * identifiers returned by <a href="../../api-reference/events/get-types"><code>GET /api/events_types</code></a> to
     * narrow results to a specific category of activity.</p>
     * <blockquote>
     * <p>📘 Events reflect audit activity scoped to the caller's organization.
     * The log captures both user-initiated and API-initiated actions.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<EventsEnvelope> list(EventsListRequest request) {
        return list(request, null);
    }

    /**
     * Lists audit events for the caller's organization.
     * <p>Results are paginated. If more events are available, the response includes
     * <code>pagination.next_page_token</code>; pass that token back unchanged to continue from
     * the last item you received.</p>
     * <p>Filter by event type using the <code>event_type</code> query parameter. Pass one of the
     * identifiers returned by <a href="../../api-reference/events/get-types"><code>GET /api/events_types</code></a> to
     * narrow results to a specific category of activity.</p>
     * <blockquote>
     * <p>📘 Events reflect audit activity scoped to the caller's organization.
     * The log captures both user-initiated and API-initiated actions.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<EventsEnvelope> list(EventsListRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/events");
        if (request.getOrganizationId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "organization_id", request.getOrganizationId().get(), false);
        }
        if (request.getType().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "type", request.getType().get(), false);
        }
        if (request.getStartingAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "starting_after", request.getStartingAfter().get(), false);
        }
        if (request.getEndingBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "ending_before", request.getEndingBefore().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, EventsEnvelope.class), response);
            }
            try {
                switch (response.code()) {
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns the set of event type identifiers supported by GET /api/events.
     * <p>Use the identifiers returned here as the <code>event_type</code> filter value when calling
     * <a href="../../api-reference/events/list"><code>GET /api/events</code></a>.</p>
     */
    public PolytomicHttpResponse<EventTypesEnvelope> getTypes() {
        return getTypes(null);
    }

    /**
     * Returns the set of event type identifiers supported by GET /api/events.
     * <p>Use the identifiers returned here as the <code>event_type</code> filter value when calling
     * <a href="../../api-reference/events/list"><code>GET /api/events</code></a>.</p>
     */
    public PolytomicHttpResponse<EventTypesEnvelope> getTypes(RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/events_types");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, EventTypesEnvelope.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }
}
