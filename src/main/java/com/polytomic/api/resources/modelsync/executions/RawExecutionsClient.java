/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.polytomic.api.resources.modelsync.executions;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.polytomic.api.core.ClientOptions;
import com.polytomic.api.core.IdempotentRequestOptions;
import com.polytomic.api.core.ObjectMappers;
import com.polytomic.api.core.PolytomicApiException;
import com.polytomic.api.core.PolytomicException;
import com.polytomic.api.core.PolytomicHttpResponse;
import com.polytomic.api.core.QueryStringMapper;
import com.polytomic.api.core.RequestOptions;
import com.polytomic.api.errors.BadRequestError;
import com.polytomic.api.errors.ForbiddenError;
import com.polytomic.api.errors.InternalServerError;
import com.polytomic.api.errors.NotFoundError;
import com.polytomic.api.errors.RequestTimeoutError;
import com.polytomic.api.resources.modelsync.executions.requests.ExecutionsGetConsoleLogsRequest;
import com.polytomic.api.resources.modelsync.executions.requests.ExecutionsListRequest;
import com.polytomic.api.types.ApiError;
import com.polytomic.api.types.CancelSyncExecutionResponseEnvelope;
import com.polytomic.api.types.ExecutionConsoleLogsResponseEnvelope;
import com.polytomic.api.types.ExecutionLogType;
import com.polytomic.api.types.ExecutionLogsResponseEnvelope;
import com.polytomic.api.types.GetExecutionResponseEnvelope;
import com.polytomic.api.types.ListExecutionResponseEnvelope;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawExecutionsClient {
    protected final ClientOptions clientOptions;

    public RawExecutionsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Lists executions for a model sync.
     * <p>Results are ordered by start time descending. If more results are available, the
     * response includes <code>pagination.next_page_token</code>; pass that token back unchanged
     * to continue paging.</p>
     * <p>The token is opaque. Do not construct or edit it yourself.</p>
     * <p>For full details about a specific execution — including record counts and error
     * summaries — use
     * <a href="../../../../api-reference/model-sync/executions/get"><code>GET /api/syncs/{sync_id}/executions/{id}</code></a>.</p>
     */
    public PolytomicHttpResponse<ListExecutionResponseEnvelope> list(String syncId) {
        return list(syncId, ExecutionsListRequest.builder().build());
    }

    /**
     * Lists executions for a model sync.
     * <p>Results are ordered by start time descending. If more results are available, the
     * response includes <code>pagination.next_page_token</code>; pass that token back unchanged
     * to continue paging.</p>
     * <p>The token is opaque. Do not construct or edit it yourself.</p>
     * <p>For full details about a specific execution — including record counts and error
     * summaries — use
     * <a href="../../../../api-reference/model-sync/executions/get"><code>GET /api/syncs/{sync_id}/executions/{id}</code></a>.</p>
     */
    public PolytomicHttpResponse<ListExecutionResponseEnvelope> list(String syncId, RequestOptions requestOptions) {
        return list(syncId, ExecutionsListRequest.builder().build(), requestOptions);
    }

    /**
     * Lists executions for a model sync.
     * <p>Results are ordered by start time descending. If more results are available, the
     * response includes <code>pagination.next_page_token</code>; pass that token back unchanged
     * to continue paging.</p>
     * <p>The token is opaque. Do not construct or edit it yourself.</p>
     * <p>For full details about a specific execution — including record counts and error
     * summaries — use
     * <a href="../../../../api-reference/model-sync/executions/get"><code>GET /api/syncs/{sync_id}/executions/{id}</code></a>.</p>
     */
    public PolytomicHttpResponse<ListExecutionResponseEnvelope> list(String syncId, ExecutionsListRequest request) {
        return list(syncId, request, null);
    }

    /**
     * Lists executions for a model sync.
     * <p>Results are ordered by start time descending. If more results are available, the
     * response includes <code>pagination.next_page_token</code>; pass that token back unchanged
     * to continue paging.</p>
     * <p>The token is opaque. Do not construct or edit it yourself.</p>
     * <p>For full details about a specific execution — including record counts and error
     * summaries — use
     * <a href="../../../../api-reference/model-sync/executions/get"><code>GET /api/syncs/{sync_id}/executions/{id}</code></a>.</p>
     */
    public PolytomicHttpResponse<ListExecutionResponseEnvelope> list(
            String syncId, ExecutionsListRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/syncs")
                .addPathSegment(syncId)
                .addPathSegments("executions");
        if (request.getPageToken().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page_token", request.getPageToken().get(), false);
        }
        if (request.getOnlyCompleted().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "only_completed", request.getOnlyCompleted().get(), false);
        }
        if (request.getAscending().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "ascending", request.getAscending().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListExecutionResponseEnvelope.class),
                        response);
            }
            try {
                if (response.code() == 404) {
                    throw new NotFoundError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns a single model sync execution.
     * <p>For the log files produced by this execution, use
     * <a href="../../../../../api-reference/model-sync/executions/get-log-urls"><code>GET /api/syncs/{sync_id}/executions/{id}/{type}</code></a> to retrieve
     * signed URLs grouped by log category.</p>
     */
    public PolytomicHttpResponse<GetExecutionResponseEnvelope> get(String syncId, String id) {
        return get(syncId, id, null);
    }

    /**
     * Returns a single model sync execution.
     * <p>For the log files produced by this execution, use
     * <a href="../../../../../api-reference/model-sync/executions/get-log-urls"><code>GET /api/syncs/{sync_id}/executions/{id}/{type}</code></a> to retrieve
     * signed URLs grouped by log category.</p>
     */
    public PolytomicHttpResponse<GetExecutionResponseEnvelope> get(
            String syncId, String id, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/syncs")
                .addPathSegment(syncId)
                .addPathSegments("executions")
                .addPathSegment(id);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetExecutionResponseEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Requests cancellation of a model sync execution.
     */
    public PolytomicHttpResponse<CancelSyncExecutionResponseEnvelope> cancel(String syncId, String id) {
        return cancel(syncId, id, null);
    }

    /**
     * Requests cancellation of a model sync execution.
     */
    public PolytomicHttpResponse<CancelSyncExecutionResponseEnvelope> cancel(
            String syncId, String id, IdempotentRequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/syncs")
                .addPathSegment(syncId)
                .addPathSegments("executions")
                .addPathSegment(id)
                .addPathSegments("cancel");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, CancelSyncExecutionResponseEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Fetch the latest console log entries for a sync execution. Returns at most the most recent 50 entries retained in Redis.
     */
    public PolytomicHttpResponse<ExecutionConsoleLogsResponseEnvelope> getConsoleLogs(String syncId, String id) {
        return getConsoleLogs(
                syncId, id, ExecutionsGetConsoleLogsRequest.builder().build());
    }

    /**
     * Fetch the latest console log entries for a sync execution. Returns at most the most recent 50 entries retained in Redis.
     */
    public PolytomicHttpResponse<ExecutionConsoleLogsResponseEnvelope> getConsoleLogs(
            String syncId, String id, RequestOptions requestOptions) {
        return getConsoleLogs(
                syncId, id, ExecutionsGetConsoleLogsRequest.builder().build(), requestOptions);
    }

    /**
     * Fetch the latest console log entries for a sync execution. Returns at most the most recent 50 entries retained in Redis.
     */
    public PolytomicHttpResponse<ExecutionConsoleLogsResponseEnvelope> getConsoleLogs(
            String syncId, String id, ExecutionsGetConsoleLogsRequest request) {
        return getConsoleLogs(syncId, id, request, null);
    }

    /**
     * Fetch the latest console log entries for a sync execution. Returns at most the most recent 50 entries retained in Redis.
     */
    public PolytomicHttpResponse<ExecutionConsoleLogsResponseEnvelope> getConsoleLogs(
            String syncId, String id, ExecutionsGetConsoleLogsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/syncs")
                .addPathSegment(syncId)
                .addPathSegments("executions")
                .addPathSegment(id)
                .addPathSegments("consolelog");
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ExecutionConsoleLogsResponseEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 408:
                        throw new RequestTimeoutError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns signed URLs for every log file of a given type on a model sync execution.
     * <p><code>{type}</code> identifies the log category, such as <code>errors</code> or <code>warnings</code>. The
     * response contains a signed URL for each log file in that category.</p>
     * <blockquote>
     * <p>🚧 Signed URLs expire after a short period. If a URL has expired, re-request
     * it from this endpoint. To fetch a single file's URL directly, use
     * <a href="../../../../../../api-reference/model-sync/executions/get-logs"><code>GET /api/syncs/{sync_id}/executions/{id}/{type}/{filename}</code></a>.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<ExecutionLogsResponseEnvelope> getLogUrls(
            String syncId, String id, ExecutionLogType type) {
        return getLogUrls(syncId, id, type, null);
    }

    /**
     * Returns signed URLs for every log file of a given type on a model sync execution.
     * <p><code>{type}</code> identifies the log category, such as <code>errors</code> or <code>warnings</code>. The
     * response contains a signed URL for each log file in that category.</p>
     * <blockquote>
     * <p>🚧 Signed URLs expire after a short period. If a URL has expired, re-request
     * it from this endpoint. To fetch a single file's URL directly, use
     * <a href="../../../../../../api-reference/model-sync/executions/get-logs"><code>GET /api/syncs/{sync_id}/executions/{id}/{type}/{filename}</code></a>.</p>
     * </blockquote>
     */
    public PolytomicHttpResponse<ExecutionLogsResponseEnvelope> getLogUrls(
            String syncId, String id, ExecutionLogType type, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/syncs")
                .addPathSegment(syncId)
                .addPathSegments("executions")
                .addPathSegment(id)
                .addPathSegment(type.toString());
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ExecutionLogsResponseEnvelope.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns a signed URL for a specific log file produced by a model sync execution.
     * <p>The URL is signed and expires after a short period. If it has expired before
     * you download the file, call this endpoint again to obtain a fresh URL.</p>
     */
    public PolytomicHttpResponse<Void> getLogs(String syncId, String id, ExecutionLogType type, String filename) {
        return getLogs(syncId, id, type, filename, null);
    }

    /**
     * Returns a signed URL for a specific log file produced by a model sync execution.
     * <p>The URL is signed and expires after a short period. If it has expired before
     * you download the file, call this endpoint again to obtain a fresh URL.</p>
     */
    public PolytomicHttpResponse<Void> getLogs(
            String syncId, String id, ExecutionLogType type, String filename, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/syncs")
                .addPathSegment(syncId)
                .addPathSegments("executions")
                .addPathSegment(id)
                .addPathSegment(type.toString())
                .addPathSegment(filename);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new PolytomicHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiError.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PolytomicApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new PolytomicException("Network error executing HTTP request", e);
        }
    }
}
